# fixtures/secrets/known_values.txt - 04-Test-Plan.md 13, and 0.5's synthetic key.
#
# THREE SYNTHETIC STRINGS AND NO REAL CREDENTIAL. Each has the SHAPE the secret
# grep of T-U-store-10 and T-N-nfr06-01 searches for and the regular expression
# of T-N-nfr06-02 is written against, and each authenticates against nothing:
# every one is a fixed run of characters, not a value any service ever issued.
# No test reads ~/.config/bugloop/gemini.env and no real key is ever committed.
#
# The GEMINI_API_KEY line is also the value conftest.py puts in the environment
# for every test below T3 (0.5), so a test that somehow reached the network
# would present a key that cannot authenticate rather than one that can.
#
# One `<name> <value>` pair per line; every other line is a comment.
GITHUB_TOKEN ghp_000000000000000000000000000000000000
GEMINI_API_KEY AQ.00000000000000000000000000000000
GOOGLE_API_KEY AIza00000000000000000000000000000000000
