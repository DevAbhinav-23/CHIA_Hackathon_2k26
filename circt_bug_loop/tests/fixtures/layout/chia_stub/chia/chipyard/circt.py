"""Stub of CHIA's chia/chipyard/circt.py: the file 03-LLD.md 1.2 appends to.

Constructed. It holds one function so that a test can assert the append left
what was already there alone, and nothing else: the real file is 834 lines of
CIRCT glue and none of it is what the append is about.
"""


def firtool_lower_chirrtl_to_hw() -> None:
    """The one pre-existing name, so the append can be shown not to remove it."""
