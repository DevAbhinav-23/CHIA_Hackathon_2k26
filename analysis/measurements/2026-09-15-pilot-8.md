# Pilot 8, 2026-09-15: the whole seeded arm, end to end, for USD 1.21

One live run, `registration/pilot-8` (commit 058e884), `budget-pilot.yaml`
unchanged in every value since pilot 4: `campaign_spend_cap_usd: 6.0`,
`arm_window_seconds: 900`, `per_seed_probe_cap: 3`, `per_seed_iteration_cap: 1`,
`filings_*: 0`, `calibration_sample_size: 0`, `max_tool_iterations: {stage_1: 12,
stage_2: 12, stage_6: 6, stage_7: 20}`. Run `138400b18e44411c94f7d6344fa19286`
under `~/bugloop-artefacts/`, started 2026-09-14T20:15:53Z, one seed
`b3b407b37bb2` (Moore), both arms, `--no-repair`; a `--dry-run` of the same
command passed every pre-flight check first.

## 1. The quota probe, which decided the run happened

| UTC | answer |
|---|---|
| 20:10:11 | `ERR RuntimeError Cannot send a request, as the client has been closed` |
| 20:10:33 | `OK 67 2.0` |

The first line is the script's own defect and not the quota's: it built
`genai.Client` as a temporary, google-genai closes its transport when the client
is collected, nothing reached the network, and holding it in a name fixed it.

## 2. Verdict

EVERY STAGE OF THE SEEDED ARM RAN, for the first time in eight pilots. Stage 1
answered, stage 2 was admitted and wrote three probing inputs, one of them fired
the Moore assertion, the reducer reached a fixpoint, and the gate refused the
candidate at question 4 as a duplicate of issue 8508. Four HTTP 429s were waited
out inside their own calls and every one of them cleared on the first re-send.
The campaign spent USD 1.21 of a registered 6.00.

D-5's settlement is now MEASURED LIVE, which is what pilot 7 could not do: stage
1 was authorised 2.729162 and settled at its bill of 0.546450, so stage 2's worst
case of 4.384177 was admitted at 4.930627. Unsettled the sum would have been
7.113339, which is exactly how pilot 6 lost stage 2.

## 3. What ran

| | |
|---|---|
| seeds | 1 of 187: `b3b407b37bb2` (Moore) |
| arms | both opened; both stopped `seed_set_exhausted` |
| turns answered | stage 1, stage 2; stage 6 skipped by the duplicate screen |
| 429s retried | 4, all cleared on retry 1 of 6 |
| probes written | seeded 3 of a cap of 3 (a fourth write call, truncated) |
| probe outcomes | 1 `assertion`, 2 `parse_error:tool_rejected_input` |
| candidates | 1, contaminated in both symbol and file |
| wall | seeded arm 416.3 s of its 900, mutation 0.1 s; image 11.1 s |
| results artefact | rendered, 366 lines |

## 4. The money, per turn and per phase

| stage | A | B | C | calls | authorised | settled | billed | ratio |
|---|---|---|---|---|---|---|---|---|
| stage_1 | 12 | n/a | 1 | 13 | 2.729162 | 0.546450 | 0.546450 | 0.200 |
| stage_2 | 12 | 4 | 1 | 17 | 4.384177 | 0.659992 | 0.659992 | 0.151 |

* Both turns billed a fifth of their authorisation, neither reached the
  backend's own per-turn ceiling, and every turn is under USD 1.
* The 429 waits, as the streams carry them: stage 1 18.5 s and 22.8 s, stage 2
  21.9 s and 22.4 s - each one 20 s jittered by +/- 20%, none doubled.
* LEDGER AGAINST SDK. The ledger totals USD 1.2064 and the two `usage.json`
  files total 1,475,009 input and 26,716 output tokens, which at the registered
  0.75 and 3.75 per million is 1.206442. They agree to the rounding.

## 5. The candidate, and the gate

`circt-opt --convert-moore-to-core` aborted with SIGABRT on
`isa<To>(Val) && "cast<Ty>() argument of incompatible type!"` at
`/opt/circt-sdk/include/llvm/Support/Casting.h:566`, over a three-op module
whose only interesting line is `moore.net wire : <open_array<i1>>`.

| question | answer |
|---|---|
| 1 reproduce | true, on a different worker than the original |
| 2 minimal | true; `circt-reduce` 22 interestingness calls, `fixpoint`, 114 -> 101 bytes |
| 3 valid | true, basis `parsed`, exit 0 |
| 4 new | FALSE: `known_open_issue`, llvm/circt#8508, matched on the assertion text |

Stage 6 was skipped, which is the screen's own rule: the duplicate verdict
already decides the classification, so `report.md` carries the driver's sentence
and `llm_report_write.usage.json` is `{}` because no turn was made. The mutation
arm answered `stale_at_build` again and wrote nothing.

## 6. Defects found

**D-9 (new, reporting). A turn that never happened is counted as unpriced.**
`results.md` reports "Unpriced turns: 3" and says the figure is what its USD
lower bound is a lower bound BY. All three are metered model-stage ledger rows
for turns that were never dispatched: stage 6 skipped by the screen (two rows)
and the mutation arm's `stale_at_build`. The true number of turns that consumed
tokens without reporting them is zero, and the sentence claims otherwise.

## 7. What this does not claim

That D-7 holds live. It records the settlement of a turn that RAISES and no turn
raised here: both `usage.json` files carry observed counts. It is exercised by
tests only, as D-5 was until this pilot. Nor that the backoff has been seen to
double: every refusal cleared on the first re-send, so the 40 s wait, the 300 s
cap, the `Retry-After` path and the exhaustion that re-raises are tests alone.
