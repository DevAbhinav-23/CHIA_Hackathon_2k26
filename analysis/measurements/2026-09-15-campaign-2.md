# Campaign 2, 2026-09-15: the registered campaign, three attempts, three new bugs through the gate

**Registration** `registration/campaign-2` on cb75a0f: `budget.yaml` with `arm_window_seconds` 7200, `campaign_spend_cap_usd` 100 per job,
filings 3/day and 10 total, `max_tool_iterations` 12/12/6/20, `minimal_case_lines` 12, corpus head d7e94049, mutator set `set_v2.json`.
Three jobs in parallel on `cluster_single.yaml` (2 llm, 2 circt, 1 repair): seeded `--shard 0/2`, seeded `--shard 1/2`, mutation. `--no-repair`.

**Attempts.** Attempt 1 (05:29 IST, runs `11f54337`, `81b06e65`, `df996900`) stopped by the architect at 32 min: the feedback error line was
truncated before its message (D-14 fix round). Attempt 2 (06:20 IST, runs `f6d5148b`, `9748aa2b`, `460b7b48`) stopped at 45 min: gate deadlock,
the seeded generator held a `circt` slot it never used (D-12); the mutation run `460b7b48` had already completed (`seed_set_exhausted`, 13 min) and
stands as the campaign's mutation arm. Attempt 3 (07:23 IST, runs `f1e4fef5`, `73effefc`) ran both shards to `arm_window`.

**Post-run, no model turn re-run:** `--reclassify` (verifier-error oracle, contract 2.4) over all nine stored runs; `--rescreen` (symbol-level post-pin,
generic-text corroboration, verifier op+constraint corroboration, earliest-candidate duplicates) over the seeded runs; two stage-6 report turns
(`--report-turns f1e4fef5`, USD 0.25) for the verifier candidates; `--render` for every run. This file's tables are `campaign_aggregate.py`'s output
on `circt_bug_loop/loop.db` at 2026-09-15 12:40 IST.

## Per run and arm

```
run       label           arm       seeds probes parse verif fired cands   spend turns
903a37c8  c1 both(stopped)seeded        1      5     5     0     0     0    1.56     3
11f54337  c2a s0          seeded        1      2     0     1     1     1    2.12     2
81b06e65  c2a s1          seeded        1     15    13     0     0     0    2.23     6
df996900  c2a mut         mutation      3     39    21     0     0     0    0.00     0
f6d5148b  c2b s0          seeded        1      5     3     0     1     1    1.79     2
9748aa2b  c2b s1          seeded        1      5     1     0     0     0    0.44     2
460b7b48  c2 mut          mutation     96   1154   400     0     0     0    0.00     0
f1e4fef5  c2c s0          seeded        5     75    43    10    14    14   24.07    33
73effefc  c2c s1          seeded        4     59    29     0     1     1   20.66    24

=== latest verdict / decision per candidate, all runs ===
82ac21ae c-p-f8fb884cd8e3 assertion      ude/llvm/Support/Casting.h:566 known_open_issue       nothing  8508
82ac21ae c-p-5cb54ccdbc8e assertion      ude/llvm/Support/Casting.h:566 known_open_issue       nothing  8508
82ac21ae c-p-c9ad43924cd8 assertion      ude/llvm/Support/Casting.h:566 known_open_issue       nothing  8508
138400b1 c-p-4b1c72262db2 assertion      ude/llvm/Support/Casting.h:566 known_open_issue       nothing  8508
f6d5148b c-p-e43f98e60180 assertion      /lib/Dialect/HW/HWOps.cpp:1995 duplicate_of_candidate -        c-p-a2cb61b8d0aa
f1e4fef5 c-p-a2cb61b8d0aa assertion      /lib/Dialect/HW/HWOps.cpp:1995 new                    report   
73effefc c-p-d87586542f92 assertion      k/include/llvm/ADT/APInt.h:121 fixed_post_pin         nothing  ddb3d1bd
f1e4fef5 c-p-9d355cf4738c assertion      IR/StorageUniquerSupport.h:180 fixed_post_pin         nothing  ddb3d1bd
f1e4fef5 c-p-25b60ea03b72 assertion      IR/StorageUniquerSupport.h:180 duplicate_of_candidate nothing  c-p-9d355cf4738c
f1e4fef5 c-p-ca807929eac5 assertion      IR/StorageUniquerSupport.h:180 duplicate_of_candidate nothing  c-p-9d355cf4738c
f1e4fef5 c-p-075cf1d70f3a verifier_error comb.extract                   new                    report   
f1e4fef5 c-p-356e9e6e061d verifier_error llhd.sig.extract               new                    report   
f1e4fef5 c-p-65532caeb392 verifier_error comb.extract                   duplicate_of_candidate nothing  c-p-075cf1d70f3a
f1e4fef5 c-p-66f526eeffa9 verifier_error comb.extract                   duplicate_of_candidate nothing  c-p-075cf1d70f3a
f1e4fef5 c-p-6bfe37f97515 verifier_error llhd.sig.extract               duplicate_of_candidate nothing  c-p-356e9e6e061d
f1e4fef5 c-p-837603178ad9 verifier_error llhd.sig.extract               duplicate_of_candidate nothing  c-p-356e9e6e061d
f1e4fef5 c-p-91b644bf0725 verifier_error llhd.sig.extract               duplicate_of_candidate nothing  c-p-356e9e6e061d
f1e4fef5 c-p-9ecf8d5f8ce2 verifier_error llhd.sig.extract               duplicate_of_candidate nothing  c-p-356e9e6e061d
f1e4fef5 c-p-de001b86303c verifier_error llhd.sig.extract               duplicate_of_candidate nothing  c-p-356e9e6e061d
f1e4fef5 c-p-e1f81510dc52 verifier_error comb.extract                   duplicate_of_candidate nothing  c-p-075cf1d70f3a
11f54337 c-p-af7721b8af25 verifier_error comb.extract                   duplicate_of_candidate nothing  c-p-075cf1d70f3a
```

## The campaign (attempt 3 seeded shards + the mutation run), as the paper's Table II

| Measure | seeded | mutation |
|---|---|---|
| Seeds probed | 9 (5 + 4) | 96 (87 more skipped `stale_at_build`) |
| Probes | 134 (75 + 59) | 1,154 |
| Rejected by the parser | 72 (43 + 29) | 400 |
| Compiler's output rejected by its verifier | 10 | 0 |
| Assertion failures | 5 | 0 |
| Candidates | 15 | 0 |
| Distinct new through the gate (`report`) | 3 | 0 |
| Duplicates of those | 9 | 0 |
| Fixed after the pin, held for review | 2 (+2 duplicates) | 0 |
| Filed | 0, awaiting the human approver | 0 |
| Model turns; billed / authorised mean, max | 55; 0.18, 0.41 | 0 |
| Spend, USD list; cached share of input tokens | 44.49; 67.9 % (37.7 M of 55.6 M) | 0 |
| Wall | 2 × 7,200 s | 780 s |

## The three through the gate

| candidate | class | mechanism | report title (model, stage 6) |
|---|---|---|---|
| `c-p-a2cb61b8d0aa` | assertion, `HWOps.cpp:1995` | `moore.extract … from -2147483648 : array<2 x i32> -> array<4 x i32>` under `--convert-moore-to-core` builds a zero-element `hw.array_concat` | [MooreToCore] Prevent empty array concat when lowering extract with negative offset |
| `c-p-075cf1d70f3a` | verifier_error, `comb.extract` | dynamic extract lowering emits `comb.extract` with an aggregate result type | [MooreToCore] Prevent emitting comb.extract with non-integer result types during dyn_extract lowering |
| `c-p-356e9e6e061d` | verifier_error, `llhd.sig.extract` | extract-of-reference lowering emits `llhd.sig.extract` with an aggregate ref type | [MooreToCore] Prevent emitting llhd.sig.extract with non-integer result types |

All three: gate q1 reproduced in a fresh process on another worker, q2 minimal at the reducer's fixpoint, q3 valid, q4 no issue among 2,652 and
no fixing commit since the pin. Held for `python -m circt_bug_loop.approve`. The `StorageUniquerSupport.h:180` trio is `fixed_post_pin` (LLVM bump
touching a frame symbol) after the generic-text rule dropped #9574; the `APInt.h:121` candidate is `fixed_post_pin` the same way; both for human review.

## Repair

One attempt on `c-p-a2cb61b8d0aa` (`2026-09-15-repair-run.md`): assess diagnosed the 32-bit overflow and decided CLEAR; reproduce wrote its own variant
and scored `no_repro`; no patch. Model use of the chain is unobservable (authorised 32.4 at the ceiling).

## Spend of the whole project, list price

synthesis 0.98 + pilots 12.13 + campaign 1 (stopped) 1.56 + campaign 2 attempts 1 and 2 6.58 + attempt 3 44.49 + report turns 0.25 = **USD 66.0**,
plus one unmeasured synthesis attempt (0.47 to 0.53) and the repair attempt (unobserved). Google's bill is lower by the cached share.
