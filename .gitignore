# secrets and local env
api.md
*.env
.env*
# paper build products
paper/*.aux
paper/*.log
paper/*.bbl
paper/*.blg
paper/*.out
paper/main.pdf
# python
__pycache__/
*.pyc
.pytest_cache/
# loop runtime state (never commit)
loop.db
loop.db-*
issues.db*
artefacts/
# the staged package the workers run, rebuilt at every start-up (W6, K7)
circt_bug_loop/_shipped/
