[pytest]
testpaths = circt_bug_loop/tests
markers =
    t0: no CIRCT: Python 3.10 and this repository, no binary, no clone, no network (04-Test-Plan.md 0.5)
    t1: the release SDK, the measured assertions-on build, or the blobless llvm/circt clone
    t2: the assertions-on image and a Docker daemon
    t3: the single-machine cluster of 03-LLD.md 12.1
    needs_sdk: the tier-1 resource marker of 04-Test-Plan.md 0.5's table
    needs_image: the tier-2 resource marker of 04-Test-Plan.md 0.5's table
    needs_cluster: the tier-3 resource marker of 04-Test-Plan.md 0.5's table
