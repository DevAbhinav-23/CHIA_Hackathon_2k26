# Implementation errata log (running; consumed by the pre-red-team errata pass)

Each row: task, what the code does that the design did not say or said differently, and the design section owed a fix. Design files are not edited from this log until the batch pass before W-20.

## W-05 corpus builder (2026-09-14)
| # | Finding | Owed to | Status |
|---|---|---|---|
| 1 | `build_corpus(clone_path, corpus_head_sha, since, inline_cap_bytes, timeout_seconds=1800) -> dict` (LLD §3.3 signature kept; the work-plan brief's `list[SeedRecord]` form was wrong) | 05 §9 | folded: 03 §16.2 (§3.3 Returns), 03 §3.3 |
| 2 | The git-command table cited as "§4.12" lives unnumbered inside LLD §4.11.1; RUN-line normalisation is §4.1/§4.2, not §4.4 | 03 numbering | folded: 03 §16.2 (§4.11.1, §3.3) |
| 3 | FR-01.7 "nearest tag" has no schema field; returned as a top-level `nearest_tag` map; `sdk_tag` null on the 16 inexact seeds | 01 FR-01.7, 03 §3.3 | folded: 01 §1.10 FR-01.7, 01 FR-01.7 in place, 03 §16.2 (§2.4, §3.3) |
| 4 | Multi-tool seeds (27) take the first RUN line's tool in `test_paths` order; M1's per-tool partition is not reconstructible from its script, membership is | 01 FR-01.3, measurements M1 | folded: 01 §1.10 FR-01.3, 01 FR-01.3 in place |
| 5 | FR-01.2's acceptance seed `f2b15a44ec70` is not in the 187 (no bug-ish subject word); its RUN line has two spaces where the AC quotes one | 01 FR-01.2 | folded: 01 §1.10 FR-01.2, 01 FR-01.2 in place |
| 6 | `split-file` lines carry a meaningless tool/argv; only `shape=split_file` is informative | 03 §4.1 | folded: 03 §16.2 (§4.1) |
| 7 | A quoted `;` inside an option value is rejected by step 6 as written (conservative) | 03 §4.1 | folded: 01 §1.10 FR-01.10, 01 FR-01.10 AC in place, 03 §16.2 (§4.1) |
| 8 | Shell redirects (`>`) survive normalisation as argv tokens (10 lines, 4 seeds) | 03 §4.1 | folded: 01 §1.10 FR-01.10, 01 FR-01.10 AC in place, 03 §16.2 (§4.1) |
| 9 | `env … not …` ordering: strip either wrapper until neither leads | 01 FR-01.10 | folded: 01 §1.10 FR-01.10, 01 FR-01.10 step 2 in place |
| 10 | Exclusion precedence: `seed_text_over_cap` > `no_run_line` > `unsupported_shape` | 03 §3.3 | folded: 03 §3.3 in place, 03 §16.2 |
| 11 | No real RUN line carries a backtick or `$(`; those step-6 cases are constructed | 04 corpus tests | folded: 04 §13 `fixtures/corpus/runlines/` already says so; no change owed |
| 12 | Node returns `counters` (CounterBlock), `exclusions`, `nearest_tag`, `sv_seeds` and extra count keys | 03 §3.3 Returns | folded: 03 §3.3 Returns block in place, 03 §16.2 |
| 13 | "Byte-identical output" (T-U-corpus-19) means records plus counts; `CounterBlock.seconds` is wall clock | 04 | folded: 04 §16.7 counts table (corpus 24 to 38) |
| 14 | `m1_runlines.py`'s `circt-capi-.*` swallowed trailing text; copied as `circt-capi-[\w-]*` | measurements M1 | not a design row: a fix to a script in `analysis/`, outside the six files |
| 15 | New test ids T-U-corpus-25..36; `resolve_sites` + T-U-corpus-24 implemented | 04 §1 corpus | folded: 04 §16.7 counts table, 04 §1.4 heading 24 to 38 |
| 16 | Open: W-05b (A-02 subject-proxy precision, 30 seeds), W-05c (A-19 differential applicability); `fetch_clone.sh` and the raw-JSON fixture copy not created (`filtered_187.json` used instead) | 05 | open code: 04 §16.8 item 9 (`fetch_clone.sh`, the raw-JSON copy). W-05b and W-05c are 05 §9 scope, outside this pass |

## W-06 store, budget, ledger (2026-09-14)
| # | Finding | Owed to | Status |
|---|---|---|---|
| 1 | **Calling a `@ChiaFunction` node directly starts local Ray via the profiler and raises `FutureWarning` under `-W error`.** Architect's decision: nodes stay decorated per LLD §3.2; unit tests call `fn._chia_original` through a `conftest.call_node` helper. W-06 left `load_budget`, `accrue`, `artefact_write` undecorated pending this; re-decorate in the follow-up | 04 §0.4, 03 §3.2 | folded: 04 §16.7 (§0.4 `call_node`, `no_local_ray`); discharged in code by W-07 #12 |
| 2 | `snapshot(ledger, arm, budget)`: `BudgetLedger` has no cap field | 03 §3.11 | folded: 03 §3.11 table, 03 §16.2 |
| 3 | `accrue(entry, db_path, budget)`: `price` needs the `BudgetFile` | 03 §3.11 | folded: 03 §3.11 table, 03 §16.2 |
| 4 | `accrue` never sets `metered` (FR-14.8 flag owned by the caller); T-U-ledger-08/10 written to that reading | 04 ledger tests | folded: 04 §16.7 (§1.16 ledger rows), 03 §3.11, 03 §16.2 |
| 5 | `load_budget` takes keyword-only `run_start_utc`, `manifest_budget_file_sha`, `exact_pin_shas` for checks 1, 4, 5 | 03 §3.11, §9.2 | folded: 03 §3.11 table, 03 §16.2 |
| 6 | FR-05.2's mutator-set rule runs inside check 1; `_CHECKS` stays six; set path derived from the budget file's directory | 03 §9.2, §8.1 | folded: 03 §9.2 check 1 in place, 03 §16.2 |
| 7 | §9.2 heading says "five checks", body says six | 03 §9.2 | folded: 03 §9.2 in place, 03 §1.3 listing now says "the six checks" |
| 8 | §2.9 "eleven reduction fields" is nine | 03 §2.9 | folded: 03 §16.2 (§2.9) |
| 9 | Not every table carries `run_manifest_id`: 9 do, 10 join via probe/candidate ids, `image` and `issue_mirror` carry neither | 03 §6.2 | folded: 03 §16.2 (§6.2) |
| 10 | "Completion record" = a `probe_result` or `candidate` row whose `artefact_dir` equals the directory (`probe` excluded) | 03 §6.5 | folded: 03 §16.2 (§6.5) |
| 11 | `artefact_write(..., store=)`; marker protocol: `relative_path == "PARTIAL"` with `b""` writes, `None` removes; any other write creates the marker when absent | 03 §2.9, §6.5 | folded: 03 §16.2 (§2.9, §6.5) |
| 12 | `aggregate` reads `filings_*` from `filing`; `inputs_today` = stage-3 occupancy entries per UTC day per arm; keyword-only `today` | 03 §3.11 | folded: 03 §3.11 table, 03 §16.2 |
| 13 | `stop_reason` order fixed: `campaign_spend_cap`, `arm_window`, `generated_inputs_per_day`, `filings_total`, `filings_per_day` (`STOP_REASONS`) | 03 §3.11 | folded: 03 §3.11 table, 03 §16.2 |
| 14 | FR-14.4 "one `arm_window` entry per arm per run" enforced in `accrue` (`E005`) | 03 §3.11 | folded: 03 §3.11 table, 03 §16.2 |
| 15 | `ledger.py` imports `BudgetLedger` from `store.py`; `test_layout.py` rule (2) must exempt `BudgetLedger` and `LoopStore` | 03 §2.9, 04 layout | folded: 03 §1.3 rule (2) exemption in place, 03 §16.2 |
| 16 | `CandidateRecord` has 38 fields, not 41; `candidate` table has 20 columns (correct) | 03 §2.9 | folded: 03 §16.2 (§2.9), 02 §5.3 in place |
| 17 | `load_candidate` on a `differential` candidate: `frames_*` 0/0 and null `repro_command` when no `oracle_verdict` row | 03 §2.9 | folded: 03 §16.2 (§2.9) |
| 18 | Fixtures: malformed budget variants derived at test time from the committed `budget.yaml`; throwaway repos built by a Python helper; `secrets/known_values.txt` pending, T-U-store-10 values inline | 04 §13 | folded in part: 04 §13 unchanged; open code: 04 §16.8 item 6 (`secrets/known_values.txt`) |
| 19 | Trap: `git log --format=%cI` emits `Z`, rejected by Python 3.10 `fromisoformat`; `budget._iso` normalises | 03 §3.11 | folded: 03 §9.2 check 1 in place, 03 §16.2 (§3.11) |

## W-02/W-03 contract (2026-09-14)
Recorded directly in LLD §16 and test plan §16.7.

## W-09 crash fixtures (2026-09-14): real-output findings, architect's decisions
| # | Finding | Decision | Owed to | Status |
|---|---|---|---|---|
| 1 | §3.7.1 step 2 cuts at the first depth-zero `(`, so `(anonymous namespace)::X::f` normalises to `""` and is skipped; every trace loses a frame; `fatal_error` fingerprints collapse to `main` | Strip a leading `(anonymous namespace)::` (and any `(anonymous namespace)::` segment) before the cut; keep the qualified name | 03 §3.7.1; probe_task `_normalise_function` | folded: 03 §16.2 (§3.7.1); discharged in code by W-08 #1 |
| 2 | §3.6.2 step 4 `out_of_scope_root` reads only the first `#n` line of frames sharing one address (inlined chain); 3 of 4 mined bugs wrongly out of scope | Frames sharing an address form one group; the group is in scope if any member resolves to a CIRCT source file; the fingerprint frame is the group's deepest CIRCT member | 03 §3.6.2; probe_task `out_of_scope_root` | folded: 03 §16.2 (§3.6.2 step 4); discharged in code by W-08 #2 |
| 3 | `strip_probe_only_options` only knows `--verify-diagnostics`/`--split-input-file`; CIRCT tests also write the single-dash forms, which survive into probe argv | Accept both dash spellings (bare and `=`-valued) | 03 §3.3/§4.2; corpus.py | folded: 03 §3.3 in place, 03 §16.2; discharged in code by W-07 #11 |
| 4 | Fixture root is `tests/fixtures/crashes/` (brief and crash_01); plan §5.2/§13 and T-E-input-02 say `fixtures/oracle/<class>_<nn>/` | Keep `crashes/`; amend the plan | 04 §5.2, §13 | folded: 04 §5.2 and §13 (`fixtures/crashes/`), 04 §4 references |
| 5 | Six fixtures (crash_01, assertion_01..04, fatal_error_01), two SDK tags (1.143.0, 1.156.0); `_ASSERT_UNREACHABLE` still unexercised on real output; calibration-mode build costs measured: 14 s to 601 s per parent build | recorded, no decision needed | 01 A-01 note; 05 | folded: 01 §1.10 A-01, 01 A-01 in place; 04 §5.2 (six fixtures, two SDK tags) |

## W-07 / follow-ups (2026-09-14): `pin_select.py`, W-09 #3, W-06 #1
| # | Finding | Owed to | Status |
|---|---|---|---|
| 1 | **§3.4's signature cannot reach `main`.** `select_release_pinned_main(clone_path, timeout_seconds=600)` walks `HEAD`, which §4.11.1's table spells for every pin-walk command, but the head's blobless clone is detached at `corpus_head_sha` while A1 holds it (FR-01.11), and `HEAD` is then the corpus head and not `main`. Implemented with a keyword-only `ref: str = "HEAD"`, and the acceptance run passes `origin/main` | 03 §3.4, §4.11.1 | folded: 03 §3.4 signature and prose in place, 03 §16.2 |
| 2 | `corpus._walk_pins` gained the same `ref="HEAD"` parameter, and `pin_select.py` **imports** `_Git`, `_read_tags` and `_walk_pins` across the module boundary rather than copying them, which is what §4.11.1 listing §3.3's and §3.4's commands once means. Any `test_layout.py` rule about private names must exempt these three | 03 §3.3, §3.4; 04 layout | folded: 03 §1.3 exemption and §3.3 in place, 03 §16.2 |
| 3 | **Two refusals, not one.** §3.4 names `PinSelectError("no_match")` only, and T-U-pin-05 raises it on an empty tag list; the shared `corpus._read_tags` already separates that case, and the operator's next action differs (a clone fetched without `--tags`, not a `main` that drifted), so an empty tag map raises `PinSelectError("no_tags")` naming the refspec and the walk never starts | 03 §3.4, 04 §1.5 T-U-pin-05 | folded: 03 §3.4 Raises in place, 01 §1.10 FR-02.5, 04 §16.7 (§1.5) |
| 4 | FR-02.5's "24 months" has no number. Implemented as `pin_select.WINDOW_DAYS = 730`, measured from the **head commit's own date** and not from now, so the window a run examines is a property of the clone rather than of the day the run happens | 01 FR-02.5, 03 §3.4 | folded: 01 §1.10 FR-02.5, 01 FR-02.5 in place, 03 §3.4 |
| 5 | **F-02's preconditions name a network the code does not use.** F-02 says "network access to the `llvm/circt` git remote and the GitHub releases API"; the selector reads a local clone only, and takes releases from `for-each-ref refs/tags/firtool-*`, never from the releases API. The fetch is the operator's, before the run | 01 F-02 preconditions | folded: 01 §1.10 F-02 preconditions, 01 F-02 preconditions struck in place |
| 6 | F-02's ACs are written against the clone "at `b792c772`", which is this host's clone's detached HEAD and one first-parent commit behind `origin/main`. Both ACs hold at either point (the pin `e297b52ec9d8` has no release at both), but the lag differs: 7 commits from `b792c772`, 8 from `origin/main` | 01 F-02 ACs | folded: 01 §1.10 F-02 acceptance, 01 F-02 feature acceptance in place |
| 7 | **The eight fields do not land in the manifest as eight.** §3.4 says B12 "writes all eight fields into the `RunManifest`"; `RunManifest.run_commit` is `list[RunCommit]`, so the returned `run_commit` string must be wrapped, and `resolved_utc` has no manifest field at all. Seven map, one wraps, one has nowhere to go | 03 §3.4, §2.4 | folded: 03 §3.4 in place, 03 §16.2 (§2.4, §2.7) |
| 8 | `lag_days` is returned unrounded; W-04's 4.807 is `round(lag_days, 3)`. The tier-1 test asserts the rounded figure and the raw one against the two `%ct` values | 03 §3.4, measurements W-04 §1 | folded: 03 §3.4 in place, 03 §16.2 |
| 9 | **The pin fixtures are recorded git output, not rows of `pin_window_raw.json`.** Plan §13 gives `fixtures/pin/` four files derived from that JSON; the fixtures are at `tests/fixtures/pin_select/` and each replays the six git commands of §4.11.1 verbatim, because the recorded form exercises `corpus._read_tags` and `_walk_pins` and a parsed-window fixture would step over exactly the code §3.4 says A2 shares with A1. `make_fixtures.py` is committed beside them, which is §13's own rule | 04 §13, §1.5 | folded: 04 §13 `fixtures/pin_select/` row |
| 10 | Test ids: T-U-pin-02's criterion (the lag against `rev-list --first-parent --count`) is discharged **inside** the one tier-1 test, T-U-pin-08, rather than as a second live test; T-U-pin-09 (every window released) and T-U-pin-10 (the `_chia_options` row, the six argument vectors, §3.1's paragraphs) are new. Nine ids over eleven pytest items, T-U-pin-07 being parameterised over three fixtures | 04 §1.5 | folded: 04 §16.7 counts table, 04 §1.5 heading 8 to 9 |
| 11 | W-09 #3 **discharged**. `_PROBE_ONLY_OPTIONS` is four spellings, not two. The reach is measured, not assumed: 46 of M1's 331 corpus `RUN:` lines spell one of the two options with a single dash (`raw/m1-per-runline.csv`). New ids T-U-corpus-37 (three recorded lines) and T-U-corpus-38 (`crashes/assertion_02/argv.json` fed back through the function) | 03 §3.3/§4.2, 04 §1 corpus | folded: 03 §3.3 in place, 03 §16.2; 04 §16.7 counts table (corpus-37, -38) |
| 12 | W-06 #1 **discharged**. `load_budget`, `accrue` and `artefact_write` carry `@ChiaFunction(max_retries=0)` again and their tests call `conftest.call_node`. `test_corpus.py`'s tier-1 tests were calling `build_corpus` and `resolve_sites` through the wrapper and carried a `_RAY_WARNING` mark to silence what Ray's startup then raised under `-W error`; both are gone, and the module is 7 s faster | 03 §3.2, 04 §0.4 | folded: 04 §16.7 (§0.4 `call_node`); closes W-06 #1 |
| 13 | `conftest.py` gains one session finaliser, `no_local_ray`, asserting `ray.is_initialized()` is false at session end and naming `call_node` in the message; it stands down when `BUGLOOP_CLUSTER` or `CHIA_LIVE_CLUSTER` is set, the two tiers that legitimately hold a Ray. §0.4 should name both `call_node` and this check, since §0.4 as written (a plain call to the wrapper) is what the check now forbids | 04 §0.4 | folded: 04 §16.7 (§0.4 `no_local_ray`) |

## W-08 follow-ups (2026-09-14): the two frame fixes, the contract fixtures, B4
| # | Finding | Owed to | Status |
|---|---|---|---|
| 1 | **W-09 #1 discharged.** `_normalise_function` strips every `(anonymous namespace)::` segment before the depth-zero cut. Effect on the six recorded fixtures: two fingerprint frames stopped collapsing to `main circt-opt.cpp` (`assertion_01` to `VariableOpConversion::matchAndRewrite MooreToCore.cpp`, `fatal_error_01` to `ClassNewOpConversion::matchAndRewrite MooreToCore.cpp`) and two evidence tuples lost a blank slot (`assertion_01` slot 2, `assertion_02` slot 4 to `SVTraceIVerilogPass::runOnOperation`). New id `T-U-probe-52` | 03 §3.7.1 | folded: 03 §16.2 (§3.7.1), 04 §16.7 counts table (T-U-probe-52) |
| 2 | **W-09 #2 discharged.** `_address_groups` groups consecutive frames sharing one address; `root_in_scope` is true when any member of the ROOT group is in a CIRCT object, and `_fingerprint_frame` takes the **last-listed** usable CIRCT member of the first group that has one. `out_of_scope_root` flips true to false on **four** of the six fixtures: `assertion_02`, `assertion_03`, `assertion_04` and `crash_01`. `assertion_01` was already in scope and `fatal_error_01` stays out (its root group is one unresolved SDK `.so` frame), so W-09's prediction of "3 of 4 assertions" is exactly right. `assertion_03` and `assertion_04` also move their fingerprint frame from `verifyResult ElaborateObject.cpp` to `ElaborateObjectPass::elaborateClass ElaborateObject.cpp`, the last-listed member of the group that owns the address. New id `T-U-probe-53` | 03 §3.6.2 step 4, §3.7.1 | folded: 03 §16.2 (§3.6.2 step 4), 04 §5.2 (`out_of_scope_root`), 04 §16.7 (T-U-probe-53) |
| 3 | `expected.json` **gains `out_of_scope_root`**, which nothing pinned before, so W-09 #2's rule has a regression guard of its own; `w09_mkfixture.py` records it from now on. The six were re-derived with `analysis/measurements/w09_oracle.py`, the independent transcription, amended to the same two decisions; it reproduced the previous recording byte for byte before the amendment, and the loop's own classifier agrees with it field for field after. `stderr.txt`, `input.*`, `argv.json`, `commit.json` and `README.md` are untouched in all six | 04 §5.2, §13 | folded: 04 §5.2 in place, 04 §13 `fixtures/crashes/` row |
| 4 | **W-08 errata 3 discharged.** `contract/fixtures/probe_spec/seeded_01.json`'s `argv` no longer repeats the tool: §4.1 builds the invocation as `<binary> *spec.argv` and `corpus.normalise_run_line` returns an argv that excludes it | 03 §4.1, §4.2 | folded: 03 §16.2 (§4.1, §4.2) |
| 5 | **W-08 errata 4 discharged.** `contract/fixtures/probe_result/assertion_01.json`'s `assertion_text` is §3.6.2 step 2's `expr` group. The constructed line it held would **not have matched `_ASSERT_GLIBC` at all**: it carried no `func` field, and the pattern requires `<func>: Assertion \`` between the site and the expression. `assertion_site` is left as recorded (`LowerTypes.cpp:412`), which is neither stderr-verbatim nor §3.7.1-normalised; the fixture is constructed and W-17 replaces it | 04 §16.7, 03 §3.6.2 | open code: 04 §16.8 item 5 (`probe_result/assertion_01.json`) |
| 6 | **B4's signature.** §3.6.3 gives `oracle_differential(spec, build, image_spec, artefact_dir)`. Implemented with three keyword-only additions: `limits` (the two simulator runs are bounded by the **probe** limits through `circt_exec_probe`, not by B4's 1800 s node timeout), `bin_dir` and `verilator` (every fixture and tier-1 measurement runs a build outside the image) | 03 §3.6.3 | folded: 03 §3.6.3 signature in place, 03 §16.2 |
| 7 | **The work-plan brief's return shape is not the schema's.** The brief asks for "an `OracleVerdict` of class `differential` with the divergence index, both outputs' hashes, both tool versions and `report_only=True`". `DifferentialVerdict` is what §3.6.3 returns and it has no such columns. Where each actually lives: the divergence index is `first_divergent_cycle` plus `first_divergent_signal` (the positional index is returned by `compare_traces` and is not a column); `report_only` is a `CandidateRecord` field the head writes (FR-08.5, FR-08.10); the arcilator side's version is `ImageSpec.circt_sha` and `BuildResult.binary_sha256` and the Verilator side is `DifferentialVerdict.verilator_version`; the two output digests have no column at all | 03 §2.9, §3.6.3; 05 W-08 | folded: 03 §16.2 (§2.9, §3.6.3); the brief is 05 scope and is recorded as wrong |
| 8 | **FR-08.1's `circt-opt` half has no enumeration anywhere.** "a pipeline that ends in the HW dialect" is not a testable rule without a list. Implemented as `_HW_TERMINAL_PASSES`, the seven `--lower-*-to-hw` conversions the measured source build registers (`circt-opt --help`, 2026-09-14), applied to the **last** option of the argv that is not one of `_NOT_A_PASS`. `--convert-moore-to-core` is deliberately absent, which is FR-08.1's own acceptance criterion. Both dash spellings are accepted (W-09 #3's lesson). A `--pass-pipeline=` probe is refused rather than parsed: 50 of M1's corpus RUN lines use that spelling and **none** of them is HW-terminal, so reading the last pass out of a nested pipeline string would be code for no case. `firtool` with **no** output mode also emits Verilog by default, and the rule as FRD writes it ("requesting" one of three) excludes it | 01 FR-08.1, 03 §3.6.3 | folded: 01 §1.10 FR-08.1, 01 FR-08.1 in place, 03 §16.2 (§3.6.3) |
| 9 | **FR-08.9 had no computable test.** "a divergence confined to cycles before the first write of the divergent output signal" is not something a differ that sees two line sequences can evaluate. Implemented as its observable form: every signal that diverges does so over a contiguous PREFIX of the cycles it is sampled at and agrees from some cycle onward. A register the design never writes stays undefined for the whole run under `--x-initial unique` and is a plain `diverge`; one written at cycle k differs only while uninitialised, which is the bucket | 03 §3.6.3, 01 FR-08.9 | folded: 01 §1.10 FR-08.9, 01 FR-08.9 struck in place, 03 §16.2 (§3.6.3) |
| 10 | `port_list_sha` is `hashlib.sha256` over `_canonical([dataclasses.asdict(port) …])`, **not** over `contract.to_json` of the list: `to_json` takes one dataclass (`dataclasses.asdict(obj)`) and raises on a list. The canonical shape is the same four `json.dumps` options | 03 §3.6.3 | folded: 03 §16.2 (§3.6.3) |
| 11 | `dut.sv` comes from `firtool --verilog` on the lifted HW IR, as §3.6.3 says, and that was **verified rather than assumed**: `firtool --verilog` does read an HW-dialect `.mlir` and emits synthesisable SystemVerilog (2026-09-14, measured build). The probe's own `--verilog` stdout is not reused, so both arms come from one file and cannot drift | 03 §3.6.3, §4.13 | folded: 03 §16.2 (§3.6.3, §4.13) |
| 12 | `_module_body`: `circt-opt` and `firtool` print the implicit top-level module **explicitly**, and `gen_arc_harness` wraps what it is handed in a module of its own, so a lifted design must be unwrapped or `arc.sim.instantiate` cannot resolve its symbol one level down. Measured: the nested form makes arcilator exit 1 | 03 §3.6.3 | folded: 03 §16.2 (§3.6.3) |
| 13 | **§6.5 names no file that holds a recorded argv**, and FR-08.4's acceptance criterion reads "the recorded Verilator argument vector" for the two X flags. B4 writes `differential_evidence.json` beside §6.5's `arcilator.out.txt` and `verilator.out.txt`: both arms' argv (the Verilator build's included), both stdout digests, both exit statuses, the driver deviation, the X policy and the port-list digest. One file §6.5 does not list | 03 §6.5, 01 FR-08.4 | folded: 01 §1.10 FR-08.4, 01 FR-08.4 in place, 03 §6.5 tree, 03 §16.2 |
| 14 | **A-08 is closed by measurement.** The generated harnesses build and run against a generated design: the clocked accumulator agrees over all 56 sampled cycles through `oracle_differential` end to end, and the same design with one constant changed diverges at line index 1, cycle 8, signal `p`, `1` against `0`. §4.13's `[UNVERIFIED]` row and §15 item 1 can be closed. **A-19 is still open**: FR-08.1's rule now exists as a pure function, but the count over the 187-seed corpus has not been run | 03 §4.13, §15; 01 A-08, A-19 | folded: 01 §1.10 A-08 and A-19, both in place, 03 §16.2 (§4.13, §15) |
| 15 | Tiers and ids: `T-U-probe-27` stays T0; `T-U-probe-29` is implemented at **T0** over `differential/x_only/`, the classification being pure and the real X behaviour needing a design no corpus seed provides; `T-U-probe-28` and the new `T-U-probe-30b` are **T1** with `needs_sdk`, the host having both tools, where §1.9 says T2. New ids: `T-U-probe-30b`, `T-U-probe-52`, `T-U-probe-53`, `T-U-probe-56`. No T2 test exists in the suite at all | 04 §1.9 | folded: 04 §16.7 counts table and tier-relabel table |
| 16 | **`T-U-probe-35`'s `prologue_dropped == 4` is not a stable property** and the assertion is now the RULE and not the count. A stack overflow sometimes hits its guard page inside libc's own `realloc`, and the leading unresolved libc frames §3.7.1 strips are then two or three rather than one: measured 4, 4, 4, 4, 6, 4, 5, 4 over eight runs of the same input on one host. Pre-existing, and identical before and after W-09's two frame fixes | 04 §1.9, 03 §3.7.1 | folded: 04 §16.7 (§1.9 `T-U-probe-35`) |
| 17 | `fixtures/differential/` holds `x_only/` and `broken_tb/` only. §13's `port_lists/` and `register_add/` are not created: `T-U-probe-47` and `T-U-probe-48` construct their five port-list cases inline and `T-U-probe-50` carries the accumulator it runs | 04 §13 | folded: 04 §13 `fixtures/differential/` row |

## W-10 (2026-09-14): `triage_task.py` (B6a, B6b, B7), F-10, F-11, F-15
| # | Finding | Owed to | Status |
|---|---|---|---|
| 1 | **B7 cannot import `generate_task` at module scope yet.** §3.7.4 has B7 call `generate_task.build_llm` and `generate_task.llm_turn` "imported rather than repeated", and 05 §2.3 forbids either half importing the other until the join (W-17, 09-19); W-10 is 09-17 and `generate_task.py` is W-13's and does not exist. Implemented as a function-local import inside `triage_report`, so the module-scope import graph carries no cross-half edge and the call shape is §3.5.1's unchanged | 03 §3.7.4, 05 §2.3 | folded: 03 §3.7.4 in place, 03 §16.2 (the three function-local imports) |
| 2 | **§7.1's parser has no module.** "The parser is one function, shared by all four" names no home; `prompts/` is a directory of markdown and not a package, §14.7 gives `prompts/` four FRs and no callable, and `generate_task.py` is not written. `parse_json_footer` and `PromptContractError` are in `triage_task.py`; **W-13 imports them rather than re-implementing them**, or the architect moves both to one shared module and this row becomes the reason | 03 §7.1, §1.1, §14.7 | folded: 03 §7.1 in place, 03 §1.1 (`llm.py`), 03 §16.2; open code: 04 §16.8 item 3 |
| 3 | **§3.7.1's `signal_name` has no parameter.** `dedup_and_screen(candidate, seed, verdict, clone_path, db_path, top_n)` carries no `BuildResult` and neither `CandidateRecord` nor `OracleVerdict` has a `signal` field, so the crash and fatal-error fingerprint's first half is unreachable from the signature. Read from `build_result.signal` by `probe_id` through the store the node already opens for the mirror screen | 03 §3.7, §3.7.1, §2.9 | folded: 03 §3.7 in place, 03 §16.2 |
| 4 | **FR-10.7's detection point does not fit `RunManifest.issue_mirror`.** §2.7 closes that dict at six keys and `validate` compares the key set exactly, so an incomplete refresh has nowhere to be recorded. `issue_mirror_refresh` returns the six plus a seventh, `incomplete_reason`, which is the caught exception's class name or None; the driver copies the six into the manifest and not the seventh | 03 §3.7, §2.7, 01 FR-10.7 | folded: 01 §1.10 FR-10.7, 01 FR-10.7 in place, 03 §16.2 (§2.7) |
| 5 | **"Once per run" is not B6a's to enforce.** §3.2 gives the node the idempotency key `(repo, issue_cap, run_manifest_id)` and the signature carries neither a run id nor a refresh flag, and `issue_mirror_meta` is keyed by `run_manifest_id` with a foreign key to `run`. B6a writes `issue_mirror` rows only; B12 calls it once unless `--refresh-mirror` and writes the meta row from the return | 03 §3.2, §3.7, §6.2 | folded: 03 §3.7 in place, 02 §0.4 and §1.3 B6a row, 03 §16.2 |
| 6 | **`issue_mirror_issue_cap` is 20,000 in the committed `budget.yaml` and 600 in `contract/fixtures/run_manifest/discovery_01.json`** (and in W-10's brief). They are not both right. At 20,000 one refresh mirrors llvm/circt's whole issue history: the newest issue or PR number on 2026-09-14 is 11,113, so the listing is roughly 112 requests of 100, comfortably inside the authenticated 5,000/hour but not a thing to run per test. The fixture and the file should be reconciled before W-22 | 03 §9.5, 01 FR-10.9 | superseded by W-14 #21 and architect decision 1, folded there; 04 §16.8 records that the fixture bound and the campaign bound are two numbers and not a defect |
| 7 | **Verdict precedence is fixed nowhere.** §3.7 names six `DedupVerdict` values and no order between them. Implemented and stated in the node's own docstring: an empty mirror is `dedup_unavailable` for every candidate; then a mirror hit, open before closed; then a candidate-to-candidate duplicate; then a post-pin fix; then `new`. The mirror hit outranks the candidate duplicate because its evidence is the only carrier of FR-13.9's label | 03 §3.7, 01 FR-10.5 | folded: 01 §1.10 FR-10.5, 01 FR-10.5 in place, 03 §3.7 in place |
| 8 | **FR-13.9 is unsatisfiable through §3.7.3's selection order.** FR-13.9 refuses a filing that matched **any** `good first issue` issue, and `DedupVerdict.evidence`'s eight keys hold the labels of **one**. Under §3.7.3's order (open wins, then issue number) a labelled match can be masked by an unlabelled one. `mirror_screen` therefore promotes a `good first issue` match ahead of the state rule, which is the safe direction for a refusal; the alternative is a ninth evidence key, which is a MINOR contract bump | 03 §3.7.3, 01 FR-13.9, C-05 | folded: 01 §1.10 FR-13.9, 01 FR-13.9 in place, 03 §3.7.3 in place |
| 9 | **`--since <bound>` includes a commit stamped exactly at the bound.** FR-10.4 scans commits made **after** the run's commit and FR-15.1 fixes made after the seed's, and §4.12's command takes a date. Both scans drop the bound commit's own SHA from the result (`_after`); without it every candidate is `fixed_post_pin` against its own run commit | 03 §3.7.2, §4.12 | folded: 03 §16.2 (§3.7.2 and the §4.11.1 numbering) |
| 10 | **The scans need repo-relative pathspecs and the frames carry build-absolute paths.** §3.7.2 says "the candidate's source paths" and §3.7.1's site normalisation keeps only two components, which is a fingerprint and not a pathspec. `frame_paths` cuts each path at the first of `lib/ include/ tools/ test/ frontends/ integration_test/` and falls back to `:(glob)**/<basename>`, and takes the **raw** `assertion_site` rather than the normalised one. Only `in_circt_object` frames contribute, because one real trace in §3.7.1 is 466 frames long and a pathspec list that size is not a query | 03 §3.7.2, §3.7.1 | folded: 03 §16.2 (§3.7.2, §3.7.1) |
| 11 | **Nothing merges before the gate has run, and that is the design.** T-U-triage-26 puts both `fingerprint_stable=false` and the pre-re-run `None` outside every partition, so at stage 6 no two candidates of one run can merge. B6b's candidate-to-candidate query therefore asks the `fingerprint` table for a row whose value matches **and whose `fingerprint_stable` is 1**, which is FR-10.6's cross-run query and the only shape in which `duplicate_of_candidate` is ever reachable | 03 §3.7.1, §3.9, 04 §1.11 T-U-triage-26, 01 FR-10.6 | folded: 01 §1.10 FR-10.6, 01 FR-10.6 in place |
| 12 | **§7.4's "six primary variables" is under-determined.** The variable list has ten, and a `differential` candidate cannot fill seven of them (`assertion_text`, `assertion_site`, `frames`, `repro_command`, `reduced_case`, `dedup_verdict`, `dedup_evidence`). Seven are bound to the literal; `oracle_class` is bound to the class itself, because the prompt branches on it three times and binding it to "not applicable" would disable those branches | 03 §7.4 | folded: 03 §16.2 (§7.4) |
| 13 | **§7.4.1's `stimulus` point names a record `render_report` is not given.** The point is "`ProbeSpec.differential`'s five keys" and the signature takes no `ProbeSpec`. Three come off the `DifferentialVerdict` (`stimulus_id`, `cycles`, `port_list_sha`) and two off §9.4's campaign constants (`RESET_PROTOCOL`, `SAMPLE_POINT`), which is the same data by FR-08.3's rule that one stimulus definition serves the whole campaign | 03 §7.4.1, §9.4, 01 FR-08.3 | folded: 01 §1.10 FR-08.3, 01 FR-08.3 in place, 03 §16.2 (§7.4.1, §9.4) |
| 14 | **`triage_report` has no way to receive a `DifferentialVerdict`.** §3.7.4's signature is `(candidate, reduced, verdict, dedup, manifest, cfg, artefact_dir)` and §7.4.1's differential template needs one. Implemented with a keyword-only `differential=None` | 03 §3.7.4, §7.4.1 | folded: 03 §3.7.4 signature in place, 03 §16.2 |
| 15 | **§3.7.4 names no system message for B7.** `build_llm` takes one and CHIA's chain has `prompts/system.md`, which §1.1 gives stage 6 no equivalent of. `TRIAGE_SYSTEM_MESSAGE` is one line in the module | 03 §3.7.4, §3.5.1, §1.1 | folded: 03 §3.7.4 in place, 03 §16.2 |
| 16 | **W13's wording constrains the renderer and not only the agent.** T-U-triage-24 asserts a `fatal_error` report contains neither "crash" nor "assertion", which the build-identity block broke by saying "Assertions: -UNDEBUG". Reworded to "Built with -UNDEBUG, so the compiler's internal checks are on"; the literal `-UNDEBUG` stays, which is what FR-11.3 and C-08 ask for | 03 §7.4, §7.4.1, 01 FR-07.10, FR-11.3 | folded: 01 §1.10 FR-07.10 and FR-11.3, both in place, 03 §16.2 (§7.4) |
| 17 | **`dedup_and_screen` raises on a `differential` candidate**, against §3.7's "Raises: nothing". FR-08.10 keeps such a candidate out of the stage entirely, so one arriving is a caller defect and not an undecidable dedup, and `dedup_unavailable` would hide it | 03 §3.7, 01 FR-08.10 | folded: 01 §1.10 FR-08.10, 01 FR-08.10 in place, 03 §3.7 Raises in place |
| 18 | `render_report` has no removal case for two of the thirteen points: `contamination` and `assisted_by` are always derivable from the `CandidateRecord` and the `RunManifest`, so `ReportIncomplete` cannot fire for them. T-U-triage-23 asserts the other eleven and names these two | 03 §7.4.1 | folded: 01 FR-11.3 in place, 03 §16.2 (§7.4.1) |
| 19 | **Fixture layout.** §1.11's columns name `fingerprint/`, `report/`, `turns/`, `run_manifest/` and `contamination/`; W-10's brief allows `tests/fixtures/{triage,mirror,dedup}/`. The five live under `triage/`, and `contamination/clone` is not a committed fixture at all: the two scans run against a three-commit repository built in `tmp_path`, which is why T-U-triage-18, 19, 20 and 32 keep the `t1` marker although they need only `git` | 04 §1.11, §13 | folded: 04 §13 (`fixtures/triage/*`, `contamination/clone`), 04 §16.7 tier table |
| 20 | **The labelled set is twenty-two pairs, not twenty**, eleven duplicate and eleven distinct, at least four per rule and five R4. Two of them are the point of the exercise and neither is synthesised: pair 16 is `assertion_01` against `assertion_04` as the latter would have been recorded under the former's SDK, where the generic `cast<>` check sits at the same `Casting.h` line, and pair 22 is §3.7.1's own ten-run `!hw.array` measurement, whose fingerprint frame moved between three adjacent frames of one recursion cycle. **Measured and printed by T-U-triage-09: collision rate 1/11 = 0.0909, false-merge rate 1/11 = 0.0909.** A-05 closes on these two numbers, with the caveat that the set is constructed from six recorded failures and not from a pilot's own probes, which §10 asks for and which no run has produced yet | 04 §10, 01 A-05, FR-10.2 | folded: 01 §1.10 FR-10.2 and A-05, both in place; 04 §16.7 (§10) and §13 pairs row |
| 21 | **T-U-triage-35 is a new id**: §1.11 allocates thirty-four and names no live test, while FR-10.9's acceptance and W-10's brief both need one. Marked `t1`, skipped cleanly with no `GITHUB_TOKEN`. **It has not been run.** The credential helper's `github.com` entry yields a thirteen-character value that `api.github.com` answers 401 to, and H-05's read-only token file (due 2026-09-16) does not exist on this host. `mirror/sample.json` was therefore recorded **unauthenticated**, which is 60 requests an hour: enough for one fifty-issue excerpt, not for a full mirror. The live test remains owed | 04 §1.11, 05 H-05, 01 FR-10.9 | folded: 04 §16.7 counts table (T-U-triage-35). Run since, and closed by W-14 #21 |
| 22 | **The model layer is faked one level higher than §0.3 asks.** §0.3's recipe monkeypatches `google.genai` and runs `build_llm` and `llm_turn` for real, through a `fake_vertex` fixture in `conftest.py`; `generate_task.py` does not exist, so `test_triage_task.py` installs a stand-in module under that name instead. No assertion in the module is about the backend, so W-13 replaces the fixture and changes none of them. `conftest.py` also does not yet do what §0.5 requires of it (delete `BUGLOOP_ALLOW_LIVE_MODEL` and set a synthetic `GEMINI_API_KEY`); it asserts the interlock is unset instead, which is the safe half | 04 §0.3, §0.5 | folded: 04 §16.7 (§0.3, §0.5); open code: 04 §16.8 item 7 |

## W-13 generators, tools, backend and mutators (2026-09-14)
| # | Finding | Owed to | Status |
|---|---|---|---|
| 1 | **7.1's parser has two copies and the design names no module for it.** §7.1 calls `parse_json_footer` "one function, shared by all four"; `triage_task.py` (W-10) defined it, and `generate_task.py` defines it again rather than importing it, because the supply half may not import the apparatus half (FR-16.1, 05 §2.3) and the import can only run the other way. The join (W-17) deletes the apparatus half's copy and has `triage_task._run_turn`, which already imports `generate_task` lazily, import the parser with it | 03 §7.1, §1.3 | folded: 03 §7.1 and §1.1 in place, 03 §16.2; open code: 04 §16.8 item 3 |
| 2 | **`triage_task._run_turn` calls `SourceReadTool(cfg["clone_path"], cfg["run_commit"])`, two positional arguments, and §3.5's constructor takes `(name, clone_path, run_commit, cap_bytes, task_options)`.** The call binds `name=clone_path`, `clone_path=run_commit`, and raises `TypeError` for the missing `run_commit`; `test_triage_task.py` installs a stand-in `generate_task` module, so nothing catches it today. Implemented as §3.5 writes it, with `cap_bytes` and `task_options` defaulted; **the call site is W-17's to fix** | 03 §3.5, §3.7.4 | open code: 04 §16.8 item 1 (architect decision 4) |
| 3 | **No supply-half module imports `store.py`** (§1.3 rule 2), so A3 writes its own artefacts through a private `_write` rather than through `store.artefact_write`. `_write` keeps FR-17.8's protocol (the `PARTIAL` marker is created whenever it is absent) and never removes a marker, which is the apparatus half's act. A7 reads the mirror through a read-only `sqlite3` connection for the same reason | 03 §1.3, §6.5, §8.3 | folded: 03 §16.2 (the `llm.py` row states the boundary); §6.5 marker protocol unchanged |
| 4 | Every node here returns a `CounterBlock` under `"counters"`, which §3.5's and §8.3's Returns blocks do not list and which §2's `CounterBlock` docstring requires of every node of §3.2. A3 and A4 both report `stage="stage_2"`, A7 `stage="synthesis"` | 03 §3.5, §8.3, §2.10 | folded: 03 §16.2 (§2.10, §3.5, §8.3) |
| 5 | **`SourceReadTool.grep` strips the `<rev>:` prefix.** `git grep -n -F -- <pattern> <rev> -- <prefix>` prefixes every line with the rev; the method's docstring, which is the text the model reads, promises `<path>:<line>:<text>`, and that is also the shape whose path the model can hand straight back to `read_file`. §3.5's table gives the command and says nothing about the return | 03 §3.5 | folded: 03 §16.2 (§3.5) |
| 6 | **`mutate_seed` returns `(mutants, no_ops, failures)` and each mutant is a FIVE-tuple.** §8.2's bare `list[tuple[str, str, str, int]]` has nowhere to put FR-05.6's no-op count, FR-05.7's per-id failure count, or FR-05.5's replacement argument vector. `apply` correspondingly takes keyword-only `mutator_set` and `argv`: §8.2's three-argument form cannot look an id up, and cannot mutate an argv it is not given | 03 §8.2 | folded: 03 §8.2 signatures in place, 03 §16.2 |
| 7 | **The committed set is `mutators/set_dev.json`, `set_version: "dev"`, carrying `"frozen": false`, a field §8.1's format does not have.** A7 has not been run and `set_v1.json` does not exist; `mutators.SET_PATH` prefers the frozen name wherever it exists, and `load_set` refuses an unfrozen set outright whenever a run names a digest, so the development set cannot run a campaign. `budget.py`'s `MUTATOR_SET` still names `set_v1.json` and its check skips a set that is not there, so nothing breaks before A7 runs | 03 §8.1, §9.2 | folded: 03 §1.1 and §8.1 in place, 03 §16.2; open code: 04 §16.8 item 8 |
| 8 | **Probe ids are derived, not allocated**: `p-<sha256(run_manifest_id, seed_sha, arm, iteration, key)[:12]>`. §3.5's emitter is handed no allocator, and a derived id makes FR-16.5's replay and A4's idempotency key reproduce the same row rather than a second one. The contract fixture's `p-0000000001` is a counter and is constructed | 03 §3.5, §2.4 | folded: 03 §16.2 (§3.5, §2.4) |
| 9 | **The argv comes from `argv_template[0]`, with lit's three substitutions bound and the two probe-only options stripped.** §7.3 says only "substituting the written file's path"; `SeedRecord` records no per-run-line tool, so no index other than the first is attributable to `entry_tool` (W-05 erratum 4), and `--split-input-file` and `--verify-diagnostics` mean nothing for one generated input (§4.2) | 03 §7.3, §4.1 | folded: 03 §16.2 (§7.3, §4.1) |
| 10 | **Two declared substitution variables are never used by their prompts.** §7.3 declares `$probe_dir` and §8.3 declares `$issue_digest`, and neither text carries the name; both are supplied by the caller anyway, so `safe_substitute` leaves nothing unbound. `T-U-prompt-04`'s "declared in its file and supplied by its caller" fails on both as written | 03 §7.3, §8.3 | folded: 04 §16.7 (§1.23 `T-U-prompt-04`), 03 §16.2 (§7.3, §8.3) |
| 11 | **The staging directory and the probe directory are two different places and no section says how one becomes the other.** `ProbeWriteTool` is bound to `<iter>/probes/`, which is §3.5's "this iteration's probe directory"; the emitter then writes each accepted input to §6.5's `<iter>/probe_<probe_id>/input.<ext>` and that path is `ProbeSpec.input_path`. `input_filename` is therefore `input.<ext>` and the agent's own file name survives in the transcript and in the staging directory | 03 §6.5, §3.5 | folded: 03 §16.2 (§3.5, §6.5) |
| 12 | §8.3 step 2 selects `number`; the column is `issue_number` (§6.2). Step 1's bare `budget.yaml` path is right in only one of §1.4's two trees, so the registration check asks git for `budget.yaml` and `*/budget.yaml` | 03 §8.3, §6.2 | folded: 03 §16.2 (§6.2, §8.3) |
| 13 | **The five per-turn files are named `llm_<stage>.prompt.md`, `.md`, `.stderr`, `.jsonl`, `.usage.json`.** §6.5 lists four and FR-04.6 requires five; the prompt file is the fifth and takes the name `triage_task` already gave it (W-10) | 03 §6.5, 01 FR-04.6 | folded: 01 §1.10 FR-04.6, 01 FR-04.6 in place, 03 §6.5 tree in place |
| 14 | `turn_cost.cost_usd` is computed only where `cfg` carries the two prices, and is `None` otherwise: the generator may not read `spend_usd` (§2.5) and the ledger owns the arithmetic (§9.1). `metered` agrees with `tokens_in`, which is the contract's own conditional | 03 §2.7, §3.11 | folded: 03 §16.2 (§2.7, §3.11) |
| 15 | A3 takes its iteration index from `cfg["iteration"]`, defaulting to `feedback.iteration`; A4 takes it from `cfg["iteration"]` only, because it may not read the bundle at all (FR-16.2). The `cfg` keys the two nodes actually read are `model_id`, `per_seed_probe_cap`, `iteration`, `run_manifest_id`, `artefact_dir` or `artefact_root`, `artefact_inline_cap_bytes`, `clone_path`, `run_commit`, `head_options`, `here_options`, `timeout_seconds`, the two prices, `mutator_set_path`, `mutator_set_sha`, and the two prompt overrides | 03 §3.5 | folded: 03 §16.2 (§3.5) |
| 16 | **No test in this tree sets `BUGLOOP_ALLOW_LIVE_MODEL`, not even under `monkeypatch`.** §1.6's header permits a test to set it "inside `monkeypatch`, to exercise the allow path"; `T-U-layout-08` (1) forbids any test outside `tests/system/` to set it, and W-13's brief asks for the stronger rule. The allow path substitutes the NAME the interlock reads, `generate_task._LIVE_MODEL_ENV`, with a stub, so the refusal, the key check and the express construction all run against the real code. **`tests/test_repair_adapter.py` (W-14) does set the real variable, twice**, which `T-U-layout-08` will fail on as it stands | 04 §1.6, §0.5, T-U-layout-08 | folded: 04 §16.7 (T-U-layout-08 rule, §1.6 header); open code: 04 §16.8 item 2 |
| 17 | **`conftest.fake_vertex` also substitutes `chia.trace.profiler.get_profiler`.** Measured 2026-09-14: `ray.get_actor` starts a local Ray instance when none is running, and both `ChiaFunction._wrapper` and `VertexGeminiLLM.prompt` call `get_profiler`, so a turn driven offline would start one and trip `conftest.no_local_ray`. §0.3's recipe does not mention it. `conftest.py` still does not do what §0.5 asks (delete the variable, set a synthetic key): it asserts the variable is absent, which is strictly stronger, and each test sets its own synthetic key | 04 §0.3, §0.5 | folded: 04 §16.7 (§0.3 `get_profiler`, §0.5); open code: 04 §16.8 item 7 |
| 18 | **`-W error` turns a third-party warning into an error on every `ChiaTool` construction.** `FastMCP`'s settings model raises `pydantic_settings.IncompleteFieldDefinitionWarning` about its own `lifespan` field. Filtered by a module-level `pytest.mark.filterwarnings`, which is the one filter that outranks a command-line `-W` | 04 §0.3 | folded: 04 §16.7 (§0.3 warning policy) |
| 19 | **`tests/test_tools.py` is a twenty-sixth test module** and §1.3's mapping names twenty-five. `T-U-gen-11` to `-18` live in it, at W-13's brief's request; `test_layout.py`'s exemption list must gain it, or the two modules must be merged | 03 §1.3, 04 §1.6 | folded: 03 §1.3 in place, 04 §1.3 and §16.7 (§1.6, two modules) |
| 20 | **Tier, and the fixtures.** `T-U-gen-15` to `-18` run at **T0** against a throwaway two-file git repository built in `tmp_path`: what is under test is the argument vector, the error string and the cap, none of which needs CIRCT's history. §3.5's **measured pair** is a separate tier-1 test, `T-U-gen-15b`, against the real blobless clone at `b792c772`, and it passes on this host. `T-U-gen-05` likewise runs `corpus.resolve_sites` for real at T0 against that repository, which §1.6 marks T1 | 04 §1.6, §13 | folded: 04 §16.7 counts and tier tables; open code: 04 §16.8 item 11 (`T-U-gen-15b`) |
| 21 | **The turn fixtures are `.md`, not `.jsonl`.** §1.6 names `turns/*.jsonl`; a `.jsonl` in §6.5 is the raw session transcript, and what a replay needs is the turn's final text, which is what `parse_json_footer` reads. All six under `fixtures/generate/turns/` and all three under `fixtures/synth/turns/` are **constructed** and their READMEs say so: no model has been run against this design | 04 §1.6, §1.8, §13 | folded: 04 §13 turns row |
| 22 | **A7's mirror fixture is a real `loop.db` with constructed rows at M9's two counts**, 487 closed `label:bug` and 101 open, built through `store.LoopStore`. §1.8 names `mirror/closed_bug.jsonl`; the recorded `fixtures/mirror/` belongs to B6a (W-10) and holds fifty real issues, which is neither count, and a fixture of 487 fabricated issues presented as recordings would be worse than one whose README says what it is | 04 §1.8, §13 | folded: 04 §13 mirror row |
| 23 | `contract/fixtures/probe_spec/mutation_01.json`'s `argv` still repeats the tool, which W-08 erratum 4 corrected in `seeded_01.json` only. §4.1 builds the invocation as `<binary> *spec.argv`, so the mutation fixture is wrong in the same way the seeded one was | 04 §16.7, 03 §4.1 | open code: 04 §16.8 item 4 (architect decision 5) |

## W-14 (2026-09-14): `repair_adapter.py` (B8), `gate.py` (B9a, B9b), `approve.py` (B9c); F-12, F-13, F-20
| # | Finding | Owed to | Status |
|---|---|---|---|
| 1 | **`repair_adapt` needs three values §3.8's signature does not carry**, added as keyword-only parameters. `local_id` is `LOCAL_ID_BASE + <the candidate's rowid>`, which is a query against the head's `loop.db` and not something a `repair` worker can run, so `mint_local_id(store, candidate_id)` is the head's half and the node takes the minted value. `input_path` is the probe's own input path: `repro.sh` runs the tool with the **reduced case** in the input's place, which is `probe_task._test_args`' rule, and `OracleVerdict.repro_command` names the original input without saying which token it is. `created_utc` is §3.8's own free variable `candidate_created_at_utc`, which `CandidateRecord` has no field for although the `candidate` TABLE has the column | 03 §3.8, §2.9 | folded: 03 §3.8 signature and prose in place, 03 §16.2 (§2.9) |
| 2 | **FR-12.8's "stops the run" is the caller's, not B8's.** §3.8's `Raises` paragraph closes the exception set at `RepairRefused` and `LiveModelRefused`, so a lit run that discovered nothing is **recorded** (`lit_unusable` true, `lit_ok` null, so no patch can attach) and B12 stops on the field. `T-U-repair-12` asserts the mapping and that the mapper's own docstring names FR-03.17 | 03 §3.8, 01 FR-12.8 | folded: 01 §1.10 FR-12.8, 01 FR-12.8 struck in place, 02 §0.4 B8 row, 03 §16.2 |
| 3 | **`--no-repair` is a refusal inside B8**, `RepairRefused("repair_disabled")`, checked **before** the interlock so a disabled run asks for no credential at all. §3.8 consequence 4 states the outcome and names no mechanism | 03 §3.8, §13.1 | folded: 03 §3.8 in place, 03 §16.2 |
| 4 | **The six prompt keys are not the six file names.** §3.8's table renders them `f"{n}.md"`; `cfg["repro_prompt"]` is read from `reproduce.md` (`circt_issue_loop.py:88`). Implemented as `PROMPT_FILES`, a key-to-file map | 03 §3.8 | folded: 03 §16.2 (§3.8) |
| 5 | **`cfg["backend"]` and `cfg["model"]` come from ONE recorded pair.** §3.8 sources the backend from `cfg["repair_backend"]` and the model from `manifest.model_ids["repair_adapt"]`, which are two places for one fact; `build_cfg` splits the manifest's `"<backend>:<model id>"` and `repair_adapt` raises `ValueError` where `cfg["repair_backend"]` disagrees, because `stages_metered["stage_7"]` reads the manifest and the chain reads the flag (FR-14.8) | 03 §3.8, §2.7 | folded: 03 §16.2 (§3.8, §2.7) |
| 6 | **`fix.diff` has no place in §6.5's `repair/<local_id>/` listing.** The chain returns the diff as a string and `RepairResult.diff_path` is a path, so it is written beside `repro.sh`, `case.<ext>` and the two hashes | 03 §6.5, §2.9 | folded: 03 §6.5 tree in place, 03 §16.2 |
| 7 | **Question 3 needs a third node.** §3.9 names two and §4.8 gives question 3 three tool invocations "under the same `prlimit` prefix as a probe" against "the source-tree binaries"; `gate_decide` runs on the head and holds no `circt` resource, so it can run neither. `gate_validate` is that node, dispatched exactly as `gate_rerun` is, and `gate_decide` stays resource-free | 03 §3.9, §4.8, 02 §3 | folded: 03 §3.9 (`gate_validate`) and §3.2 (B9d) in place, 02 §0.4 and §1.3 and §3, 01 §1.10 FR-13.15 |
| 8 | **`image_spec` is a dict, not an `ImageSpec`.** §3.9's `gate_rerun` signature names the dataclass; `RunManifest.image_spec` is §2.7's eight-key dict, and the four keys the two nodes read (`tool_hashes`, `flag_string`, `circt_sha`, `image_digest`) are all in it. `oracle_primary` reads exactly one field off its own `image_spec`, `flag_string`, so `gate_rerun` hands it a one-field stand-in rather than restating §3.6.2's rule | 03 §3.9, §2.7, §3.6.2 | folded: 03 §3.9 signature in place, 03 §16.2 (§2.7) |
| 9 | **`gate_decide` and `gate_rerun` take three arguments §3.9 does not list**: `limits` (the re-run and the validity check are bounded by the **probe** limits, as §4.8 requires), `top_n` (`compute_fingerprint`'s frame depth, `budget.yaml`'s `fingerprint_frame_depth`) and `bin_dir` (every fixture and tier-1 measurement runs a build outside the image, as `probe_execute`'s own erratum records). `gate_rerun` also takes `run_manifest_id` and `candidate_id`, without which §3.9's `mkdtemp(dir=<artefact_root>/<run>/gate/)` has no path | 03 §3.9, §6.5 | folded: 03 §3.9 signatures in place, 03 §16.2 |
| 10 | **A fourth question-2 stopping value, `not_fixpoint`.** FR-13.3's pass requires a fixpoint and FR-18.6's table has three failing rows, none of them "a reducer ran and did not reach a fixpoint", which §9.5's own `reduction_wall_seconds` comment says was the common case at 60 s. It buckets as `not_minimal` with the other three, so the taxonomy is unchanged and total. A free-text `ReducedCase.reason` (FR-09.12 permits one) also buckets as `not_minimal` | 03 §3.9, 01 FR-13.3, FR-18.6 | folded: 01 §1.10 FR-13.3 and FR-18.6, both in place (FR-18.6 gains a table row), 03 §3.9 |
| 11 | **A null answer has no bucket.** FR-13.10 defaults the decision to `nothing` and FR-18.6's table has no row for an unanswered question. Recorded as `undecided`, so the six values stay total, and the rule lives in a pure `decide(fields, repair)` so that `T-U-gate-17` can produce a null answer at all: the four questions as implemented cannot be made to return one on demand | 03 §3.9, 01 FR-13.10, FR-18.6 | folded: 01 §1.10 FR-13.10 and FR-18.6, both in place, 03 §3.9 in place |
| 12 | `gate_rerun`'s directory is `mkdtemp(prefix="<candidate_id>-")` **inside** `<artefact_root>/<run>/gate/`, which satisfies §3.9's `mkdtemp` and §6.5's `gate/<candidate_id>/` at once; neither spelling alone is both unique per call and readable | 03 §3.9, §6.5 | folded: 03 §3.9 in place, 03 §16.2 (§6.5) |
| 13 | **`q3_after_parse` needs the frame's FULL path**, which `OracleVerdict.fingerprint_frame` does not carry: it is `"<function> <basename>"`, which answers FR-13.15's `*Parser*.cpp` half and none of its three directory halves. The frame is read back out of the recorded `oracle_verdict.frames_json` and re-stripped by §3.7.1's rule. "The probe's argv carried a pass pipeline" is `probe_task._last_pass` over `probe.argv_json`, reused rather than restated | 03 §3.9, §3.7.1, 01 FR-13.15 | folded: 01 §1.10 FR-13.15, 01 FR-13.15 in place, 03 §16.2 (§3.9) |
| 14 | **The CLI's commands are not §13.2's table.** W-14's brief names `refuse` where §13.2 names `reject`, and splits the URL in two: `url <id>` prints the pre-filled link or FR-13.17's hand-filing text, `filed <id> <url>` records the pasted one. §13.2's `status` is not written; `list` and `show` print what it would. A human refusal is recorded as the candidate's `held_reason`, §13.2 and §6.2 giving it no table | 03 §13.2, §6.2, 05 W-14 | folded: 03 §3.9 in place (the `approve.py` paragraph), 03 §16.2 (§13.2, §6.2) |
| 15 | **There is no `pyproject.toml` and no `bugloop-approve` console script.** §13.2 and §1.1 require one in the flow directory; the team repository packages nothing, so `python -m circt_bug_loop.approve` is the entry that exists and `T-U-appr-15` exercises it as a program. W-26's packaging work owes the console script | 03 §13.2, §1.1 | folded: 03 §1.1, §3.9, §16.2; 02 §0.4 and §1.3 B9c row; 04 §16.7 counts (T-U-appr-15) |
| 16 | `approve.py` reads `budget.yaml` with `yaml.safe_load` and not through `budget.load_budget`, which also runs the campaign's git pre-registration checks. A human approving a report is not starting a run, and the two caps of FR-13.8 are what the program needs | 03 §13.2, §3.11 | folded: 03 §3.9 in place, 03 §16.2 (§13.2) |
| 17 | **Tiers.** `T-U-repair-03`, `-04` and every `T-U-gate-10` to `-13`, `-21` to `-26` run at **T0** where §1's column says T1 or T2: FR-12.3's acceptance asks for "a committed fixture binary that still crashes" and one "patched to emit a diagnostic", and the gate's questions are about a recorded class, text and site read off stderr. Shell scripts are those binaries for both purposes, and `circt_exec_probe`'s `prlimit` prefix, `classify_build`, `oracle_primary`'s parse and `compute_fingerprint` all run unmocked. `T-U-repair-07`, `-21` and `-08` stay **T1**: they need CHIA's git checkout | 04 §1 | folded: 04 §16.7 tier-relabel table |
| 18 | **`tests/fixtures/repair/issues.db` is force-added past `.gitignore`**, which excludes `issues.db*` as runtime state. §13's table requires a committed one and `T-U-repair-01` asserts disjointness against it; its rows are real `llvm/circt` issue numbers taken from `fixtures/mirror/sample.json` and its schema is CHIA's own `db.py` `_SCHEMA` | 04 §13, repository `.gitignore` | folded: 04 §13 `fixtures/repair/` row |
| 19 | **The shared `run_manifest/` fixtures are read from `fixtures/triage/`**, where W-10's erratum 19 put them, rather than copied into `fixtures/repair/` and `fixtures/gate/`. `T-U-repair-09` and `T-U-gate-26` both need `calibration_two.json` and one manifest must not become three | 04 §13, W-10 erratum 19 | folded: 04 §13 `fixtures/triage/run_manifest/` row |
| 20 | **`generate_task` is imported INSIDE `repair_adapt`**, as `triage_task` imports it inside `triage_report` (W-10 erratum 1) and for the same reason plus one more: this module is imported on the head, and CHIA's `issue_task` and `circt_util` are on `sys.path` only on the repair worker, where §13.1's `_PY_MODULES` ships them. All three imports are function-local; `T-U-repair-17` asserts the call site is still the bare `run_issue_remote(issue_md, local_id, chain_cfg)` | 03 §3.8, §13.1 | folded: 03 §3.8 and §16.2 (the three function-local imports) |
| 21 | **W-10's owed live mirror test is run, and `issue_mirror_issue_cap: 20000` does not work.** With `GITHUB_TOKEN` set (authenticated: `X-RateLimit-Limit: 5000`), `T-U-triage-35` made **100 requests in 99.3 s**, the first 99 returning 200 and the **100th returning 422**, which is GitHub's own pagination ceiling on this endpoint. `issue_mirror_refresh` caught it as `GithubRequestError`, so `incomplete_reason` was set and **zero rows** were written: at the committed cap the mirror does not merely truncate, it produces nothing. A second, bounded run at the fixture's cap of 600 succeeded: **600 rows, 249 open and 351 closed, 35 requests, 33.9 s wall, `X-RateLimit-Remaining` 4865 of 5000**: 35 listing pages for 600 issues because the node drops pull requests, which are the majority of the numbering. W-10 erratum 6's "reconcile before W-22" is therefore a defect and not a preference: the cap must be at or below GitHub's ceiling, and `GithubIssuesNode` exposes no response header, so the remaining limit was read by wrapping `requests.Session.get` | 01 FR-10.9, 03 §9.5, W-10 erratum 6, 21 | folded: 01 §1.10 FR-10.9 and C-22, both in place; 03 §3.7.3 and §9.5; 02 §0.4 and §3 B6a rows; open code: 04 §16.8 item 10 |

## Architect decisions from W-13/W-14 findings (2026-09-14)
| # | Finding | Decision | Owed to | Status |
|---|---|---|---|---|
| 1 | GitHub issues listing returns 422 at page 100 (10,000 items incl. PRs); `issue_mirror_issue_cap: 20000` yields zero rows | Mirror walks `direction=desc` then, on the ceiling, `direction=asc`; union deduplicated by number; cap stays the row bound; record the ceiling as constraint C-22 | 03 §3.7.3, §9.5; 01 C-22; triage_task (join) | folded: 01 C-22 (new) and FR-10.9, 03 §3.7.3 and §9.5, 02 §0.4; open code: 04 §16.8 item 10 |
| 2 | W-14 tests set `BUGLOOP_ALLOW_LIVE_MODEL` via `monkeypatch.setenv`, which T-U-layout-08 forbids | `require_live_model(purpose, *, need_key, env=None)`: tests pass an explicit mapping; no test touches the process environment | generate_task, repair tests (join) | folded: 03 §3.5.1 (`env=`), 04 §16.7 T-U-layout-08 rule; open code: 04 §16.8 item 2 |
| 3 | `parse_json_footer`, `build_llm`, `require_live_model`, `llm_turn` live in `generate_task` (supply half) and are imported by `triage_task` and `repair_adapter` (apparatus half) | Move them to a neutral module `circt_bug_loop/llm.py`; both halves import it; `generate_task` keeps the generators only | 03 §1, §3.5.1 (join) | folded: 03 §1.1, §1.3, §3.5.1, §7.1, §16.2; 02 §0.4; open code: 04 §16.8 item 3 |
| 4 | `triage_task._run_turn` calls `SourceReadTool(cfg["clone_path"], cfg["run_commit"])` positionally against `(name, clone_path, run_commit, cap_bytes, task_options)` | Fix the call site at the join; add a real-construction test | triage_task (join) | open code: 04 §16.8 item 1 |
| 5 | `contract/fixtures/probe_spec/mutation_01.json` still repeats the tool in `argv` | Regenerate like `seeded_01` | contract fixtures (join) | open code: 04 §16.8 item 4 |
| 6 | Question 3 needs a `circt` node (`gate_validate`); `not_fixpoint` as a fourth Q2 value bucketed `not_minimal`; null answer bucketed `undecided` | Accept | 03 §3.9, FR-18.6 | folded: 03 §3.9; 01 §1.10 FR-13.3, FR-13.10, FR-13.15 and FR-18.6; 02 §0.4 |
| 7 | No console script; entry is `python -m circt_bug_loop.approve` | Accept; LLD §13.2 erratum | 03 §13.2 | folded: 03 §1.1, §3.9, §13.2 erratum in §16.2; 02 §0.4 and §1.3 B9c row |

## W-15 (2026-09-14): `results.py` (B11), the results artefact and its fixtures
| # | Finding | Owed to | Status |
|---|---|---|---|
| 1 | `render_results(store, manifest, *, labelled_pairs=None)`: FR-10.2's hand-labelled duplicate-pair set is a committed measurement of the project and not a row of the campaign, so it arrives as a keyword-only parameter; its absence is `_require_dedup_rates`'s refusal rather than a silent omission. §3.11 gives the signature as two positional arguments | 03 §3.11 | folded: 03 §3.11 table in place, 03 §16.2 |
| 2 | FR-05.8's AC requires the declaration to say the synthesis is **Mut4All's design**; §14.4's element row names only "its input, date and frozen-set SHA", and the first implementation said "the design this arm reimplements" without the name. Fixed in the render; §14.4's row is owed the clause | 03 §14.4, 01 FR-05.8 | folded: 01 §1.10 FR-05.8, 03 §14.4 element row in place |
| 3 | `_require_regeneration_marks` re-runs the two **deterministic head-side** stages only: `probe_task.classify_build` over the recorded stderr and `gate.decide` over the recorded gate answers. Re-executing a tool stage inside an image of the recorded digest is §11's system half (`T-S-regen-01`); the unit half marks `image_unavailable` off the store's own `image` table, and `reduction_budget_truncated` off `reduced_case.budget_truncated` | 04 §11, 03 §14.4 | folded: 03 §3.11 in place, 03 §16.2, 04 §16.7 (§11) |
| 4 | §13's `results/store_full.db` and its three variants (about 20 MB, committed, "the pilot's own database") cannot exist before W-18. Replaced by a committed generator, `tests/fixtures/results/make_store.py`, regenerated into `tmp_path` at test time; the three variants are one edit each inside the tests, exactly as §13 describes them. `tests/fixtures/results/example.md`, the render of that store, is committed instead, and `T-U-results-22` compares it byte for byte | 04 §13, §1.19 | folded: 04 §13 `fixtures/results/` row, 04 §16.7 counts table |
| 5 | Test count is **22**, not §1.19's 21: `T-U-results-22` is the `example.md` comparison. The renderer and the fixture generator are two files that drift apart quietly, and the artefact is the thing a reader reads | 04 §1.19 | folded: 04 §16.7 counts table, 04 §1.19 heading 21 to 22 |
| 6 | `T-U-results-18` is **behavioural**, not syntactic: relabel every row of the store with the other arm, re-render, and every arm-keyed table comes back exactly transposed. A syntactic walk says nothing here, this module being the exemption `T-U-layout-02` is written around | 04 §1.19 | folded: 04 §16.7 (§1.19 `T-U-results-18`) |
| 7 | `T-U-results-21` hard-codes §14.4's fourteen names. §3.11 offers a count of §14.4's table-body lines that begin with a pipe as the cross-check, which is a repository-wide check and not a unit test: §1.4 puts this package at two depths in two trees, so no test module here may walk to `design/` | 03 §3.11, 04 §1.3 | folded: 03 §3.11 in place, 03 §16.2 |
| 8 | The bucket table prints a **seventh** row, `no gate decision`, so a candidate the gate never reached is counted rather than dropped and the sum check stays meaningful. FR-18.6's six buckets are unchanged; the seventh row is not a bucket | 03 §14.4, 01 FR-18.6 | folded: 01 §1.10 FR-18.6, 01 FR-18.6 in place, 03 §14.4 in place |
| 9 | `_facts` raises `ResultsIncomplete` directly when `loop.db` holds no `run` row for the manifest. That is a fifteenth *failure mode* and not a fifteenth *check*: there is no artefact to refuse parts of, so `len(_REFUSALS)` stays 14 | 03 §3.11 | folded: 03 §3.11 in place, 03 §16.2 |
| 10 | The validation table's `probes` column counts a seed's probes across **both** arms, seeds being shared within a mode (FR-18.2); it is not a per-arm count, and the table's own point is that none of it reaches the headline | 03 §14.4, 01 FR-18.5 | folded: 01 §1.10 FR-18.2 and FR-18.5, 01 FR-18.5 in place, 03 §14.4 in place |
| 11 | False-merge rate over a small campaign has a **zero denominator**: no two of four candidates are labelled duplicates, and `triage_task.rates` returns 0.0 for an empty class. The artefact prints numerator and denominator beside the rate so `0/0` is visible as "unmeasured" rather than read as "no false merges"; §10's twenty pairs are the pilot's denominator | 04 §10, 03 §14.4 | folded: 01 FR-10.2 in place, 03 §14.4 in place |
| 12 | Two defects the tests found in the module as committed, both fixed: `_regeneration` read the recorded stderr with a bare `open(...).read()` and leaked the handle, which `-W error` turns into a `ResourceWarning` failure (now `Path.read_text`, and `os` dropped for `pathlib`); and `_count` pluralises the noun but not the verb, so the dedup sentence read "1 candidate carry an unstable fingerprint" | 03 §3.11 | no design change owed: both were fixed in `results.py` at the time; recorded in 03 §16.2 as facts of the module |
| 13 | `_require_qualifiers` finds a table textually: a line beginning with a pipe whose predecessor does not, then the nearest non-blank line above as the caption. That is why `_table` writes the caption immediately above every table and why no table in the artefact may be introduced any other way | 03 §14.4 | folded: 03 §3.11 in place, 03 §16.2 |
| 14 | The ledger fixture charges the offline mutator synthesis to a `shared` entry at stage `synthesis`, which is where FR-05.8's declaration takes its date; nothing in §9 names that stage, and `results.SYNTHESIS_STAGE` fixes the spelling | 03 §3.11, §9 | folded: 03 §3.11 in place, 03 §16.2 (§9) |

## W-16 (2026-09-15): `bug_loop.py` (B12, B1), the cluster YAMLs, the submit and sync scripts, `test_layout.py`, `test_image.py`
| # | Finding | Owed to | Status |
|---|---|---|---|
| 1 | `probe_task.DIFFERENTIAL_DRIVER` is a **string**, B4 recording it verbatim on every `DifferentialVerdict.driver_source`, and `RunManifest.differential_driver` is a four-key dict. `bug_loop.differential_driver()` derives the four keys from that one constant, splitting it at its one semicolon into `obstacle` and `deviation` and naming the two harness generators as `source`, so the manifest and the verdict cannot state different reasons for the same deviation. §13.1's producer table says "from whether `circt/arc-tests` was reused or a deviation was recorded" and gives no mapping | 03 §13.1, §3.6.3 | folded: 03 §16.2 (§13.1, §3.6.3) |
| 2 | `feedback.build_feedback` takes **four keyword-only arguments** §3.11's signature does not name: `run_manifest_id`, `budget`, `remaining` and `probes_this_seed`. The arm comes from `remaining.arm` and FR-16.3's `probe_cap` from the seed's running probe count, so the driver passes both; §3.11's five-positional form cannot build a `FeedbackBundle` at all | 03 §3.11 | folded: 03 §16.2 (§3.11) |
| 3 | `budget.snapshot(ledger, arm, budget)` takes **three** arguments and §3.11 gives two: the cap is `budget.arm_window_seconds` and a snapshot cannot be built without the budget file | 03 §3.11 | folded: 03 §3.11 table in place (W-06 #2 is the same row), 03 §16.2 |
| 4 | `gate.gate_decide` takes keyword-only `limits`, `top_n` and `bin_dir`, and `repair_adapter.repair_adapt` takes keyword-only `local_id` and `input_path`; §3.9 and §3.8 name them and §13.1's run loop does not, so the driver mints the identifier on the head with `repair_adapter.mint_local_id(store, candidate_id)` before it dispatches stage 7 and passes the reduced case's path as the input | 03 §13.1 | folded: 03 §16.2 (§13.1); the two signatures are already amended at §3.8 and §3.9 |
| 5 | `OracleVerdict` carries `frames` and **no** `frame_tuple`, and `CandidateRecord.frame_tuple` is required by `validate_candidate` **before** B6b runs. `bug_loop._candidate` therefore builds it with B6b's own two lines, `probe_task.strip_prologue` then `probe_task._normalise_function`, truncated at `budget.fingerprint_top_n`; the tuple B6b computes from the same verdict is then equal by construction. No section says who fills the field on the way in | 03 §2.9, §3.7.1 | folded: 03 §16.2 (§2.9, §3.7.1) |
| 6 | The driver reaches the nine stage nodes through **`Stages`**, a record of callables, and dispatches them through **`Dispatch`**, which is `chia_remote` under Ray and the node's own undecorated body without it. That is the one structural deviation from §13.1 and it is what makes a whole iteration runnable at tier 0 against a fixture store and fake stages, which is `T-U-driver-30`. `--dry-run` still dispatches nothing and still stops after the manifest | 03 §13.1 | folded: 03 §16.2 (§13.1) |
| 7 | `--forum-post-url` and `--forum-post-date` are **CLI arguments** §13.1's table does not list, and pre-flight check 11 and two `RunManifest` fields need them; §13.1 attributes them to "the operator, via pre-flight check 11" and gives no mechanism | 03 §13.1 | folded: 03 §16.2 (§13.1) |
| 8 | The twelve pre-flight checks are **twelve named functions** taking what they check rather than reading the world, so all twelve run at tier 0 from a fixture `ImageSpec`, a fixture store and an explicit environment mapping; `interlock_probe(env=None)` takes that mapping, which is architect's decision 2 applied to the driver's own probe | 03 §13.1, 04 §1.20 | folded: 03 §16.2 (§13.1), 04 §16.7 tier table (`T-U-driver-03`, `-05`, `-07`, `-08`) |
| 9 | `cluster_summary` reads `min_workers`/`max_workers` from the **raw YAML**, not from `load_config`: CHIA's `NodeTypeConfig` exposes `num_workers` only, filling it from `num_workers`, then `max_workers`, then `min_workers` (`chia:chia/cluster/config.py:789`). The two spellings §12.1 uses are read and the fixed-size property is asserted on the file | 03 §12.1 | folded: 03 §16.2 (§12.1) |
| 10 | `T-U-cluster-07` asks for `BUGLOOP_ALLOW_LIVE_MODEL` "set to a dummy" and `T-U-layout-08` forbids any test outside `tests/system/` from putting that name into the process environment. The cluster tests load with it **unset**, which is also `T-U-cluster-08`'s own second half: the loader leaves `${VAR}` as literal text, so the container receives the reference as its value and `build_llm` refuses it | 04 §1.24 | folded: 04 §16.7 T-U-layout-08 rule; 04 §1.24 unchanged, the loader leaving `${VAR}` literal being `T-U-cluster-08`'s own half |
| 11 | `cluster_gcp.yaml` has **no `available_node_types` key at all**: §12.2 leaves it as a comment saying "identical to cluster_single.yaml except the artefact mount", so the three worker types appear under `gcp_nodes` and the images live in that comment. `T-U-cluster-06`'s "same images" is asserted as the three names plus the recorded deferral | 03 §12.2, 04 §1.24 | folded: 03 §16.2 (§12.2) |
| 12 | Every commit of a throwaway registration repository must be **dated explicitly**: `budget.py`'s freeze rule is `committed_at >= budget_at`, and two `git commit`s in one wall-clock second make a correctly ordered repository fail check 1. The fixture builder sets `GIT_AUTHOR_DATE` and `GIT_COMMITTER_DATE` an hour apart | 04 §1.20 | folded: 04 §13 `fixtures/repo/` row already commits the builder; the dating rule is recorded in 03 §16.2 under §9.2 check 1 |
| 13 | `bug_loop.py` imports `CIRCT_BIN_DIR` from `circt_core.py` for `gate_decide`'s `bin_dir`; §5.1's table gives the path twice, once there and once as `repair_adapter.CIRCT_BUILD_BIN`, and the two are the same string | 03 §5.1 | folded: 03 §16.2 (§5.1) |
| 14 | `circt_bug_loop/__init__.py` is a source module §1.1's table does not list. It is the package marker the two-tree layout needs, `tests/` importing the flow as `circt_bug_loop.<module>`; it holds no logic and has no test module, so it joins `contract/__init__.py` in the mapping's second set | 03 §1.1, §1.3 | folded: 03 §1.1 and §1.3 in place, 03 §16.2 |
| 15 | The image test module is **`test_image.py`** and §1.3 names `test_image_spec.py`. One rename, recorded so the exemption list can be compared for equality | 03 §1.3, 04 §1.22 | folded: 03 §1.3 in place, 03 §16.2, 04 §1.22 heading |
| 16 | `tests/test_circt_core.py` is a test module neither §1.3 nor its exemption list names: the three functions §3.10 proposes for `chia/chipyard/circt.py` are developed in this tree as `circt_core.py`, `chia/chipyard/test/` being a CHIA path the team repository does not hold. (`tests/test_tools.py` was the same finding and LLD §16.2 has since made it an exemption) | 03 §1.3 | folded: 03 §1.1 and §1.3 in place (`circt_core.py` and `test_circt_core.py`), 03 §16.2 |
| 17 | `generate_task.llm_turn` is `@ChiaFunction(resources={"llm": 1.0}, max_retries=0)`, a **fourth** placement, and §3.2's table and `T-U-layout-06`'s "three placements and no fourth" predate §3.5.1's own node. The cluster YAML declares the `llm` resource, so the placement is consistent with §12.1 and the count is not | 03 §3.2, 04 §1.1 | folded: 03 §3.2 `llm_turn` row in place, 03 §16.2, 02 §0.4 |
| 18 | **`T-U-layout-01`'s module count fails**: `tests/test_prompts.py` and `tests/test_budget_yaml.py` are not written. The prompts are W-13's and `budget.yaml` is W-06's, so the assertion is `xfail`ed against this row rather than weakened, and the two modules are owed before W-20 | 04 §1.1, §1.23 | folded: 04 §16.7 xfail table; open code: 04 §16.8 item 14 |
| 19 | **`T-U-layout-02`'s walk fails on `probe_task.py`**, and on a name collision rather than a violation: `oracle_differential` calls its two SIMULATOR sides `arm` (`for arm, name in ((arc, "arcilator"), (ver, "verilator"))`), so §14.5's "operand ending in arm" fires on `arm["exit_status"]`. Two refinements are owed: the local should be renamed (`side`), and the walk should read a branch's **operand** and not its body, an `If`'s test rather than its whole subtree, or every mention of `arm` inside a branch is reported as a branch on it. "Ending in arm" also matches `warm`, and `circt_warm_build` is a name this tree uses, so the check reads `arm` or a `_arm` suffix | 03 §14.5, 04 §1.1 | folded: 03 §14.5 in place, 03 §16.2, 04 §16.7 xfail table; open code: 04 §16.8 item 15 |
| 20 | **`T-U-layout-03` is narrowed to the nodes and the two tools** (architect's instruction). As §3.1 states it the rule covers every public callable; measured 2026-09-15, **45 of the flow's 184 public module-level functions** across `approve.py`, `bug_loop.py`, `contract/schema.py`, `corpus.py`, `gate.py`, `generate_task.py` and others carry fewer than the three paragraphs. All 27 nodes and all four tool methods carry them | 03 §3.1, 04 §1.1 | folded: 03 §3.1 in place, 03 §16.2 |
| 21 | §0's dependency paragraph lists the standard-library modules used and is **five short**: `collections`, `fnmatch`, `logging`, `select` and `types` are imported too (plus `__future__`). No third-party dependency is added, which is the requirement; the list is the erratum. `T-U-layout-05` asserts the rule, `sys.stdlib_module_names` plus `yaml`, `ray`, `chia`, the flow's own package and CHIA's two shipped example modules | 03 §0, 01 FR-19.8 | folded: 03 §0 in place, 03 §16.2 |
| 22 | **`T-U-layout-07` fails on nine node modules**: only `corpus.py`, `generate_task.py`, `mutator_synth.py` and `bug_loop.py` return a `CounterBlock`. `probe_task`, `triage_task`, `repair_adapter`, `gate`, `feedback`, `budget`, `ledger`, `store` and `results` return bare records or dicts, so §3.11's "every node of §3.2 returns exactly one `CounterBlock`" is unimplemented across the apparatus. The driver does not repair it: `Campaign.call` synthesises a block from its own timing and records `counters_missing:<arm>:<stage>` as a named violation, which is §3.11's rule for an unbalanced block applied to a missing one. A5 is the one node with a real obstacle: `_COUNTER_STAGES` has no stage a feedback bundle belongs to | 03 §3.11, 01 FR-17.4 | folded: 04 §16.7 xfail table; open code: 04 §16.8 item 16, which also carries A5's open question for the architect |
| 23 | **`T-U-layout-08`'s first assertion fails on `tests/test_repair_adapter.py`**, which sets `BUGLOOP_ALLOW_LIVE_MODEL` with `monkeypatch.setenv` at two places. This is architect's decision 2 unimplemented: `require_live_model` still has no `env=` parameter, so W-14's tests had no other way to reach the refusal. The join owes both halves | generate_task, repair tests (join) | duplicate of W-13 #16; open code: 04 §16.8 item 2 |
| 24 | `ProbeWriteTool.write_probe`'s docstring names `filename` and not `content`, and an MCP method's docstring is what the model reads. `T-U-layout-03`'s tool half asserts the rule as the plan states it, one argument, and this row records the second | 03 §3.5, 04 §1.1 | folded: 03 §16.2 (§3.5); open code: 04 §16.8 item 17 |
| 25 | `cluster_single.yaml`'s `head_setup_commands` and `head_start_ray_commands` are §12.1 verbatim and activate a **conda environment named `circtbugloop`**, which does not exist on the implementation machine: the head environment is the uv venv at `~/.cache/chia-venv` (HANDOFF). W-19 must create that conda environment or edit the two command lists at deployment; the file is left as the design wrote it | 03 §12.1, W-19 | folded: 03 §16.2 (§12.1); open code: 04 §16.8 item 18, owed to W-19 |
| 26 | `upstream/chia-circt-assert.yml`, the workflow §1.2 lists beside the Dockerfile, does not exist. `sync-to-chia.sh` copies whatever `upstream/.github/workflows/` holds and tolerates the directory being empty, so the script is complete and the artefact is owed | 03 §1.2 | folded: 03 §16.2 (§1.2); open code: 04 §16.8 item 13 |
| 27 | `--record-fixtures` records the **`FeedbackBundle`** as well as the four §2.13 names, one JSON document per file under `<dir>/<snake_case schema>/<id>.json`; the id is the schema's own key and `LedgerEntry`'s is `entry_id`. Every instance goes through `contract.validate` first, so a recorder cannot write a fixture the seam would refuse | 03 §13.1, 02 §2.13 | folded: 03 §16.2 (§13.1) |
| 28 | `run_campaign` passes `ref="origin/main"` to `pin_select.select_release_pinned_main`, the clone being detached at `corpus_head_sha`; that is LLD §16.2's own 2026-09-15 erratum, and the driver was written to it | 03 §3.4 | no change owed: the driver was written to 03 §16.2's own erratum and agrees with §3.4 as amended |
| 29 | Test counts, W-16: 30 `T-U-driver-*`, 7 `T-U-cluster-*`, 4 `T-U-submit-*`, 19 `T-U-layout-*` (18 at T0 incl. four `xfail`, one at T1), 13 `T-U-image-*` (5 at T0, 8 at T2). `T-U-layout-10` and the eight tier-2 image tests were run once each on 2026-09-15 and pass; `T-U-driver-26`'s backfill is tier 1 and has not been run | 04 §1.1, §1.20, §1.22, §1.24, §1.25 | superseded by row 32 for the image count; the rest folded: 04 §16.7 counts table (§1.3, §1.20, §1.22) and 04 §1.24 and §1.25 unchanged at 8 and 4 |
| 30 | **`build_image` step 3 looked for the wrong line.** The code read `"llvm pin matches"` out of the build log and the Dockerfile prints `PIN CHECK PASSED`, so every real build would have raised `ImageBuildError("pin", "the pin equality check did not run")` on a build whose pin check ran and passed. The phrase is now the constant `PIN_CHECK_LINE` and `T-U-image-02`'s tier-0 half asserts the Dockerfile prints the string the code looks for. §4.11.1 specifies the step and names no line, which is how the two could disagree | 03 §4.11.1 | folded: 03 §16.2 (§4.11.1 step 3); the code defect was fixed at the time |
| 31 | **`build_image` step 7 measured the wrong thing.** It counted `__assert_fail` in the six target BINARIES and returned the targets that lacked it as `assertion_nonreferencing`, which FR-03.5 and §2.9 define as the `obj.CIRCT` OBJECTS that do not reference it, 19 of 555 at `eade0de6`. It now does both halves: a target binary without the symbol raises (an assertions-off build), and the object list is W-04's own scan (`analysis/measurements/w04_verify_in_image.sh:38-45`), paths relative to the build directory. Run against the published image on 2026-09-15: 19 of 555, the baseline W-04 recorded, reproduced exactly | 03 §4.11.1, §2.9, 01 FR-03.5 | folded: 03 §16.2 (§4.11.1 step 7, §2.9, 01 FR-03.5); the code defect was fixed at the time |
| 32 | Test counts, superseding row 29 after rows 30 and 31: `T-U-image-*` is **15** (6 at T0, 9 at T2). The tier-0 suite is **485 passed, 1 skipped, 4 xfailed**; the tier-2 image suite **9 passed in 16 s**; `T-U-layout-10` at tier 1 passes | 04 §1.1, §1.22 | folded: 04 §16.7 counts table (§1.22, 19 to 15) and the measured-suite paragraph |

## Architect decision on open code item 16 (2026-09-15)
| # | Finding | Decision | Owed to |
|---|---|---|---|
| 1 | A5 `build_feedback` returns no `CounterBlock` because `_COUNTER_STAGES` has no stage for the feedback bundle | Add `"feedback"` to the driver's `_COUNTER_STAGES`; A5 reports under it; `CounterBlock.stage` is a free string in the frozen contract, so no bump | bug_loop.py, feedback.py (join) |

## W-17 (2026-09-14): the join

Every architect decision of the W-13/W-14 section is implemented; every row of
`04-Test-Plan.md` §16.8 is closed but item 8, which needs A7 to run. Twelve
commits, one per fix. The tier-0/tier-1 suite is **570 passed, 1 skipped, zero
xfailed**; the one skip is `T-U-triage-35`, which skips without a token and
which was run separately (row 8 below).

| # | Finding | Owed to |
|---|---|---|
| 1 | **`llm.py` lands, and three defects came out with the move.** Architect decision 3. `MODEL_BACKEND`, `LiveModelRefused`, `require_live_model`, `build_llm`, `llm_turn`, `dispatch_turn`, `PromptContractError` and `parse_json_footer` moved out of `generate_task.py`; `triage_task.py`'s duplicate parser is deleted; `triage_task`, `repair_adapter` and `mutator_synth` import `llm` at module scope and reach the supply half for nothing but `SourceReadTool`. The three defects: `triage_task._run_turn` called `generate_task.llm_turn`, the DECORATED wrapper, which routes through `chia.trace.profiler` and starts a local Ray where none is running (it goes through `dispatch_turn` now, which is why that function exists); `bug_loop._PY_MODULES` is **fifteen** entries and not §13.1's fourteen, a CHIA checkout that shipped the other fourteen shipping no backend at all; and `bug_loop._model_backend` read the constant off the supply half | 03 §1.1, §1.3, §3.5.1, §13.1 |
| 2 | **`require_live_model(purpose, *, need_key, env=None)`**, architect decision 2, and `build_llm` and `repair_adapt` forward it. `repair_adapt`'s `env` is a keyword-only parameter §3.8 does not name; it is `bug_loop.interlock_probe(env=None)`'s own shape applied to the one STAGE that checks the interlock for itself. No test writes `BUGLOOP_ALLOW_LIVE_MODEL`, or a stand-in name for it, into any process environment | 03 §3.5.1, §3.8 |
| 3 | **`T-U-layout-08`'s own walk had a defect.** It matched a bare `setdefault` by method name alone, so `by_rule.setdefault(raw["rule"], [])` in `test_triage_task.py`, a computed key on a plain dict, was reported as an interlock write and was half the reason the row was `xfail`ed. The walk reads the RECEIVER as well now, and a self-check confirms it still catches all four real spellings | 04 §1.3 |
| 4 | **The `SourceReadTool` call site could never have worked.** Architect decision 4: two positional arguments against `(name, clone_path, run_commit, cap_bytes, task_options)` bound `name` to the clone path and raised `TypeError` for the missing `run_commit`, so **every** triage turn would have failed as `turn_failed:TypeError` and nothing caught it because the tool was a stand-in in every test. Fixed by keyword; the name is `src_<candidate_id>`, `cap_bytes` is `artefact_inline_cap_bytes` and `task_options` is `head_options`. New id `T-U-triage-36` constructs the REAL tool, reads a file at the run's commit out of a throwaway repository, and asserts the registry is empty again afterwards | 03 §3.5, §3.7.4; 04 §1.11 |
| 5 | **The mirror's two-direction walk** (architect decision 1, C-22). `mirror_walk` is new: `GithubIssuesNode.recent` hard-codes `direction: "desc"` and takes no parameter, and CHIA is not modified (FR-12.1), so the walk reuses the two halves that matter, `_request` and `_build_issue`, and a recorded response set replayed through `GithubClient._request` exercises it exactly as it exercises CHIA's own. `desc`, then `asc` only on the ceiling and only with the cap unfilled, unioned by issue number. The node's return gains `ceiling_hit`, `pages` and `issues_added`; **they cannot go into `RunManifest.issue_mirror`**, whose six keys `validate` compares exactly and whose contract is frozen at 2.0, so the driver still copies the six and the extras are the return's. A partial walk is no longer discarded: every `GithubError` stops that direction and comes back as a reason, and only the ceiling's own 422 is a ceiling (`T-U-triage-38` shows a 403 recorded as `incomplete_reason`). New ids `T-U-triage-37`, `-37b`, `-38`, all T0 against a fake client that answers 422 at page N | 03 §3.7.3; 01 C-22 |
| 6 | **`issue_mirror_issue_cap` is 12000** `[DEFAULT]`, down from 20000, with the comment rewritten. One direction reaches 9,900 numbered items and two reach 19,800; `llvm/circt`'s newest number on 2026-09-14 was 11,113. A cap above the union's reach is a promise the endpoint cannot keep. **This contradicts `04-Test-Plan.md` §16.8's closing note**, which says architect decision 1 keeps the cap where it is; the work-plan brief for W-17 says 12000 and the code follows the brief | 03 §9.5; 04 §16.8 |
| 7 | **The live mirror, measured 2026-09-14 at the new cap.** `T-U-triage-35` with a token: **2,652 issues (1,795 closed, 857 open), 198 requests, 198 pages accepted, `ceiling_hit` true, 200.2 s wall, cap 12000, `incomplete_reason` None.** Both directions ran their 99 reachable pages, so the union covers the whole numbering; at the old cap the same walk wrote **zero** rows. The token was read from the field before the colon in `~/.git-credentials` and was never printed | 01 FR-10.9; 03 §3.7.3 |
| 8 | **`contract/fixtures/probe_spec/mutation_01.json`** regenerated through `to_json` without the tool in `argv` (architect decision 5), and **`probe_result/assertion_01.json`**'s `assertion_text` and `assertion_site` are now the recorded ones of `tests/fixtures/crashes/assertion_01/expected.json` (§16.8 item 5) | 04 §16.7, §16.8 |
| 9 | **Every node of §3.2 returns a `CounterBlock`** (errata row 22). Nine already returned a mapping and gained the key; ten returned a bare value and now return `{<record>: ..., "counters": ...}`. Four decisions came with it. **(a)** `_COUNTER_STAGES` gains five names, `feedback`, `budget`, `ledger`, `artefact` and `results`: row 22 called A5's missing stage its "real obstacle" and four other nodes had the same one. `CounterBlock` is not a contract member, so this moves neither MAJOR nor MINOR. **(b)** `llm_turn` and `dispatch_turn` take a keyword-only `stage`, a turn belonging to the stage that asked for it. **(c)** `oracle_differential`, `reduce_case` and `artefact_write` have several return statements each and are now four-line counting wrappers over `_oracle_differential`, `_reduce_case` and `write_artefact`, whose bodies are unchanged; `triage_task` calls `write_artefact`, which is the plain function and returns the path it needs. **(d)** `T-U-layout-07` gains a second half, every node's return annotation being `dict`, and every test helper that unwraps one of the new dicts asserts the block's stage at the same time | 03 §2.5, §3.11; 04 §1.1 |
| 10 | **`probe_task`'s two simulator sides are `side`** (errata row 19). §14.5's walk reads the NAME, so the collision reported a module that branches on the campaign's arm nowhere. `T-U-layout-02` covers `probe_task.py` with the other five apparatus modules and additionally asserts the one `arm` the module keeps, `arm=spec.arm` carried into the `ProbeResult`, which FR-18.1 requires of it | 03 §14.5; 04 §1.1 |
| 11 | **`tests/test_prompts.py`** is written, `T-U-prompt-01` to `-08` plus `-02b`. `T-U-prompt-04` reads what each renderer SUPPLIES from the call and not from the source, by recording every `Template.safe_substitute`, so a mapping built in a branch is read as the branch builds it; `$probe_dir` and `$issue_digest` are named as §16.7's exception and asserted supplied anyway. `T-U-prompt-02b` is new and is what makes §7.1's "one function, shared by all four" assertable | 04 §1.23 |
| 12 | **`tests/test_budget_yaml.py`** is written, `T-U-byaml-01` to `-05`. Two deviations. `T-U-byaml-02`'s walk over `01-FRD.md`'s `[DEFAULT]` markers **cannot live here**, this package being at two depths in two trees (§1.4) and `T-U-layout-09` forbidding the ancestor walk it needs; the direction the code can check is that no key of the file is dead, and the FRD walk is a repository-wide check that is owed. §9.3's seven acceptance numbers are transcribed for the same reason. `T-U-byaml-05` is new: the committed bytes are registered, and the same bytes edited after the commit are refused | 04 §1.26 |
| 13 | **The calibration sample is drawn.** `budget.yaml` shipped §9.5's twenty illustrative placeholders, whose own comment said they were not the campaign's draw, so `T-U-byaml-04` could not pass on it. The file now holds twenty of the 171 exact-pin seeds, drawn by `bug_loop.draw_calibration` with `random.Random(corpus_head_sha)` and therefore **re-derivable from the file itself**; `T-U-byaml-04` re-derives it from the committed 187-seed measurement and compares. FR-14.3's order is kept: the draw lands BEFORE the registration commit, which is what "drawn before this file is committed" requires and what the placeholders made impossible. **This is a pre-registration act the brief did not ask for and the architect may reverse it**; it is deterministic and re-derivable either way | 03 §9.5; 01 FR-14.3 |
| 14 | **`cluster_single.yaml`'s head environment** (errata row 25). The three head commands `source ${BUGLOOP_HEAD_ENV}`, expanded from the operator's shell by CHIA's own loader; the comment says this operator's value is `$HOME/.cache/chia-venv/bin/activate` and that the path is the operator's. YAML has no defaulting form and the loader leaves an unset `${VAR}` as the literal, so the variable is REQUIRED and a missing one fails `chia up` loudly. `bug_loop_submit.sh` holds the default and derives the job's own interpreter from it; it is not forwarded through `--runtime-env-json`, which stays at three keys. New id `T-U-cluster-09` | 03 §12.1; 04 §1.24 |
| 15 | **`corpus.py`'s two `decode("utf-8", "replace")`** are `errors="backslashreplace"` (W-08 erratum 20). `T-U-schema-18`'s own check is why it survived: it grepped for the KEYWORD spellings only. It is an `ast` walk over every `decode` call in the flow now, reading positional and keyword arguments alike, with a self-check that it catches both | 03 §3.3; 04 §1.1 |
| 16 | **The seam is recorded.** `contract/fixtures/recorded/` holds sixteen documents covering all seven members, each the return value of the node §3 names as its producer, written through `bug_loop.FixtureRecorder` after `validate` accepted it; the generator is committed beside its output as §13 requires. Two of the four recorded `ProbeResult`s **fired a real assertion**. Three things are not the pilot's and the README says so: the `ImageSpec` is this host's measured build and its digest reads `local-build:`; the mutator set is `set_dev.json` because A7 has not run; and W-09's six recorded failures are **not** re-run, each having fired against a CIRCT build at its own seed's first-parent commit, so re-running them here would record a legalization error and call it a crash. `T-U-fixt-01` and `-02` validate the recorded set beside the constructed one and require it to cover all seven on its own | 04 §0.6, §13; `contract/FROZEN.md` |
| 17 | **Two defects the recording run found.** `FixtureRecorder` identified a `FeedbackBundle` by seed and iteration alone, so the two arms' bundles for one seed overwrote each other (the arm is part of the identity now); and the recorder's build resolution took the first directory holding a `circt-opt`, and two of this host's four CIRCT builds are linked against a `libz3` that is not installed and exit 127 from the dynamic loader, so the first recording classified that as `parse_error` on every probe. It smoke-tests `--version` before it believes a directory is a build | 03 §13.1 |
| 18 | **`tests/test_integration.py`** is written: §2's seventeen `T-I-*` ids over twenty-four pytest items, every replayed side read out of `contract/fixtures/recorded/` through `from_json`. Nothing starts Ray. Fourteen ids are T0; `T-I-gen-app-01`, `-02` and `T-I-app-tri-01` are `t1`. **Two tier relabels, both downward**: §2's column says T2 for the two `gen-app` rows and they run at T1, the binary being the host's measured build rather than the image's. `test_integration.py` is `T-U-layout-01`'s **third** structural test module: §2's slugs name a crossing and not a module | 04 §2, §1.3 |
| 19 | **`bug_loop.py` writes no row of any table.** Found by `T-I-rep-gate-02`, which set out to read back the `gate_decision` row §6.2 declares and §6.4 orders. Nothing writes it, and nothing writes `run`, `probe`, `build_result`, `probe_result`, `oracle_verdict`, `reduced_case`, `repair_result`, `filing`'s upstream rows or `issue_mirror_meta` either: `triage_task._write_rows` writes three tables and `approve.py` writes filings, and the driver writes none. It went unnoticed because every results test runs against `tests/fixtures/results/make_store.py`'s hand-built store. **This is the largest open defect in the tree** and it is not the join's: §6.4's write order is a section of its own and the driver is W-16's. Until it lands, a real campaign produces a `loop.db` with three tables filled and `results.render_results` refuses | 03 §6.2, §6.4, §13.1; 05 W-18 |
| 20 | **`issue_mirror_meta` is never written either**, which is the same finding at one table: `bug_loop._mirror` READS the meta row to decide whether a mirror can be reused, and nothing inserts one, so `--refresh-mirror` is effectively mandatory on every run | 03 §3.7, §6.2 |
| 21 | **§16.8's remaining items are closed.** Item 6 and 7 together: `tests/fixtures/secrets/known_values.txt` is created with §13's three synthetic strings, and `conftest.py` now does both halves of §0.5's rule, still refusing to start with the interlock set and now setting `GEMINI_API_KEY` to the synthetic value for every test below T3; `T-U-layout-08` (3) reads that effect. Item 9: `tests/fixtures/fetch_clone.sh` is written and READS the corpus head out of `budget.yaml` rather than repeating it, with new id `T-U-corpus-39` asserting that and the quoted tag refspec. Items 11 and 12: five docstrings gain the id the plan allocates them. **Item 8 stays owed**: `mutators/set_v1.json` needs A7 to run, which needs a model | 04 §16.8, §13 |
| 22 | Test counts, W-17: `T-U-prompt-*` **9** (8 ids plus `-02b`), `T-U-byaml-*` **5**, `T-I-*` **17 ids over 24 items**, `T-U-triage-*` **39** (`-36`, `-37`, `-37b`, `-38` new), `T-U-corpus-39` new, `T-U-cluster-*` **9**, `T-U-gen-*` unchanged at 26 but split across three modules, `tests/test_llm.py` being the third. Whole tier-0/tier-1 suite: **570 passed, 1 skipped, 0 xfailed** in 68 s | 04 §1, §2 |

## W-19a (2026-09-14): the single-machine cluster brought up, `T-U-cluster-01` without the driver

`chia up circt_bug_loop/cluster_single.yaml` on `cachyos-x8664`, five containers, four
trivial CHIA nodes, `chia down`. No model call; `BUGLOOP_ALLOW_LIVE_MODEL` never set and
`GEMINI_API_KEY` a placeholder string. It did not come up as the file stood: four defects,
three fixed in the YAML and one in the head environment. Bring-up 36 s, teardown 177 s,
resources exactly `llm: 2` / `circt: 2` / `repair: 1`, the six tool binaries hash to
`raw/image-manifest.json` inside a running worker (FR-03.16). The eight `T-U-cluster-*`
tests pass unchanged after all three YAML edits; no test was edited. Full record in
`analysis/measurements/2026-09-15-cluster-smoke.md`.

| # | Finding | Owed to |
|---|---|---|
| 1 | **The assertions image cannot be pulled and cannot resolve.** §12.1 wrote `ghcr.io/ucb-bar/chia-circt-assert:${BUGLOOP_IMAGE_TAG}` with `pull_before_run: True`; W-04 built that image locally and never published it, so `docker pull` answers `error from registry: denied` and CHIA runs the pull with `check=True` (`chia:chia/cluster/docker.py:34-37`), failing `chia up` before the first container — and CHIA appends `--pull=never` to every `docker run` (ibid. 56-61), so the registry-qualified name could not resolve locally either. Both circt-side types now name `chia-circt-assert:${BUGLOOP_IMAGE_TAG}`, the tag `raw/image-manifest.json` records, with `pull_before_run: False`. Only the LOCATION changes; the driver's check is a suffix match (`bug_loop.py:2058`) and both spellings satisfy it. `bugloop_llm` keeps ghcr and keeps its pull | 03 §12.1 |
| 2 | **`/etc/passwd` is not writable in the llm image**, so both llm workers FAILED to set up: the first `run_setup_commands` line appends a passwd entry and CHIA runs the block under `set -e`. `/etc/passwd` is `-rw-r--r-- root root` in `ghcr.io/ucb-bar/chia` and `-rw-rw-rw-` in `chia-circt-assert`, so the line only ever worked on the circt side. `|| true` on the `bugloop_llm` line only; nothing is lost, uid 1000 in that image is already `ray` and already has the entry | 03 §12.1 |
| 3 | **`-v $SSH_AUTH_SOCK:/ssh-agent` needs a live agent in the operator's shell.** The unbraced form is left alone by CHIA's loader (`chia/cluster/config.py:300-304`) and expands in the remote `bash --login`, which CHIA opens with `ssh -A`; with no local agent the remote value is empty, the option becomes `-v :/ssh-agent` and `docker run` rejects it. Not a YAML defect — an agent was started on the head and the requirement is now stated in the file's header, beside the exports | 03 §12.1 |
| 4 | **The head environment's Python cannot run this cluster.** `~/.cache/chia-venv`, which `cluster.env` and `bug_loop_submit.sh` both name, is Python 3.10.21; both images ship 3.10.19, which CHIA's quickstart calls the required version; Ray compares at patch level by default (`RAY_DEFAULT_PYTHON_VERSION_MATCH_LEVEL`), so every `ray start --address=...` died with `Version mismatch` and **no worker could ever have joined**. Errata row 25 / W-17's row 14 replaced §12.1's non-existent conda environment with this venv and introduced it. A 3.10.19 venv was built for the run (`~/.cache/chia-venv-py31019`) and `~/.cache/chia-venv` left untouched, `bug_loop.py` being worked on against it. **Not fixed in the repository**: the project must decide whether that venv is rebuilt at 3.10.19 or the new one becomes the head environment. Two further mismatches in the same file: `cluster.env` sets `BUGLOOP_HEAD_ENV` to the venv DIRECTORY where §12.1 and the wrapper both want the ACTIVATE SCRIPT (`source` of a directory fails; the wrapper's `PYBIN="$(dirname "$BUGLOOP_HEAD_ENV")/python"` resolves to `/home/adi/.cache/python`) | 03 §12.1; 05 W-19; errata row 25, W-17 row 14 |
| 5 | **`${BUGLOOP_ALLOW_LIVE_MODEL}` reaches the container EMPTY, not as the literal.** §12.1's header and `T-U-cluster-08` reason that an unset `${VAR}` survives the loader as seven literal characters and that `build_llm` refusing a `${`-prefixed value is therefore a live defence. The loader does leave the literal — and the literal is then expanded a SECOND time by the remote `bash --login` that runs `docker run`, where the variable is also unset, so the container gets the name with an empty value. Measured in all three keyed containers. The interlock still holds (empty is falsy; the count in the circt containers is 0), and the same double expansion applies to `${GEMINI_API_KEY}` | 03 §12.1 header, §11.2; 04 `T-U-cluster-08` |
| 6 | **`--net=host` puts six raylets in one network namespace.** Two prestarted Ray workers died during the four dispatches with `Address already in use` on ports 10023 and 10024: CHIA hard-codes `--net=host` and every raylet takes the default `--min_worker_port=10002 --max_worker_port=19999`. Nothing was lost at this concurrency and `ray status` showed no failures, but under campaign load the same collision reaches a TASK worker and the loop's nodes are `max_retries=0`. `worker_start_ray_commands` is one cluster-wide list in this schema, so disjoint per-type ranges are not expressible in the YAML as it stands | 03 §12.1; 05 W-19b/W-20 |
| 7 | **The container limits exceed the machine.** Host: 20 cores, 15.23 GiB RAM. §12.1's limits: 24 g × 2 + 32 g = 80 GiB of memory and 20 CPUs on the circt side plus two unbounded llm containers. Ray advertises 80 CPU, 57.62 GiB memory and 24.69 GiB object store, roughly four times what exists, because it sums per-container limits. NFR-05's outer memory guards therefore never bind on this host. Either the campaign runs on a machine matching §12.1's numbers or those numbers come down to this one; FR-14.5's equal-wall-clock claim is a claim about this cluster at this concurrency, so it is the architect's call | 03 §12.1; 01 NFR-05, FR-14.5 |
| 8 | Smaller: `chia` has no `--version` (the checkout revision is the version); `ray status` warns `Found multiple active Ray instances` for the one cluster reachable as both hostname and IP, harmlessly; `chia down` (177 s) takes five times `chia up` (36 s), the teardown doing a scoped `ray stop` inside each container over one SSH session at a time | — |

## W-18 (2026-09-14): the driver's write path, §6.4 landed

W-17 row 19's defect is closed: `bug_loop.py` wrote no row of any table and
`render_results` refused every real campaign. Every table §6.2 declares now has
a producer, and a store the driver itself produced renders with **zero**
refusals (`T-U-driver-32`). Nothing below changes a node's return, so
`contract/fixtures/recorded/` is untouched by this work (row 18).

| # | Finding | Owed to |
|---|---|---|
| 1 | **§6.4 is a write ORDER and two rules and names a producer for almost nothing.** The section says which row must precede which and never who writes it, so the driver's write points are now the normative list and §6.4 owes a producer column: `run`, `image`, `seed`, `sdk_map` and `issue_mirror_meta` from `write_run_rows` before the first dispatch; `probe` before stage 3 and `probe_result` after the probe stops; `build_result`, `oracle_verdict`, `differential_verdict`, `reduced_case`, `report`, `repair` and `gate_decision` each as its stage returns; `feedback` per A5 bundle; `candidate`, `fingerprint` and `dedup_verdict` from B6b unchanged; `filing` from `approve.py` unchanged; `issue_mirror` from B6a unchanged; `ledger_entry` from `ledger.accrue` at every point below | 03 §6.4, §13.1 |
| 2 | **`candidate` has two producers and §6.4 rule 2 assumes one.** B6b writes every candidate it screens, in its own transaction with the row's `fingerprint` and `dedup_verdict` (rule 4's first batch), and FR-08.10 keeps a `differential` candidate out of that stage entirely - `dedup_and_screen` raises `ValueError` on one. §3.6.3's "the report-only flag lives on the `CandidateRecord` the head writes" is therefore literal: `bug_loop.write_candidate` is the only writer of a differential candidate row, and it calls `validate_candidate` first exactly as B6b does | 03 §6.4, §3.6.3, §2.9 |
| 3 | **§6.4 rule 1 attributes the `local_id` update and the `repair` insert to `repair_adapter` and B8 cannot write either.** It runs on a `repair` worker holding no `loop.db` handle, which is why `mint_local_id` is the head's half of B8 (§3.8). The head therefore writes both, in one transaction, **before** it dispatches stage 7: the candidate's `local_id` and a `repair` row at status **`dispatched`**, completed by an UPDATE from the returned `RepairResult`. `dispatched` is not one of `RepairResult.status`'s six values: it is the state of a row whose record does not exist yet, and an attempt that never comes back leaves it, which is what FR-12.10's ordering is for | 03 §6.4 rule 1, §3.8, 01 FR-12.10 |
| 4 | **`probe_result` is written once, when the probe stops, and not at stage 3.** §2.4 makes it "produced by B2 and completed by the stage the probe stopped at", so a row written at stage 3 would carry `stopping_stage="stage_3"` for a candidate that reached the gate. It is also the completion record §6.5 requires before a PARTIAL marker may be cleared, which is the second reason it is the probe's last row | 03 §2.4, §6.4, §6.5 |
| 5 | **Three §6.2 tables no rule of §6.4 mentions and no node writes**: `seed`, `sdk_map` and `feedback`. The driver writes all three (A1's return, A1's map, A5's bundle beside its `feedback.json`). `seed` is not optional: §14.4's `_require_validation_table` refuses an artefact whose run has no seed rows, so without them no campaign could ever render | 03 §6.2, §6.4, §14.4 |
| 6 | **`seed.exclusion_reason` needed a spelling for §5.3 and both eligibility columns take one value.** A1 returns one reason per excluded seed (`no_run_line`, `unsupported_shape`, `seed_text_over_cap`) and, separately, the 36 seeds ADR-D-13 branch (b) excludes. The driver writes `sv_frontend_unavailable` for the second, **only when `ImageSpec.slang_enabled` is false**, and sets `eligible_seeded` and `eligible_mutation` together: §6.2 has two columns and no rule makes a seed eligible for one arm and not the other | 03 §6.2, §5.3 |
| 7 | **`run_campaign` never passed `sv_seeds_excluded` to `build_manifest`**, which §13.1's table attributes to A1; the parameter existed and no caller filled it, so a branch-(b) run would have recorded no exclusion. It is passed now, from `mined["sv_seeds"]`, and is None under branch (a) | 03 §13.1 |
| 8 | **`issue_mirror_meta` is stamped for the reusing run too.** The table is keyed by `run_manifest_id` with a foreign key to `run`, and §3.7 says B12 writes the row "from the return" - which the FR-10.9 reuse path has none of, `_mirror` returning the previous run's row. The driver writes one row per run either way, from whichever block it holds, so `--refresh-mirror` stops being effectively mandatory (W-17 row 20) and a candidate's screening mirror is traceable to its own run | 03 §3.7, §6.2 |
| 9 | **`run` and `image` are insert-if-absent, which §6.4 does not discuss.** `image.image_digest` is a primary key and one image serves many runs; `--resume` reuses a run id. Both are checked for first, so a resumed run adds neither and a second run of one image does not raise | 03 §6.4 |
| 10 | **Per-stage `ledger_entry` occupancy had no producer at all.** `ledger.aggregate` counts FR-13.8's per-day cap off `scope='stage'` rows at `stage_3`, and `results._observed` sums tokens and money off every entry's `observed` block, and nothing wrote one. `Campaign.call` now accrues exactly one entry per dispatched stage, `entry_id` a `uuid5` over `(run, arm, stage, node, unit key)` so a double charge is the primary-key violation §6.4 rule 3 asks for rather than a silent second row. `metered` comes from `RunManifest.stages_metered` (FR-14.8's fact, not the node's) | 03 §3.11, §6.4 rule 3, 01 FR-13.8, FR-14.5 |
| 11 | **A3 is one node and two stages, so it is charged as two entries.** `generate_seeded` returns a per-turn usage map (`logs["usage"]["seed_read"]`, `["probe_write"]`); the seed-read turn is charged at `stage_1` with its own wall clock and the rest of the call at `stage_2`, and the two amounts sum to the node's own `CounterBlock.seconds`. §3.11 fixes what a counter counts and says nothing about how a two-stage node's occupancy is split | 03 §3.11 |
| 12 | **The `shared` `synthesis` entry cannot be A7's and is the run's.** §14.4's `_require_mutator_declaration` reads FR-05.8's synthesis date off a `shared` ledger entry at stage `synthesis`, and A7 runs offline **before** the registration commit, so it cannot write one: `ledger_entry.run_manifest_id` has a foreign key to `run` and no run exists yet. `accrue_offline` writes it once per run, timestamped from the frozen set's own `synthesised_utc` (the set records the date and not the duration, so the amount is 0.0), beside a `shared` `image` entry from B1's counters | 03 §9, §14.4, 01 FR-05.8, FR-14.4 |
| 13 | **B4 had no caller.** §3.6.3 says "the driver calls it for the seeds FR-08.1 admits and for no others" and `Stages` had nine fields with no differential oracle, so `differential_verdict` could never have had a row and FR-08.1's whole path was dead. `Stages` has **ten** fields now. §3.6.3 gives applicability and no dispatch condition; the driver's is *the primary oracle did not fire* **and** `differential_applicable(spec)`, because a tool that died has no design to simulate and its crash is stage 4's answer | 03 §3.6.3, §13.1 |
| 14 | **A divergence's candidate, report and stopping point are the driver's.** The candidate is `oracle_class="differential"` with every conditional group None (§2.9's partition), the report is `triage_report(..., differential=verdict)` and takes the `differential` template, and the probe stops at `stage_6`: no reducer, no dedup, no repair, no gate (FR-08.10). A `not_applicable` or `agree` verdict is still written to `differential_verdict` and stops the probe at `stage_4` | 03 §3.6.3, §2.9, 01 FR-08.9, FR-08.10 |
| 15 | **Rows are built from the schema's own column list.** `table_columns` reads each table's columns through `LoopStore.query` (`pragma_table_info`) and `_row` fills them by field name, `_json` columns taking the field without the suffix as canonical JSON. §6.4 implies a hand-written INSERT per table, which is a second spelling of §6.2's DDL and the way a value lands in the column beside the one it belongs in | 03 §6.2, §6.4 |
| 16 | **The PARTIAL marker of a probe directory is the DRIVER's, not "the stage's".** §6.5 says the marker is written by the stage before it writes anything else into a directory and removed as its last act; five stages write into one probe directory, so there is no such stage. The driver writes it before stage 3 and clears it after the `probe_result` row exists, `write_artefact` still creating it lazily for any other writer. A probe whose stage 3 never returned keeps its marker, which is the protocol working | 03 §6.5, 01 FR-17.8 |
| 17 | **`--dry-run` still dispatches nothing and now writes five tables.** §13.1 says a dry run runs every check and builds the manifest; it now also lands `run`, `image`, `seed`, `sdk_map` and `issue_mirror_meta`, so an operator can read what the run would have been taken against. It does not render: no arm ran, and `_require_both_windows` refuses an artefact with no `arm_window` entry, correctly | 03 §13.1 |
| 18 | **`--record-fixtures` is not byte-for-byte reproducible, and was not before this work.** Measured 2026-09-14: a re-recording of `contract/fixtures/recorded/` differs in `LedgerEntry.observed.cpu_seconds`, `LedgerEntry.amount` and `ProbeSpec.turn_cost.wall_seconds`, which are this host's measured timings, and in `RunManifest.cluster_yaml_sha`, because `cluster_single.yaml` was committed at `8a1b611` after the recording at `8de68d8`. The README names two moving fields (`budget_file_sha`, `issue_mirror.refreshed_utc`) and not these. The committed set is left as it is: `make_recorded.py` calls each node directly and never goes through `campaign_drive`, so nothing in this change reaches it, and re-recording now would fold another agent's in-flight cluster YAML into the frozen seam | 04 §0.6, §13; `contract/FROZEN.md` |
| 19 | **Open: four §6.5 artefacts still have no writer**, all of them the driver's: `corpus/seeds.json`, `corpus/sdk_map.json`, `corpus/counts.json` and `image/image_spec.json` (with `lit_discovery.txt`, `assertion_objects.txt` and `build.log`, which need `build_image` to return more than the `ImageSpec`). Every row they duplicate is in `loop.db`, so nothing refuses without them; they are the artefact tree's copy | 03 §6.5, §4.11.1 |
| 20 | Test counts, W-18: `T-U-driver-*` **35** (`-32` the render over a driver-produced store, `-33` every table and the write order, `-34` the SQLiteNode dispatch path, `-35` the repair row's order); the whole tier-0/tier-1 suite is **574 passed, 1 skipped, 0 xfailed**. `T-U-driver-30`'s fixtures are a mini-campaign rather than nine stubs: stage 3 classifies a recorded stderr through the real `classify_build` and the screen writes its three rows through B6b's own `_write_rows`, so `-32`'s render is over rows the driver wrote from records the real functions produced | 04 §1.20 |
| 21 | **The iteration directory's PARTIAL marker has no one who can clear it.** `generate_task._write` creates one for every turn file it writes and its own docstring says "never removed by this half"; `_completed` keys on `probe_result.artefact_dir` and `candidate.artefact_dir`, which are PROBE directories, so `artefact_write` would refuse to clear an iteration directory's marker whoever asked. The driver therefore writes `feedback.json` directly, as it writes `manifest.json`, the budget copy, `counters.json` and `results.md`, rather than adding a marker nothing can remove. §6.5's protocol assumes one stage per directory and the iteration directory has three writers | 03 §6.5, §3.5, 01 FR-17.8 |

## W-05c / A-19 (2026-09-14): the differential's applicable count over the corpus

**A-19 is closed by measurement.** FR-08.1's rule, run over the 187-seed corpus at
`d7e94049…` and asked exactly as `bug_loop._drive_differential` asks it (the seed's
`entry_tool` and `generate_task.probe_argv`, that is `argv_template[0]` stripped and bound),
admits **6 of 187** seeds — **6 of the 171 exact-pin seeds** — all six `circt-opt`, all six
MLIR, one per HW-terminal lowering the corpus exercises, none of them excluded by FR-01.9,
FR-01.10 or the inline cap. A wider "any run line under that line's own tool" reading returns
the **same six**, so FR-01.3's first-line convention costs nothing here. The count is above
zero, so R-09 does not fire and F-08's harness generators and `T-U-probe-50` are worth
writing. Going one step further, the six seeds' own recorded test inputs were lifted with
their own pipelines against the measured build and handed to `probe_task.extract_port_list`:
**3 of 6** yield a port list. No model call, no docker, no cluster, and neither simulator was
run. Whole measurement 7.3 s. Full record in
`analysis/measurements/2026-09-15-a19-differential-applicability.md`; script
`analysis/measurements/a19_applicability.py`; raw `analysis/measurements/raw/a19-*`.

| # | Finding | Owed to |
|---|---|---|
| 1 | **FR-08.1's `--pass-pipeline=` justification is false on the corpus, and it costs a sixth of the applicable set.** The requirement and `differential_applicable`'s docstring both say "**50** of M1's corpus `RUN:` lines use that spelling and **none** of them is HW-terminal, so reading the last pass out of a nested pipeline string would be code for no case". The 50 is right; the none is not. `raw/m1-per-runline.csv` line 29 is seed `3f65acfd617b` (`[FIRRTLToHW] Fix firrtl enum lowering (#10648)`, exact-pin, **one** run line): `circt-opt -pass-pipeline="builtin.module(lower-firrtl-to-hw)" -verify-diagnostics %s --split-input-file \| FileCheck %s`. Parsing that spelling for an HW-terminal lowering takes the count from **6 to 7**. Refusing to parse is still a defensible choice — but its stated reason is measurably wrong and its price is one seed in six, not zero | 01 FR-08.1 (the amended paragraph); `probe_task.differential_applicable` docstring; 04 `T-U-probe-27`'s comment |
| 2 | **`firtool`'s default-Verilog exclusion, priced.** Already flagged as an erratum candidate in `test_u_probe_27`. Admitting `firtool` with no explicit output mode takes the count from **6 to 7** in the driver's reading and from 6 to 10 in the wider one. Separately: exactly **one** of the 187 seeds enters through `firtool` at all, and none of the **nine** `firtool` run lines in the whole corpus requests `--ir-hw`, `--verilog` or `--split-verilog`. **The `firtool` half of FR-08.1's rule matches no corpus seed under any reading**; the entire count is the `circt-opt` half | 01 FR-08.1; 04 `T-U-probe-27` |
| 3 | **The seventh HW-terminal pass is code for no case, which is the charge FR-08.1 levels at `--pass-pipeline=`.** All seven `--lower-*-to-hw` conversions are registered in the measured build (`circt-opt --help`, `bassert_g`, CIRCT `5056ff0`), but only six have a corpus seed: `--lower-pipeline-to-hw` has none. Keeping it is right, the rule having to outlive this corpus; describing the enumeration as derived from the corpus is not | 01 FR-08.1 |
| 4 | **`--verify-roundtrip` is treated as a pass, latently.** Seed `351189822e33` (`[ESI] Window lowered type fix (#9243)`) has `circt-opt %s --lower-esi-ports --lower-esi-to-hw --lower-esi-types --verify-roundtrip` on its second run line. `--verify-roundtrip` is a registered pass, so `_NOT_A_PASS` omitting it is not itself wrong, and the count does not move (the last **lowering** there is `--lower-esi-types`, not HW-terminal) — but "the pipeline ends in the HW dialect" and "the last option that is a pass names an HW lowering" are not the same sentence, and a verification pass appended after a lowering is where they come apart | 03 §3.6.3; `_NOT_A_PASS` |
| 5 | **Single-dash spellings are load-bearing for this number, not decorative.** Four of the six applicable seeds spell their conversion with one dash. Without W-09 finding 3's both-spellings rule the count would be **2** | none; recorded so the number is not read as robust to that rule |
| 6 | **`entry_tool_of_line`'s `circt-capi-.*` is still greedy in `corpus.py:62`.** The mine's `counts["entry_tool_seeds"]` carries the literal key `"circt-capi-firrtl-test 2>&1 "`. W-05 erratum 14 fixed exactly this in `analysis/measurements/m1_runlines.py` and not in the package. No verdict changes — `classify_entry_tool` maps anything outside the five names to `other` — but a reported count's key holds a shell fragment | `circt_bug_loop/corpus.py:62`, `_TOOLS` |
| 7 | **The port-list step, and what it does not show.** Of the six, two lift and yield a port list whole-file (`73e28e09a3b5` top `main`, 7 ports; `91872b228b74` top `VerbatimBlackBoxTest`, 4 ports) and one more does at chunk level (`99826b849987` chunk 7, top `sigAndOps`, 5 ports); two lift but hold 12 and 14 public `hw.module`s, which is `no_top`; one is a negative test and does not lift. Two things worth carrying: **`_CLOCK_NAMES` earns its place** — the Calyx design's clock is `clk : i1`, not `!seq.clock`, so the type test alone would have said `no_clock` — and **`no_clock` is the commonest design-level refusal**, 9 of HWArith's 13 chunks being purely combinational. Whether a generated harness BUILDS AND RUNS on one of these designs is still open and is W-18's pilot; A-08 is closed on the recorded register-add (W-17 row 14) and on nothing mined | 01 A-19 (close), A-08 (stays as it is); 03 §3.6.3, §9.4; 05 A-19 row |

## W-20b K10/K11 (2026-09-14): `upstream/vertex-usage.patch`, CHIA's Vertex token accounting

`upstream/vertex-usage.patch` is the K11 disposition's patched copy of
`chia/models/vertex.py`, plus K10's half that CHIA can fix on its own side. Written
against `ucb-bar/chia` at `16c35e92`; `git diff --stat` is one file, **18 insertions,
1 deletion** (the deletion is the `meta = {...}` initialiser, respelled to bind the
same dict to `self`). Verified in a throwaway `git worktree` of `~/.cache/chia-src`:
`git apply --check` clean, applies, `ast.parse` clean, and CHIA's own
`chia/models/tests/test_vertex.py` gives **37 passed, 11 skipped** on the patched
file — byte-identical to the unpatched baseline, and no test there asserts the key
set of `_last_metadata`: six assert individual keys by subscript and four (the
skipped live tests) read one with `.get(...)`, so nothing had to be weakened. New tests: `circt_bug_loop/tests/test_upstream_patches.py`,
three tier-0 (`T-U-upstream-01..03`), 3 passed.

| # | Finding | Owed to |
|---|---|---|
| 1 | **`thoughts_token_count` and `tool_use_prompt_token_count` are the two billed fields CHIA sums nowhere, and both are separate from the two it does sum.** Confirmed against the installed `google-genai` (`GenerateContentResponseUsageMetadata.model_fields`): the eleven fields include `prompt_token_count`, `candidates_token_count`, `thoughts_token_count` and `tool_use_prompt_token_count` as four distinct scalars, and the SDK's own description of `total_token_count` is "the sum of `prompt_token_count`, `candidates_token_count`, `tool_use_prompt_token_count`, and `thoughts_token_count`" — so the four are disjoint and pricing `thinking_tokens` at the output rate does not double-count what `candidates_token_count` already holds. The patch adds `thinking_tokens` and `tool_use_prompt_tokens` to `_last_metadata` and leaves `input_tokens` / `output_tokens` exactly as they were, so a consumer that reads only the old two keys is unaffected and one that wants the billed total adds the new two. **`llm.py` must now read them**: `thinking_tokens` is billed at the OUTPUT rate (ADR-D-03) and `tool_use_prompt_tokens` at the input rate, and nothing in this tree reads either key yet. Measured only against a fake client — how many thinking tokens `gemini-3.8-flash` actually emits still needs a live call | K11; `circt_bug_loop/llm.py` (fix round), 03 §3.11 pricing |
| 2 | **`_last_metadata` is reset per ATTEMPT, not per turn, and the patch does not change that.** `VertexGeminiLLM.prompt` assigns `self._last_metadata = {}` at the top of each of its `retries` attempts (`vertex.py:288`), so a turn that fails twice and succeeds on the third attempt still reports only the third attempt's counts — the first two attempts' tokens are billed and lost. The patch fixes only the case K10 states, a turn that RAISES out of `prompt`, by finalising in `_run_generate`'s `finally`; summing across attempts would change what `_last_metadata` means (currently: the last attempt's) and is a second, larger upstream argument. **The loop's own exposure is therefore an undercount of up to `retries - 1` attempts per priced turn**, which K10's `observed=False` marking does not cover, because the successful attempt does report counts | K10 (residual); 01 FR-14.6, 03 §3.11 |
| 3 | **Staging, not patching the operator's checkout, is how both patches reach a run — and only one of the two is wired anywhere today.** The K3/K11 disposition has the driver stage a patched copy under `circt_bug_loop/_shipped/` at startup, with pre-flight check 13 for the `vertex` branch in `issue_task.py` and check 14 for this patch in `vertex.py`. Neither check nor the staging exists yet (fix round). `upstream/sync-to-chia.sh` applies `issue_task-vertex-branch.patch` only (`sync-to-chia.sh:151`); it needs a second `git apply` for `vertex-usage.patch`, or the sync path and the shipped path disagree about which `vertex.py` a campaign runs | K3, K11; `circt_bug_loop/bug_loop.py` staging + checks 13/14, `upstream/sync-to-chia.sh` (both fix round) |
| 4 | **Hand-off, not an erratum: the new test module breaks `T-U-layout-01`'s equality.** `test_layout.py:207` compares the set of `test_*.py` files against `SOURCE_MODULES | STRUCTURAL_TESTS | EXEMPT_TESTS | EXTRA_MODULES` for EQUALITY, so `test_upstream_patches.py` fails it as an extra item (verified: 1 failed). It is an artefact test module in the sense `test_image.py` is — its artefacts are `upstream/*.patch`, not a source module — so the fix is `EXEMPT_TESTS` growing to seven with the matching §1.3 row, exactly as the Dockerfile's row reads. Not fixed here: `test_layout.py` is another agent's file this round | 04 §1.3 exemption list, `circt_bug_loop/tests/test_layout.py` (fix round) |

## W-19b (2026-09-14): the system tier on the single-machine cluster, no model

`04-Test-Plan.md` §3's tier as far as it goes without a model turn, driven through
`bug_loop.py`'s new `--generator recorded` onto the real `cluster_single.yaml`
cluster. Measurement record: `analysis/measurements/2026-09-15-system-tier.md`.
`pytest -m t0`: **553 passed, 1 skipped**. `tests/test_system.py` on the live
cluster: **15 passed, 1 xfailed**, 26 s. `chia up -y` 34 s, `chia down -y` 178 s,
zero Ray worker deaths, teardown clean.

| # | Finding | Owed to |
|---|---|---|
| 1 | **`Dispatch` never dispatched: every stage of every run so far executed in the driver process on the head.** Its remote branch read `isinstance(fn, ChiaFunction)`, and that is FALSE for every node in the flow — `ChiaFunction.__call__` returns a `functools.wraps` closure carrying `_chia_original`, `_chia_options` and `chia_remote` as ATTRIBUTES and merely `cast` to the `ChiaWrapped` protocol (`chia:chia/base/ChiaFunction.py:108-129`), so `type(probe_execute)` is `function`. Measured across seven nodes, all False. At `remote=True` the call fell through to `_chia_original(...)`, so the fixed-size cluster, the `circt: 2` concurrency, `apparatus_concurrency` and FR-14.5's equal-wall-clock argument were decorative. It hid because the head can run most nodes (the clone, the store and the artefact root are there) and surfaced only at `probe_execute`, which hashed a `/workspace/circt/build/bin/circt-opt` that does not exist on the head, got `""` and raised `BinaryMismatch` for all sixteen probes — FR-06.1's check catching a defect it was not written for. **FIXED**: the predicate is `hasattr(fn, "chia_remote")` | fixed in `bug_loop.py`; 03 §13.1's `Dispatch` paragraph and §3.2's placement table are now load-bearing for the first time and should say so |
| 2 | **`mutator_seed_int` overflows SQLite for half of A4's mutants, and the overflow stops the campaign.** `mutators.mutant_seed_int` returns `int.from_bytes(digest[:8], "big")`, an UNSIGNED 64-bit integer; SQLite's INTEGER is signed, so `write_probe` raises `OverflowError` out of `SQLiteNode.execute` — **outside `drive_probe`'s try** — and it propagates through `drive_seed` and `campaign_drive`. Measured: **9998 of 20000** derivations exceed `2**63-1`, both committed mutation `ProbeSpec` fixtures are already over it (`11630822309445067853`, `12839405957335514270`), and plain `sqlite3` raises on the same value. Expected time to failure in a pilot's mutation arm: the second probe. **NOT FIXED**: masking the derivation changes every mutant the arm produces and so must be decided before A7 freezes a set. `bug_loop._seed_int` masks the recorded mode's own seed to 63 bits so this mode does not reproduce it | `circt_bug_loop/mutators/__init__.py:mutant_seed_int` or `store.py`'s column; 03 §8.2 rule 5, §6.4; the two recorded `probe_spec` fixtures need re-recording after the fix |
| 3 | **The submitted job could not import its own package.** `bug_loop_submit.sh` runs `python <flow dir>/bug_loop.py`, which puts the FLOW directory on `sys.path` and not its parent; in this tree every import in `bug_loop.py` is `circt_bug_loop.<module>`, so a real `chia job submit` died with `ModuleNotFoundError: No module named 'circt_bug_loop'` before its first line of work. **FIXED** in the wrapper, as `env PYTHONPATH=<flow dir's parent>` on the ENTRYPOINT: not in `bug_loop.py`, because FR-19.2 forbids a flow module to walk past its own directory and `T-U-layout-09` enforces it by AST (the first attempt failed that test, correctly); and not in `--runtime-env-json`, whose key set §11.2 fixes at three. `T-U-submit-03`'s argv assertion moved by two positions | fixed in `bug_loop_submit.sh`, `tests/test_submit.py`; 03 §11.2 and §13.1 owe the sentence that the job's entrypoint carries the flow's parent |
| 4 | **`render_results` refuses at the end of EVERY campaign, and `main` does not catch it.** Thirteen of §14.4's fourteen refusals are satisfied by a real run's store; the fourteenth is FR-10.2's collision and false-merge rates, measured over a HAND-LABELLED duplicate-pair set that `render_results` takes as a parameter. `run_campaign` calls it as `render_results(store, manifest)` and offers no way to pass one, `_dedup_rates`'s `if not labelled_pairs` gap fires, `ResultsIncomplete` is a bare `Exception`, and `main` catches only `(ImageBuildError, BuildTimeout, OSError, ValueError)` — so a campaign runs both four-hour arm windows and then dies with a traceback instead of writing its results artefact. Second half: a labelled set may only name candidates the store has fingerprints for, so a run that confirms nothing cannot supply one either. **NOT FIXED**; pinned by `T-S-regen-01`, which asserts the refusal set is exactly that one element today | `circt_bug_loop/bug_loop.py` (a `--labelled-pairs` path, and catching `ResultsIncomplete`); 01 FR-10.2, FR-18.7; 03 §14.4 |
| 5 | **B1 `build_image` is placed where no Docker daemon is.** It is `@ChiaFunction(resources={"circt": 1})` and its docstring says "it needs a Docker daemon and the CIRCT worker type is the one that has one". Measured: `docker run --rm chia-circt-assert:eade0de61bc5 which docker` exits 1, and `cluster_single.yaml` mounts `/var/run/docker.sock` on no type — the CIRCT worker type IS a container of the image B1 would build. Either that type mounts the socket, which gives every probe root on the host and is a poor trade for a node that runs generated input, or B1 moves to the head, where the daemon and the build context actually are. This is one of the four pre-flight checks that makes `run_campaign` unreachable on this host today (the others: check 3's clone head, check 10's issue mirror with no token, check 11's forum post) | 03 §3.2's placement row for B1, §4.11.1; 01 FR-03.x; `cluster_single.yaml` if the socket route is taken |
| 6 | **FR-02.7's calibration mode cannot be satisfied by a fixed image.** The manifest carries one `run_commit` per sampled seed with a non-null `seed_sha`, as required; but a run has ONE image at ONE commit, and `probe_execute` runs and hashes that image's binaries. Measured: three calibration seeds whose manifest names `1728445e7e57`, `5481d1901ca0` and `87f131c0af7f` produced three `build_result` rows all recording `run_commit = eade0de61bc5`. The driver records the commit it ran, so manifest and build rows then disagree and FR-18.5's validation table would be taken at the wrong commits. Three ways out, all the architect's: one image per calibration seed; one campaign per seed; or amend FR-02.7 to claim detection AT THE RUN'S COMMIT of a bug whose fix is known. `T-S-calib-01` is **xfailed** until one is chosen | 01 FR-02.7, FR-18.5; 03 §2.4 `RunCommit`; 04 §3 `T-S-calib-01` |
| 7 | **The report's `Assisted-by:` trailer names a model that did not run.** `triage_task.render_report` reads it off `manifest.model_ids['triage_report']` in three places rather than taking it as a parameter, so a `--generator recorded` report whose first line is `NO MODEL TURN WAS MADE` still ends `Assisted-by: vertex:gemini-3.8-flash`. The `Report` record was left agreeing with the artefact rather than introducing a second disagreement | `circt_bug_loop/triage_task.py:render_report` (a parameter); 01 FR-11.6, FR-20.3 |
| 8 | **`test_system.py` is a fourth structural test module, and it states two tiers.** Its slugs name a TIER and not a source module, so it joins `STRUCTURAL_TESTS` beside `test_integration.py`. It carries ten `t0` tests of the `--generator recorded` mode and six `t3` tests of the tier the mode exists for, marked per test rather than per module, which §0.5's "a test states the LOWEST tier it can run at" permits and no module has needed before | 03 §1.3's mapping; 04 §0.5, §1.3, §3 |
| 9 | **The mutation arm cannot run at all, in any mode, until A7 freezes a set.** `mutators.load_set` refuses a set whose `frozen` field is false to any run naming a digest (§8.1 rule 1); the committed set is `set_dev.json` with `"frozen": false`; and `build_manifest` stamps `mutator_set_sha=mutators.set_sha256()` on every run. So A4 raises `MutatorSetError` for every seed and `drive_seed` ends each with `generator_failed:MutatorSetError`. This is the design working, not a defect — it is recorded because `--generator recorded` had to replace A4 for that reason and not because A4 reaches a model (it does not, FR-05.1) | none; 05's A7 row is the blocker, and 04 §16.8 item 8 already names it |
| 10 | **`ray.init(address="auto")` does not fail fast, so §0.5's "skip cleanly" was unbounded.** With no cluster it reads `/tmp/ray/ray_current_cluster`, which `chia down` leaves behind, and retries the GCS for minutes rather than raising; measured, a stale file from W-19a hung a T3 collection past a two-minute timeout. `test_system._gcs_answers` resolves the address Ray's own way and probes it with one bounded TCP connect first. Two smaller ones beside it: Ray 2.54 raises one `FutureWarning` of its own on every `ray.init` with `num_gpus` unset, which `-W error` turns into a false "skip" (suppressed at that one call; `RAY_ACCEL_ENV_VAR_OVERRIDE_ON_ZERO=0` deliberately NOT used, since it opts the cluster into the future behaviour rather than silencing the notice), and `conftest.no_local_ray` needs `BUGLOOP_CLUSTER=1` in the tier-3 shell | 04 §0.3, §0.4, §0.5 |
| 11 | **`chia up` needs `-y` from any non-interactive caller**, or the CLI raises `EOFError` at its `Proceed? [y/N]` prompt. W-19a ran it interactively and did not meet this | `analysis/measurements/2026-09-15-cluster-smoke.md` §1's shell; 03 §12.1's operator notes |
| 12 | **`repair_adapter.BUILD_JOBS` is 4, not 16, and `bugloop_repair --cpus` is 4 to match.** §9.4 took CHIA's sixteen verbatim on the strength of a `--cpus=8` that this cluster does not have. The bound that binds on a 15 GiB host is MEMORY: a `ninja -j16` link of the CIRCT targets peaks far past the container's 4 g, and an OOM-killed link fails the `fix` phase with a build error that is not the agent's diff's fault — the one failure mode `failing_phase` cannot tell apart from a real one. NOT measured: no repair chain ran, a repair attempt being a model turn | 03 §9.4; `T-U-repair-16`'s literal updated |
| 13 | **The store and the clone are head paths reached from nodes Ray may place anywhere.** `ledger.accrue`, `dedup_and_screen` and `render_results` take `db_path` or `clone_path` and are `@ChiaFunction(max_retries=0)` with NO resource, so Ray places them on any node with a free CPU. Inside a container `bug_loop.DB_PATH` (`FLOW_DIR / "loop.db"`) is the runtime env's unpacked package directory — a different, empty file per worker. Until finding 1 was fixed this could not bite, because nothing was dispatched. Either those nodes are pinned to the head (`_head_options()` already exists for exactly this) or `DB_PATH` moves under the artefact root, the one path FR-17.9 makes identical everywhere. A second reason for the move: `runtime_env()` ships the whole flow directory (2.53 MiB measured) excluding only `__pycache__` and `*.pyc`, so a `loop.db` there would be shipped to every worker on every `ray.init`. The tier-3 tests put their own store under the artefact root and assert nothing about the driver's | 03 §3.2's placement rows for A6b, B6b and B11; §6.1's `loop.db` path; 01 FR-17.9 |

## W-20b (2026-09-15): the fix round after the code red team and the system tier

Every kill and wound of `design/reviews/red-team-code.md`, the W-19b additions
to its disposition, and nits N2, N3, N5, N6, N8, N9, N10. Ten commits, one per
fix, each with its tests. Suite at the end: `pytest circt_bug_loop/tests -q
--strict-markers -W error -m "not t2 and not t3"` gives **620 passed, 1
skipped, 16 deselected**; the nine tier-2 image tests pass against the local
`chia-circt-assert:eade0de61bc5`.

| # | Finding | Owed to |
|---|---|---|
| 1 | **K2/W7 discharged.** `llm_turn` takes a turn REQUEST - system message, prompt, tool endpoints, stage, timeout, model id - and builds the client itself from the LLM worker's own environment through the new `llm.worker_env()`. No caller holds a backend and no Ray task argument carries a key. `llm.ToolEndpoint` is the four attributes CHIA's backend actually reads off a tool (`chia:chia/models/vertex.py:291-295, 426-428`). `cluster_single.yaml` is unchanged and `T-U-cluster-10` now asserts the key is on `bugloop_llm` and `bugloop_repair` and nowhere else | 03 §3.5.1's turn signature; 04 §1.6 (the substitute is one function, not two) |
| 2 | **K4/K5 discharged.** `HEAD_NODES` names the twelve unresourced nodes and `Dispatch.call` pins each with `NodeAffinitySchedulingStrategy(soft=False)`. `_head_options()` becomes `head_options(node_id)`, pure, and `Campaign.cfg()` is the one place the four `generator_cfg` call sites take the placement from. `generator_cfg` gains `head_options`, `here_options` and `mutator_set_path`, which makes its docstring's "sixteen keys" true | 03 §3.2's placement table and §13.1's `Dispatch` paragraph are load-bearing and should say so; 04 §1.1 (`T-U-layout-11`), §3 (`T-S-place-01`) |
| 3 | **K1 discharged, in four parts.** `build_image` is a HEAD node - the CIRCT worker type is a container of the image it would build and mounts no docker socket. The tag is the Dockerfile's own `chia-circt-assert:<CIRCT_SHA[:12]>`; the six-key manifest's digest is `manifest_digest()`. `dockerfile_and_context()` finds the file in either tree and refuses by name in neither. `docker push` and `DEFAULT_REGISTRY` are gone (W-19a measured the ghcr push `denied`). `--image-tag` compares the CIRCT SHA prefix, which is what `${BUGLOOP_IMAGE_TAG}` holds, and `--dry-run` passes `inspect_only=True`. **Verified against the local image**: reuse hits, all six tool hashes equal `raw/image-manifest.json`, `image_digest` `sha256:8ac3cb31...`, `manifest_digest` `a9e9080a...`, lit 1400, 19 non-referencing objects | 03 §3.2's B1 row (head, not `{"circt": 1}`), §4.11.1 (no step 8), §13.1's `--image-tag`; 01 FR-03.x's tag scheme |
| 4 | **`ImageSpec.manifest_digest` is NOT a column of `image`.** §6.2's DDL is compared to `03-LLD.md` §6.2 for equality by `T-U-store-01`, and this round may not edit `design/0*.md`, so the digest is a field of the record and is written durably as `<artefact_dir>/image_manifest.json` beside the lit log. **Owed**: the column, and `RunManifest.image_spec`'s key set, which §2.7 closes at eight | 03 §6.2, §2.7; `store.py`'s `image` DDL |
| 5 | **K3/K6/K7 and W6 discharged.** The head calls `repair_adapter.build_cfg` and passes the cfg complete; `repair_adapt` filters CHIA's sixteen out of it and names every missing key. `stage_shipped()` builds `circt_bug_loop/_shipped/` at start-up - the flow package without `tests/` or `loop.db`, a copy of `chia` with `__init__.py` added and `vertex-usage.patch` applied, `issue_task.py` with the vertex branch applied, `circt_util.py` - and `runtime_env` ships those four and nothing else. New pre-flight checks 13 and 14 grep the staged copies. `sync-to-chia.sh` applies both patches | 03 §3.8 (`repair_adapt`'s new `chia_artifact_dir`, the cfg's two loop keys), §13.1's `py_modules`; 04 §1's repair rows |
| 6 | **`git apply` inside a work tree SKIPS silently.** The staging directory is inside this repository, and git in a work tree resolves a patch's paths against the REPOSITORY ROOT and ignores every path outside the current directory: measured, `git apply -p1` printed `Skipped patch 'chia/models/vertex.py'.` and exited **0**, so the copy went out unpatched with no error anywhere. `_apply_patch` sets `GIT_CEILING_DIRECTORIES` to the staging directory's parent, which makes git find no repository and resolve against the cwd, and behaves identically under a `tmp_path` that is in no repository at all | none; recorded because the failure mode is silent and the fix is not obvious |
| 7 | **K9 discharged, and its rule refined.** `_limit_hit` returned `address_space` for ANY stderr containing one of FR-06.7's three literals. `circt_core.allocation_evidence` now requires a literal to BEGIN a line, as a failing allocator prints it; beside it, peak RSS reaching the `prlimit --as` value is the rusage arm. K9's disposition names "died by signal AND a literal" as the first arm and **that is not implementable as written**: the measured `RLIMIT_AS` case exits 1 with `MemoryError` and NO signal (`T-U-core-15`), so requiring a signal loses the only case the address-space limit produces, and requiring it does not separate the cases K9 is about. The line rule does, signal or not | 01 FR-06.7's "as a substring"; 03 §3.6's `oom` rows |
| 8 | **N9 discharged at contract 2.1.** Exit 127 is `tool_unavailable:loader_failed`, a new `BuildStatus`: measured, `circt-opt` under `--as=64MiB` exits 127 with `failed to map segment from shared object`, and `circt_core`'s own `execvp` failure path exits 127 too. It is not a firing status and the differential is not asked of it. `CONTRACT_VERSION` is `2.1`, every fixture's version string moved with it and no other fixture byte changed. **Owed**: the `contract-2.1` annotated tag, which this round cannot cut (it pushes nothing); `01-FRD.md` FR-06.9's eighth status and `03-LLD.md` §2 | `contract/FROZEN.md`'s new section carries the whole record; 01 FR-06.9, 03 §2.1, §3.6 |
| 9 | **K10/K11 discharged.** `llm.turn_usage` folds `thinking_tokens` into the OUTPUT count and `tool_use_prompt_tokens` into the INPUT count, so `ledger.price`'s two rates need no third; an empty `_last_metadata` yields NULL counts and a null cost, never a zero. The results artefact prints `Unpriced turns: N` beside the USD total, counted over metered model-stage entries with null tokens | 03 §3.11's pricing paragraph; 01 FR-14.6; 04 §1.19 |
| 10 | **W1 discharged, with a residual.** `llm.SpendGuard` pre-authorises every turn in `dispatch_turn`, which is the one function every turn passes through: worst case `(prompt_chars/3 + 16000)` at the two rates, refused when spend plus already-authorised plus worst case reaches the cap, and the arm stops on the refusal. **The formula bounds ONE `generate_content` call**; a turn with tools runs a loop of up to `max_tool_iterations` = 100 of them (`chia:chia/models/vertex.py:227`), so for such a turn the authorised figure is a FLOOR. The overshoot is now one turn's and no longer one seed's, and the observed spend of a turn that overran stops the arm at the next authorisation | 01 FR-18.10; 03 §3.11; the residual is the architect's to price |
| 11 | **W10 discharged.** The `LedgerSnapshot` is rebuilt per iteration from the same `ledger.aggregate` the spend guard is built from | 03 §3.11 |
| 12 | **W2 discharged.** `tool_probe` hashes the six binaries and runs `verilator --version` ON the worker; `tool_probes` dispatches it once per image-bearing worker type, which `cluster_summary` reads off the YAML's own `docker.image` rather than guessing from a resource name. A binary a worker cannot read leaves no entry, so check 7 refuses on a missing tool | 03 §13.1's checks 7 and 8; 02 §12.1 (which types run the image) |
| 13 | **W4 discharged.** `--generator recorded` skips check 12 entirely | 03 §13.1's check 12 |
| 14 | **New check 15**, W-19b #3's own: the entrypoint imports `circt_bug_loop` in a subprocess with the wrapper's `PYTHONPATH`. `PREFLIGHT_CHECKS` is fifteen | 03 §13.1's check list (twelve), §11.2 |
| 15 | **W3 discharged.** `_close_probe` copies `stopping_stage` and `stopping_reason` onto the record. Two consumers move with it: `results` regenerates against `build_result.status`, which is stage 3's own row, and keys the two `parse_error:` rows only on a stage-3 reason | 03 §14.4's regeneration check; 01 FR-16 |
| 16 | **W5 discharged.** An undecidable FR-13.15 second conjunct sets `q3_valid = None`, which `decide` already turns into question 3 and the `undecided` bucket | 03 §3.9; 01 FR-13.10, FR-13.15 |
| 17 | **W11 discharged.** Both filing caps are per campaign RUN, in `ledger.aggregate` and in `approve.refusal`, by joining `filing` to `candidate`. `BudgetLedger.filings_lifetime_total` reports the lifetime figure and is compared against nothing | 01 FR-13.8 (which does not settle it); 03 §3.11 |
| 18 | **W-19b #2 discharged.** `mutators.mutant_seed_int` is masked with `SEED_INT_MASK`. The contract fixtures are re-recorded; `make_recorded.py` gained W-19b's `--generator` flag and K2's turn signature, and the run also corrected a stale `cluster_yaml_sha` in the recorded manifest | 03 §8.2 rule 5 |
| 19 | **W-19b #4 discharged.** FR-10.2's labelled set is `circt_bug_loop/data/labelled_pairs.json` with a README; `results.load_labelled_pairs()` reads it and the tests read the same file. The driver passes it and catches `ResultsIncomplete`, writing `results/results_refused.txt` and exiting 3 after the ledger and store are complete. **The refusal still fires**: the twenty-two pairs are fingerprinted from W-09's six recorded failures, so no campaign's store holds fingerprints for them, and the artefact says exactly that by name. Closing it needs a set labelled over a pilot's own confirmed candidates, which §10 asks for and no run has produced | 01 FR-10.2, FR-18.7; 03 §14.4; 04 §10 |
| 20 | **W-19b #7 discharged.** `triage_task.assisted_by` follows `stages_metered["stage_6"]`, which `--generator recorded` sets false along with stages 1, 2 and 7 | 01 FR-11.6, FR-20.3; 03 §7.4.1 |
| 21 | **W-19b #6: the DECISION procedure is landed and the BUILD is not.** `bug_loop.calibratable(seeds, pin_sha)` makes a seed eligible exactly when its parent's `llvm` gitlink is the run's pin; the rest are `not_calibratable_in_deployment`, excluded from the sample and from `run_commit` and recorded in `seed.exclusion_reason`. `--draw-calibration` draws from the eligible set; a calibration run whose registered sample is entirely ineligible is refused at pre-flight by name. **MEASURED, 2026-09-15**: `git ls-tree <parent> llvm` over all 187 corpus seeds gives **0** carrying the image's pin `6279700538792da0c5a08e17babfe9b6e824c69f`, across **44** distinct pins, the commonest covering 27 seeds; the image commit `eade0de6`'s own gitlink IS that pin, so the comparison is the right one. The in-container checkout, incremental build, probe and restore the disposition also names is therefore **NOT written**: it has zero cases on this corpus and this image and could not be exercised even on the cluster. `T-S-calib-01` stops being an xfail and skips with the count | 01 FR-02.7 and FR-18.5 need the amendment the measurement forces; 05's calibration row; the build path is owed and is the architect's to schedule |
| 22 | **W8 discharged.** `budget.yaml`'s pricing header records the 2026-09-15 verification against `ai.google.dev/gemini-api/docs/pricing`, names the 2027-01-01 doubling and states that thinking tokens are billed as output. The registration artefact and ADR-D-03 now agree. The edit changes the file's blob hash, so the recorded `BudgetFile` fixture is re-recorded under its new name | 01 §... FR-14.2's provenance sentence; 03 §9.5 |
| 23 | **N2, N3, N5, N6, N8, N10 discharged.** `PROBE_WALL_MARGIN_SECONDS` is gone (exported, read by nothing). One canonical-JSON writer (`schema.canonical_json`), one file hasher (`store.sha256_file`, with `probe_task._sha256`'s empty-string POLICY on top of it) and one timestamp (`store.utc_now`). `ProbeWriteTool.write_probe` catches `ValueError` as well as `OSError`, a NUL in a filename being the case. `store.write_artefact` resolves with `realpath` and not `normpath`, so a symlink already inside an artefact directory is not followed. `_STAGE_OF` gains `gate_rerun` and `gate_validate`, both at stage `gate`, so their `{"circt": 1}` occupancy is charged rather than folded into the head node's wall clock. `corpus._TOOLS`'s `circt-capi-.*` is `circt-capi-[\w-]*` | 03 §9.4 (`PROBE_WALL_MARGIN_SECONDS` is not a constant of this module), §3.11's stage table (the gate's two nodes), §2.3 (one canonical writer) |
| 24 | **Still owed before the pilot, and NOT this round's.** K8 and W-19b #9: `mutators/set_v1.json` does not exist, so the mutation arm cannot run in any registered campaign until A7 synthesises and freezes a set - which is K8's own sequencing decision, the first live call. W12: `--repair-backend claude` still reaches a model with `need_key=False` and no warning banner was added. The project-level GCP spend cap, which is the single question of the red team's own last section and which nothing in this repository can check | 05's A7 row; the disposition's W12 row; the operator's billing console |

### W-20b's own cluster run (2026-09-15): one defect the tier-3 tier found

| # | Finding | Owed to |
|---|---|---|
| 25 | **`runtime_env()` reused a stale `_shipped/` and the workers ran an earlier commit's code.** It staged only `if not SHIPPED_DIR.is_dir()`, so a directory left by an earlier `ray.init` in the same session was shipped unchanged. MEASURED on the cluster: every dispatched node failed with `AttributeError: Can't get attribute 'SpendGuard' on <module 'circt_bug_loop.llm' from .../_ray_pkg_baf17209a8332bda/circt_bug_loop/llm.py>` while the head's own copy had it, and the only symptom at the driver was `T-S-disc-01` asserting "the recorded generator wrote no probing input at all". It is W6's defect in a second place - what a worker runs is not decided by this repository - and it would have been invisible to every tier below 3. **FIXED**: `runtime_env(shipped=None)` stages every time unless the caller hands it the result, and `run_campaign` hands it the one it made for checks 13 and 14 | fixed in `bug_loop.py`; 03 §13.1 owes the sentence that the staged package is rebuilt at every `ray.init` |

**The tier-3 run.** `chia up -y` **42 s**; `pytest circt_bug_loop/tests/test_system.py -q --strict-markers -W error`: **16 passed, 1 skipped, 44 s**, of which the six tier-3 rows are the table below; `chia down -y` **178 s**, teardown clean (0 `circt_bug_loop*` containers, 0 Ray processes, 6379 and 8265 free). No model call was made: `BUGLOOP_ALLOW_LIVE_MODEL` was never set in any shell or container and `GEMINI_API_KEY` was the literal `placeholder-not-a-key`.

| Test | Outcome | Evidence |
|---|---|---|
| `T-S-place-01` (new, K4/K5/W9) | **pass** | head `f0a85e81aa15`; three `{"circt": 1}` dispatches landed on `63bc439ce50b` and `47ac6e06c831`, both off-head, each with `{'circt': 1.0, 'CPU': 1.0}`; a `HEAD_NODES`-shaped node pinned with `soft=False` landed on the head, and `Dispatch(remote=True).head_options()` resolved the same id |
| `T-S-disc-01` (a) | **pass** | 16 probes (8 seeds x 2 arms) in 11.7 s; both arms `seed_set_exhausted`; `violations == []`, so every node returned its own `CounterBlock` and none was synthesised; both arms reached stage 5 and the gate this time, not only stage 3 |
| `T-S-calib-01` (b) | **skip, with the count** | `0 of 3 seeds are calibratable at the image's pin; 3 are not_calibratable_in_deployment; the seeds carry 2 distinct pins`. XFAIL until this round |
| (c) the equal-window stop | **pass** | two 1.0 s windows: seeded **1.0750 s**, mutation **1.0560 s**, both `arm_window`, 4 seeds and 4 probes each |
| `T-S-isolate-01` (d) | **pass** | all six tool binaries on both `bugloop_circt` containers hash to `ImageSpec.tool_hashes` before the run and after it |
| `T-S-regen-01` (e) | **pass** | the regeneration check ran over 2 rows of 2 candidates and regenerated both; `render_results` refuses with **exactly one** complaint, FR-10.2's labelled set, which is now the driver's caught refusal and not a traceback |
| `T-S-submit-01` (f) | **pass** | `bug_loop_submit.sh --mode discovery --generator recorded --print-config` submitted, the job succeeded, `chia job logs` returned the driver's own JSON, and neither credential appears in the job's status or logs |

**One thing the run showed that is not the loop's.** A third `-s -m t3` pass, run while the host was at **14.50 GB of 15.23 GB** (a browser holding 4.01 GB), was killed by Ray's own memory monitor at its 0.95 threshold - `ray.exceptions.OutOfMemoryError` against `SQLiteNode.init_schema`. The same tests passed in the runs either side of it. It is recorded because the pilot's own margin is the same margin: W-19b measured a worker's resident cost at about 350 MiB and the host's headroom is what is left after everything else on the machine.

### W-12's dry pass (2026-09-16): the two rules that stop A7, and two defects it found

Recorded before any live model call was made; the whole of the round below cost
USD 0.00. Full record: `analysis/measurements/2026-09-16-mutator-synthesis.md`.

| # | Finding | Owed to |
|---|---|---|
| 26 | **A7 cannot run: the campaign is already registered, and a set frozen now cannot start one.** Two committed rules, both firing. §8.3 step 1 / §8.1 rule 3: `mutator_synth.registration_commit` returns `1a26fc8`, so `synthesise_mutators` raises `already_registered` **before any turn**. §8.1 rule 2: `budget._check_frozen_set` requires the set's last commit to PREDATE the budget file's, and `budget.yaml` was last committed 2026-09-14T16:02:08+05:30, so a `set_v1.json` committed now makes `load_budget` refuse **every** campaign. Disposition row K8 says the synthesis is "frozen before pre-registration", but `budget.yaml` landed at `6e0b110` (W-06) and FR-14.3 makes that commit the registration, while 05's plan still carries W-22 "pre-registration" as future work: the code and the plan disagree about when registration happened. **NOT FIXED, and not fixable inside W-12** - the three ways out (re-land `budget.yaml` after the set; key step 1's refusal to a pre-registration TAG as `contract-2.x` already are; or declare the mutation arm unable to run a registered campaign) are all architect ordering decisions, and B2's resolution decides whether the frozen bytes are usable at all, so paying for a write-once freeze before it is taken risks paying twice | the architect; 01 FR-14.3, FR-05.2; 03 §8.1 rules 2 and 3, §8.3 step 1; 05's A7 and W-22 rows |
| 27 | **The head's `chia` is UNPATCHED, so A7's own turn would not have counted its thinking tokens.** `build_llm` imports `chia.models.vertex`, which on the head resolves to `~/.cache/chia-src/chia/models/vertex.py` and contains none of K11's fields; the patched copy exists only in `circt_bug_loop/_shipped/chia/`, which `runtime_env` ships **to the cluster**. A7 runs in-process **on the head** (§8.3), so `turn_usage` would have returned `thinking_tokens: 0` and priced the one turn whose whole purpose is to be measured without them - K11's defect, on K11's own first live call. Pre-flight check 14 proves the patch is in the file a WORKER imports and nothing checks the head. **Not a code fix; a run rule**: the live pass runs with `PYTHONPATH=circt_bug_loop/_shipped`, verified to resolve to the patched file and to report `thinking_tokens` and `observed: true` | 03 §13.1's check 14 owes the head; the W-12 and W-18 run procedures |
| 28 | **A7 never wrote §8.1's `frozen` field. FIXED** (`84e0723`). `synthesise_mutators` wrote `format_version`, `set_version`, the three provenance fields and the mutators array and not `frozen`, which §8.1's format table lists and which is the whole of what distinguishes a frozen set from `set_dev.json`; `load_set` refuses a set whose `frozen` **is False**, so a set with no such field passed by accident rather than by being frozen. It matters because the freeze is WRITE-ONCE: a set frozen without it could only be corrected by bumping `set_version` and paying for the model turn a second time. Asserted by `T-U-msyn-05` | fixed in `mutator_synth.py`; errata row 7's "a field §8.1's format does not have" is now a field A7 sets |
| 29 | **`render_results` refused FR-10.2's rates on every real run. FIXED** (`51b68ec`), closing round 4's row 4 and the one refusal `T-S-regen-01` recorded. The labelled duplicate-pair set is an EXTERNAL measurement: its sides are recorded failures labelled by hand before any fingerprint was computed and belong to no campaign, so requiring the store to hold a fingerprint per side made the rates unreportable by exactly the campaigns that most need them. `triage_task.labelled_fingerprint` now fingerprints a side through the SAME `compute_fingerprint` the campaign uses, `load_labelled_pairs` hands over the recorded failures rather than two candidate ids, and `render_results` takes G-43's registered `fingerprint_top_n` from the driver. The refusal for a set that is not supplied at all is KEPT. The fixture store carries the committed set, so `fixtures/results/example.md` prints the project's real rates, collision 1/11 and false-merge 1/11 - which `T-U-triage-09` measures independently over the same 22 pairs by a different path | fixed in `results.py`, `triage_task.py`, `bug_loop.py`; 03 §14.4 and 04 §1.19 owe the sentence that the rates are measured from the file |

**§8.3 specifies ONE turn and not batches.** Disposition row K8's parenthesis
says "one synthesis turn per batch of issues"; §8.3 fixes a single turn over
`$issue_count` issues, `mutator_synth.py` implements that, and no batch size or
splitting rule is written anywhere. None is needed: the whole 487-issue corpus
renders to **1,509,080 prompt characters**, whose pre-authorised worst case at
`budget.yaml`'s two verified prices is **USD 0.43727**, a factor of eleven under
W-12's USD 5 cap. K8's parenthesis is the erratum, not §8.3.

**The corpus.** The mirror was refreshed into a store of W-12's own at the
committed cap 12,000: 2,652 issues, 198 pages, 212.4 s, two-direction walk,
`ceiling_hit` true and the cap never binding. Of them **487 are closed and
labelled `bug`** - M9's measured count, independently re-derived from a mirror
refreshed today, so ADR-D-05's fallback to the 24-month fix-commit set is not
taken and §8.3 step 2's figure needs no erratum.

### W-12b (2026-09-14): the pre-registration becomes a tag, and the first live call

The architect's binding decision on rows 26's B1 and B2, then the turn itself.
Full record: `analysis/measurements/2026-09-16-mutator-synthesis.md` §9.

| # | Finding | Owed to |
|---|---|---|
| 30 | **The pre-registration is an annotated `registration/<campaign-id>` TAG, not the first commit that ever landed `budget.yaml`. Row 26's B1 and B2 are CLOSED.** `budget.registration(repo_root)` resolves the newest such tag and dereferences it to a commit, `("", "")` meaning "not registered"; `mutator_synth.registration_commit` **is** that resolver and no longer a `git log -1 -- budget.yaml` of its own, so the rule A7 refuses on and the rule the campaign is checked against are one implementation (N3) and the two-pathspec problem of §1.4's two trees disappears with it. **Erratum against FR-14.3's second clause** ("that SHA is the pre-registration"): the SHA is still what the `RunManifest` records and is still the run's identity, and it is the TAG that registers. **Erratum against §8.3 step 1 and §8.1 rule 3**: "once a pre-registration commit exists" is "once the tag exists", which is what makes A7 - specified to run BEFORE the registration - runnable at all; a REGISTERED campaign still cannot synthesise, so the rule's purpose is untouched. **Erratum against §9.2 check 1**: the check keeps `git log -1 --format=%H%x09%cI -- budget.yaml` for FR-14.2's "committed, and earlier", and gains the tag; `load_budget` takes `campaign: bool` and only a campaign is refused in an unregistered repository, `--dry-run`, `--generator recorded` and `--draw-calibration` being the readers that necessarily precede the tag. `_CHECKS` stays **six** | 01 FR-14.3, FR-14.7; 03 §8.1 rules 2 and 3, §8.3 step 1, §9.2 check 1; 05's W-22 row, which now has a tag to place |
| 31 | **"Frozen before the pre-registration" is ANCESTRY, not two commit dates. Erratum against §8.1 rule 2 and FR-05.2's acceptance.** `budget._check_frozen_set` and the driver's check 2 ask `git merge-base --is-ancestor <set commit> <tag commit>`, and FR-14.7's "edited after registration" asks the same of the budget file's own commit. `%cI` is metadata a rebase rewrites and a skewed clock orders either way; reachability from the tag is a property of the history the tag actually names. `check_02_mutator_set_earlier` no longer takes a `BudgetFile`: the registration SHA it used to read off one is the tag's | 01 FR-05.2's acceptance sentence; 03 §8.1 rule 2's two `%cI` reads |
| 32 | **A `registration` table, and why it is not a column of `run`.** The decision asks the run to record WHICH pre-registration it was checked against. `RunManifest` has no open `budget` block, and the fallback - a column on `run` - is not available either: `T-U-store-01` compares `store._DDL_TABLES` to §6.2 for **textual** equality and W-12b may not edit a design document. The table is therefore its own statement, `store._DDL_REGISTRATION`, appended to the schema, keyed by `run_manifest_id` and carrying the tag and its commit; §6.2's text is still exactly what `_DDL_TABLES` holds. `T-U-store-01`'s table count is 22 and `T-U-store-06`'s traced count is 20. **The contract did not move**: `budget_file_sha` is unchanged and is still the run's identity | 03 §6.2 owes the table, or the column, whichever the architect prefers; 02 §... nothing |
| 33 | **The first live call: eleven of twenty-two mutators dropped, ten of them to one unstated sentence.** §8.3's prompt never says which regular-expression engine compiles `pattern`. The model answered with ten variable-width look-behinds - `(?<=depth\s*=>\s*)` and nine like it - which PCRE accepts and Python's `re` refuses with `look-behind requires fixed-width pattern`, so step 4's `bad_pattern` check dropped all ten; an eleventh was dropped for an empty `replacement`. `set_v1.json` therefore carries **11** mutators, all of kind `text`, `mlir` 5, `fir` 5, `sv` 1 and `any` 0, so the campaign's own set exercises neither FR-05.5's `argv` path nor §8.2's line operations. **The prompt was NOT edited**: the freeze is write-once and the repository must hold the text that produced the committed bytes, so adding the sentence is a `set_v2` decision with a second turn attached, priced at about USD 0.49 | 03 §8.3's prompt owes one sentence naming Python's `re`; the architect owes the v1-or-v2 call; 01 FR-05.5 is unexercised by the frozen set |
| 34 | **W1's pre-authorised "worst case" is not one, MEASURED.** `llm.SpendGuard.worst_case_usd` estimates input tokens at `len(prompt) / CHARS_PER_TOKEN` with `CHARS_PER_TOKEN = 3`. The live turn's 1,509,080-character prompt billed **619,603** input tokens - **2.436 characters per token**, so the estimate was **18.8 % low** and the authorised USD 0.43727 was **11.3 % below** the USD 0.486733 the turn actually cost. The docstring admits the direction and nothing had measured the size. It matters because this is the control that stops the campaign at `campaign_spend_cap_usd`: at this ratio the guard lets through about a ninth more than it authorises, per turn, and `authorised_usd` accumulates the under-estimate across a whole arm. **NOT FIXED** - the constant is `03-LLD.md` §3.5.1's and the safe value is a measurement over the campaign's own prompt shapes, not this one | 03 §3.5.1 and W1's formula; 01 FR-18.10's third stop condition |

**The turn.** One dispatch, 128.3 s, `PYTHONPATH=circt_bug_loop/_shipped` so K11's
patch was in the file **this** process imported (row 27's run rule, followed).
Ledger and SDK agree exactly: 619,603 in, 5,875 out of which **2,975 thinking**,
SDK `total_token_count` **625,478 = 619,603 + 2,900 + 2,975**, **USD 0.486733**
at `budget.yaml`'s two verified prices. Priced the way unpatched CHIA counts the
same turn costs USD 0.475577, so **K11 is worth USD 0.011156, 2.3 %, on one
turn** - the first measurement of the defect it exists to fix.

**Row 24's standing item is discharged in part.** `mutators/set_v1.json` exists,
`sha256 180ca0ebf9c38c3bfd2635f7f778bbdb0c5a6b550f8b615432ab8705cd541e55`,
committed before any registration tag, so the mutation arm can run a registered
campaign. What it will run is eleven `text` mutators of which five are eligible
for an MLIR seed: over the five recorded corpus seeds it produces **8 mutants at
the registered `per_seed_probe_cap` of 5** and 43 no-ops, with 0 failures and
every derived seed inside 63 bits. The five fixtures are all MLIR and are not the
187-seed corpus, so that is evidence and not a measurement of the arm.

**One thing that went wrong and cost nothing.** The first live attempt sent no
request at all: this task's own `genai.Client` recorder dropped the real client's
last reference (`RealClient(**kwargs).models`), CPython collected it and closed
the transport under the surviving `models` object - six
`Cannot send a request, as the client has been closed`, two empty transcripts,
**USD 0.00**. The loop behaved correctly throughout: the turn was reported
unsuccessful, `turn_usage` returned NULL counts and not zeros (K10), and
`ledger.price` returned `None` and not a price.

### W-12c (2026-09-16): the guard's constant, and the second synthesis

Row 34's constant fixed, and the prompt row 33 said owed one sentence rewritten
as a versioned second file. Full record:
`analysis/measurements/2026-09-16-mutator-synthesis.md` §10.

| # | Finding | Owed to |
|---|---|---|
| 35 | **`llm.CHARS_PER_TOKEN` is 2.0, and row 34 is CLOSED.** The constant was 3 and the one turn ever measured billed 619,603 input tokens for 1,509,080 prompt characters, **2.436 characters per token**, so the pre-authorisation under-charged by 11.3 % on the control that stops the campaign at `campaign_spend_cap_usd`. 2.0 is that measurement rounded DOWN to a safe value, not a second measurement: it over-estimates the same prompt's tokens by 22 %, which is the direction a cap must err in, and leaves headroom for a prompt shape tokenising worse than the synthesis's. It is not free - every turn is now authorised for more than it will cost, so the guard refuses slightly earlier than the money requires, and `authorised_usd` accumulates that slack across an arm. `T-U-gen-28` asserts the constant, the arithmetic at the new ratio, and the inequality against the measured pair, so a future edit back towards 3 fails on the measurement rather than on the literal | 03 §3.5.1's constant; 01 FR-18.10's third stop condition. Closes row 34 |
| 36 | **The §8.3 prompt is VERSIONED with the set it freezes, and row 33 is CLOSED.** `mutator_synth.md` is not edited - it is the record of the bytes `set_v1.json` holds and the freeze is write-once - and `mutator_synth_v2.md` sits beside it, selected by `mutator_synth.prompt_path(set_version)`, so the pairing is a fact about two file names rather than a flag a caller passes. v2 adds four things to §8.3's contract and nothing else: the engine by name ("Python 3.10's `re`") with the refused construct as the worked example and the rest of the PCRE-only syntax listed; `replacement` never empty, with what it may be per kind; at least two `argv` and two `line` mutators across all four languages; and the id regex spelled out. **Measured**: 25 returned, **25 kept, 0 dropped**, against v1's 22 in and 11 out, with `text` 18 / `line` 3 / `argv` 4 and `mlir` 6 / `fir` 14 / `sv` 3 / `any` 2, so FR-05.5's argv path and §8.2's line operations are exercised by the campaign's own set for the first time. `mutators.SET_PATH` resolves to the newest `set_v<n>.json` and `budget.MUTATOR_SET` reads that same resolution, so 8.1 rule 2 checks the ancestry of the file the manifest's digest is computed over; there is no `--mutator-set` flag, `load_set(path)` already taking any path. Three of the four `argv` mutators never fire over 25 seeds, and one of them - `any.argv.disable_threading`, `^--verify-diagnostics$` - **cannot fire in the arm at all**, `seed_argv_template` stripping that option before `mutate_seed` sees the vector | 03 §8.3's prompt and §8.1's set resolution; 05 owes a "which prompt froze which set" line. Closes row 33 |
| 37 | **`VertexGeminiLLM._get_node_id` hangs for ever on every error path when `/tmp/ray/ray_current_cluster` is stale, and its `try/except` cannot catch it.** The first live attempt of W-12c returned a complete answer, was billed for it, and then sat twelve minutes inside `ray.get_runtime_context()` until it was killed; the turn's token counts died with the process and its cost is a BOUND (USD 0.466 to 0.526) and not a measurement. Reproduced on its own: with that file naming a cluster that is down, `ray.get_runtime_context()` retries the GCS five seconds at a time for ever (`Failed to connect to GCS at address …:6379 within 5 seconds`, then `Failed to get cluster ID from GCS server: TimedOut`). The file is what a `chia down` leaves behind, which is W-19b §3.4's finding in a second place. Four call sites reach `_get_node_id`, ALL of them on CHIA's error paths - a blocked prompt, a blocked response, `MAX_TOKENS`, and every translated `APIError` - so a campaign that meets a 429 or a 500 on a head with a stale file hangs instead of retrying. **Not fixed in CHIA**: the run rule is that the file must name a live cluster or not exist, and the campaign's own driver calls `ray.init(address="auto")` before any turn, which makes it live. The harness fix is this task's: token counts are now persisted the moment a call returns, so a later hang cannot take the money with it | CHIA's `chia/models/vertex.py:_get_node_id` (a bounded `ray.get_runtime_context()` or a `node_id` passed in); 03 §13.1 owes a pre-flight read of `/tmp/ray/ray_current_cluster`; the W-12 and W-18 run procedures |

### W-18 (2026-09-16): the pilot, and seven pre-flight defects nothing had met

The first run of `run_campaign`'s pre-flight on a cluster. Full record:
`analysis/measurements/2026-09-16-pilot.md`.

| # | Finding | Owed to |
|---|---|---|
| 38 | **W1's pre-authorisation is 19.7x to 64.1x below what a tool-bearing turn costs, MEASURED, and the pilot went 43.3 % past its registered USD cap.** `SpendGuard.worst_case_usd` bounds ONE `generate_content` call; CHIA's vertex backend runs a loop of up to `max_tool_iterations` = 100 of them for a turn with tools, and every call re-sends the whole conversation, so input tokens grow roughly quadratically in the tool-call count. The pilot's three billed turns made **14, 21 and 37** calls against prompts of 6,087, 3,350 and 4,687 characters and cost **USD 1.229950, 1.977202 and 3.956805** against authorisations of **0.0623, 0.0613 and 0.0618**. Ledger total **USD 7.163957** against `campaign_spend_cap_usd` **5.00**. `SpendGuard`'s docstring already said the figure is "a FLOOR and not the true worst case" for such a turn and that the overshoot is "bounded by one turn"; one turn is 64x the bound. **NOT FIXED** - the three ways out (multiply the authorisation by `max_tool_iterations`; authorise per `generate_content` call inside `llm_turn` rather than per turn in `dispatch_turn`; or register a cap sized to the measured USD 1.2-4.0 a turn) are a registered-parameter decision and W-23 cannot start without one. What DID work: the next authorisation refused, `turn_usage` returned NULL counts for the refused turns (K10), `ledger.price` priced them at nothing, the arm stopped at `campaign_spend_cap`, the mutation arm never started, and the run wrote its own `ended_utc` before the operator's `ray job stop` reached it | 03 §3.5.1's `SpendGuard`; 01 FR-18.10's third stop condition; 09's `campaign_spend_cap_usd` for W-22 |
| 39 | **B1 lost its head affinity on every SUBMITTED run. FIXED.** `node_key` reads `__module__`, and `python <flow dir>/bug_loop.py` - the entrypoint `bug_loop_submit.sh` builds - makes the driver's own file `__main__`, so `build_image`'s key was `"__main__.build_image"`, matched no `HEAD_NODES` entry, and Ray placed an unresourced task on any node with a free CPU: a worker container with no Docker daemon, `FileNotFoundError: 'docker'`, `max_retries=0`. `T-U-layout-11` imports the module and sees the dotted name, so no test could meet it. `node_key` now substitutes `circt_bug_loop.bug_loop` for `__main__` | 03 §13.1's `HEAD_NODES`; `T-U-layout-11` extended |
| 40 | **Check 12 required the credential of a machine that needs none, and made the submit wrapper unable to start any live campaign. FIXED.** §11.2 forbids forwarding `GEMINI_API_KEY` and `BUGLOOP_ALLOW_LIVE_MODEL` through `--runtime-env-json`, the values being stored in the job's metadata and shown by `chia job` and the dashboard; check 12 then refused every `--generator model` submission at `head: BUGLOOP_ALLOW_LIVE_MODEL is not exactly '1'`. The head builds no client - `llm_turn` builds it on the `llm` worker from that worker's own environment (K2, W7) - so the head is now checked only where a turn would be built there, which is a driver with no Ray. The interlock's deliberate act is unchanged and still the operator's, the `-e` lines expanding from the operator's shell at `chia up`. An empty worker map is now a refusal rather than a vacuous pass | 03 §13.1's check 12 and §11.2; `T-U-driver-27` extended |
| 41 | **The mined corpus was EMPTY on every campaign. FIXED.** The driver passed `budget.campaign_start_utc[:10]` as `build_corpus`'s `since`. That is `2026-09-20`, the registered corpus head `d7e94049` is dated `2026-09-11`, and `commit["date"] < since_dt` then drops every commit, so both arms would have run their windows over an empty seed list and reported `seed_set_exhausted` with no probe. `test_corpus.py` passes its own `SINCE = "2024-09-11"` and the tier-3 tests mine nothing, so nothing had ever mined through the driver. `bug_loop.corpus_since` derives FR-01.1's window from the corpus head's own commit date, two years back - exactly the constant the corpus tests use - so it stays reproducible from the registered file plus the clone | 03 §13.1's B12 row and §3.3's `since`; 01 FR-01.1; `T-U-driver-45` |
| 42 | **A zero filing cap stopped the arm before its first probe. FIXED.** `ledger.stop_reason` read `0 >= 0` as a binding safety cap, so a run registered with `filings_total` and `filings_per_day` at zero - which is how a run is registered as unable to file at all, and is the pilot's own registration - ended its first arm in 18 ms with `stop_reason="filings_total"`, wrote no probe and reached no model. A cap of zero is now exempt and a positive cap stops the arm exactly as before; `approve.py` enforces both caps independently at the one place a report is filed and refuses every filing at zero, so a zero-cap run still cannot file | 01 FR-18.10's safety caps; 03 §3.11; `T-U-ledger` extended |
| 43 | **`bugloop_llm` never mounted the artefact root, and check 5 probes every worker type. FIXED.** FR-17.9's one root was bind-mounted on `bugloop_circt` and `bugloop_repair` and not on `bugloop_llm`, so every run through `run_campaign` was refused with `bugloop_llm cannot write ${BUGLOOP_ARTEFACTS}`. W-19a ran no pre-flight and W-19b drives `campaign_drive` directly, so the pilot was the first run to reach check 5. `T-U-cluster-04` now asserts the mount on all three types | 03 §12.1's `cluster_single.yaml`; `T-U-cluster-04` |
| 44 | **FR-10.9's "or an existing one is reused" could never be taken, and check 11 demanded a forum post of a run that cannot file. BOTH FIXED.** `issue_mirror_meta` is keyed by `run_manifest_id` and written by `write_run_rows`, so only a previous DRIVER run can fill it; a mirror built by `triage_task.issue_mirror_refresh` called any other way - which is how A7's offline synthesis builds one (§8.3) - fills `issue_mirror` and leaves the meta table empty, and the driver then refreshed from GitHub on every first run. `_mirror` now falls back to the mirror TABLE, every field read off the rows, and an empty table still returns None so check 10's refusal is untouched. Check 11 exempts a run whose REGISTERED `filings_total` is zero: FR-20.1 posts the method so maintainers are not met by reports from a system they were never told about, and a run that can file nothing produces no report for anyone to be surprised by. The contract is frozen at `contract-2.1` and makes `RunManifest.forum_post_url` and `forum_post_date` non-null, so an exempt run's manifest records WHY there is no post rather than a URL nobody posted | 01 FR-10.9, FR-20.1; 03 §13.1's checks 10 and 11; 02 nothing |
| 45 | **`--seed-sha`, the pilot's seed subset.** A pilot drives a NAMED subset of the corpus, because a short window over the corpus's own order measures whichever seeds `build_corpus` happens to emit first. It narrows only the list `campaign_drive` is handed - the mined corpus, its exclusions and the store's 187 `seed` rows are unchanged - and a SHA the corpus does not hold REFUSES the run, a pilot that quietly drove eleven of twelve being a pilot that reports eleven as though twelve had been asked for. It is a run-time argument and not a registered parameter, and the pilot's record states the twelve SHAs | 03 §13.1's argument table; 05's W-18 row; `T-U-driver-44` |

## W-18b (2026-09-16): the fix round after pilot 1, and pilot 2

Errata rows 37 and 38, FR-11's stage-6 turn, FR-13.3's gate question 2, and the
mini pilot that measures the money control. Six commits, each with its tests.
Suite at the end: `pytest circt_bug_loop/tests -q --strict-markers -W error -m
"not t2 and not t3"` gives **635 passed, 1 skipped, 16 deselected**.

| # | Finding | Owed to |
|---|---|---|
| 46 | **The spend guard is tool-loop aware, and the ledger says so. Row 38 is CLOSED in code and OPEN on one constant.** `SpendGuard.worst_case_usd` takes the turn REQUEST and sums the whole loop - `sum(prompt_tokens + i * TOOL_OUTPUT_TOKENS_CAP for i in range(n))` at the input rate plus `n * max_output_tokens` at the output rate, with `n` the stage's registered `max_tool_iterations` for a turn with tools and 1 for one without - and the figure it authorises is handed to the patched backend as `turn_budget_usd`, which refuses the next `generate_content` call from the inside when the priced spend so far plus that call's estimate would pass it. **MEASURED, pilot 2**: two stage-1 turns, authorised USD 0.742750 and 0.752764, billed 0.369592 and 0.444723, six calls each - **billed/authorised 0.498x and 0.591x, max 0.591, mean 0.544, no turn over 1.0 and no turn at the ceiling**, against pilot 1's 19.7x, 32.3x and 64.1x. Total spend USD 0.814315 against a registered cap of 3.00. `LedgerEntry.observed` carries `authorised_usd`, `ceiling_usd`, `billed_usd` and `calls` per turn (contract 2.2) and `render_results` prints the max and the mean. **OPEN**: `TOOL_OUTPUT_TOKENS_CAP` was 32768, chosen from pilot 1's single measurement of 16,900 tokens per tool result; pilot 2 measured the same quantity at **29,378 and 36,286**, so 32768 was 11 % BELOW one of its own inputs and was not a cap. It is 65536 now - the largest of the three rounded up - and the authorisation held at 32768 only because the output side over-estimates by about 29x (96,000 tokens authorised against 3,302 billed), which is luck and not design | 03 §3.5.1's `SpendGuard`; 02 §2.9's `observed`; 01 FR-14.1's key list. Closes row 38 in code |
| 47 | **`max_tool_iterations: 6` STOPS STAGE 1 ANSWERING, measured on both seeds of pilot 2, and the seeded arm wrote no probe at all.** The cap is the architect's registered value and it is binding, so it was not changed; this row is the measurement it was chosen without. Both stage-1 turns used all six `generate_content` calls on `SourceReadTool` reads, hit `[DEBUG] Reached max_tool_iterations=6` with `final_text` EMPTY, and `parse_json_footer` then raised `no_block`; A3 recorded the stage-1 failure and never ran stage 2, so the seeded arm drove two seeds, wrote **0 probes** and ended `seed_set_exhausted` in 179.6 s of its 600. Pilot 1's stage-1 turn made **14** calls at a cap of 100 and did answer. The money control is not implicated - it never fired, and the turn was truncated by the ITERATION cap - but `max_tool_iterations` for stages 1 and 2 must be raised before W-23 or the campaign generates nothing, and the value is a registered parameter and therefore the architect's | 09's `max_tool_iterations`; 05's W-22/W-23 rows. **OPEN, and blocking** |
| 48 | **The stage-6 model turn is made only for a `new` candidate. FIXED.** `dedup_and_screen` already ran before `triage_report`, so its verdict is known inside the node, and for every other verdict FR-11.2's tool verdict already fixes the classification and FR-13.5 already refuses the candidate at gate question 4: the turn could change neither and cost a whole tool loop. Pilot 1 paid **USD 3.956805** for one such turn over a candidate the screen had matched to open issue #8508, and the prose it bought disagreed with the match and was overruled by FR-11.2 in the same function. The report is still rendered from the record, with `NO_TURN_PROSE` in the three prose slots and no token charged; `known_issue` for the three issue verdicts, the new `duplicate` for `duplicate_of_candidate` and `untriaged` for `dedup_unavailable`. `Report` is not a contract member, so the new classification costs no version bump. NOT exercised live: pilot 2 produced no candidate | 01 FR-11.2, FR-11.8; 03 §3.7's B7 |
| 49 | **Gate question 2 passes a case that is already minimal. FIXED.** `reduced=false` with `fixpoint=true` is the reducer saying the case CANNOT be made smaller, and FR-13.3 read it as `not_minimal`: pilot 1's `circt-reduce` made 20 interestingness calls on a six-line `moore.net` module, removed nothing, and all three of its candidates - one real CIRCT assertion, found on the first seed - were refused over a case the reducer had just proved irreducible. It now fails only when the fixpoint was NOT reached or the case is larger than `minimal_case_lines`, a new `[DEFAULT]` budget key at 12. `already_minimal` is a fifth question-2 value and is a PASS, so it never reaches `_q2_stopping_value` and FR-18.6's taxonomy is unchanged; an unreadable case is not called minimal. `T-U-gate-09c` drives pilot 1's own six-line case to a pass and a forty-line one to `not_minimal`. NOT exercised live: pilot 2 produced no candidate | 01 FR-13.3, FR-18.6; 03 §3.9 |
| 50 | **Row 37's two halves, both FIXED, and a third thing the reproduction found.** Pre-flight now removes `/tmp/ray/ray_current_cluster` when nothing answers on the address it names, before `ray.init`; nothing is removed when a cluster answers or when this process already holds one. The backend half is in `upstream/vertex-usage.patch`: `_get_node_id` asks Ray nothing when `ray.is_initialized()` is false and gives up on the answer after `NODE_ID_TIMEOUT_SECONDS` = 10 when it is. **The `is_initialized` guard is the load-bearing one and was not in the first draft**: MEASURED while writing `T-U-upstream-06`, a process with no Ray that calls `ray.get_runtime_context()` blocks sixty seconds and then Ray's own client calls QuickExit - `Failed to connect to GCS within 60 seconds ... The program will terminate.` - and it killed the pytest process, so a worker thread with a ten-second bound answers "unknown" and is then taken down with everything else ten seconds later. Confirmed in passing that `chia down` leaves the file behind: it was present after the teardown of 2026-09-16 19:58 UTC+2 | 03 §13.1's pre-flight; CHIA's `chia/models/vertex.py`. Closes row 37 |
| 51 | **Contract 2.2**, MINOR: two `BudgetFile` keys (`max_tool_iterations`, a dict over the four model-bearing stages; `minimal_case_lines`, an int) and four `LedgerEntry.observed` keys (`authorised_usd`, `ceiling_usd`, `billed_usd`, `calls`). Every `contract_version` string under `contract/fixtures/` and `tests/fixtures/` moved to 2.2 and the two `BudgetFile` and every `LedgerEntry` fixture gained the new keys; no other byte of any fixture changed. `contract/FROZEN.md` carries the section. **Owed**: the freeze rule asks for an annotated `contract-2.2` tag, which this round does not cut, and 03 §2.4/§9.1, 01 FR-14.1 and 02 §2.9 are owed the six keys | 03 §2.2's version rule; `contract/FROZEN.md` |
| 52 | **`calibration_sample_size` is ZERO in the campaign's own budget.yaml, and it is a measurement.** W-17's draw put twenty exact-pin seeds in the file; `bug_loop.calibratable`'s own docstring has recorded since 2026-09-15 that **0 of the 187 seed parents carry this deployment's pin** `6279700538792da0c5a08e17babfe9b6e824c69f`, the 187 being spread over 44 distinct pins. So those twenty name seeds calibration mode refuses to probe, the eligible set being empty, and the campaign is registered as discovery mode with an empty sample. `T-U-byaml-04` still pins `draw_calibration` to `random.Random(corpus_head_sha)` over the committed 187-seed corpus, at a size of twenty it supplies itself | 01 ADR-D-01(d); 09's calibration block; 05's W-22 row |
| 53 | **`arm_window_seconds` is 7200 and not 14400 in the final budget.yaml.** Pilot 1 spent about USD 0.6 a minute of wall clock on the seeded arm, so two four-hour arms are USD 288 against a registered cap of 200 and the money would stop the campaign inside its first arm, which is what happened to pilot 1 at 43.3 % over. Two hours an arm, four for the campaign. The window is the BUDGET (G-48) and is fixed before the campaign from the pilot's own rate and the registered cap, not chosen to fit a result. The W-18b fixes are all expected to cut the rate and none of them is measured at campaign scale, so the number errs toward the cap | 09's `arm_window_seconds`; 05's W-22 row |
